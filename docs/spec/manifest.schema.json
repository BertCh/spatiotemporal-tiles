{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://poopdeck.gl/spec/manifest.schema.json",
  "title": "STT packed-format manifest",
  "description": "Canonical schema for the per-dataset `manifest.json` of the STT packed format (formatVersion 1). This is the cross-language wire contract: the Rust writer (`crate::pack::Manifest`) and every reader (TS `@poopdeck.gl/core` `PackedManifest`, Rust `PackedReader`) MUST agree with this document. Unknown properties are permitted at every envelope level (additive evolution within formatVersion 1): readers MUST ignore fields they do not recognize. See docs/spec/stt-packed-format.md.",
  "type": "object",
  "required": ["format", "formatVersion", "compression", "directory", "packs", "metadata"],
  "properties": {
    "format": {
      "description": "Format discriminator. Always the literal `stt-packed`.",
      "const": "stt-packed"
    },
    "formatVersion": {
      "description": "Manifest schema version. This schema describes version 1. Distinct from the directory codec version (see `directory.directoryVersion`) and the tile-payload encoding.",
      "const": 1
    },
    "compression": {
      "description": "Per-blob compression codec. Every tile blob is compressed independently (no shared zstd dictionary), so a streaming reader can decode any blob in isolation. The format is zstd-only: `zstd` is the default and only real codec (it beats gzip on both ratio and browser-side decode speed, so no writer has ever emitted gzip), and `none` exists only for already-incompressible blobs. The canonical reader (Rust `PackedReader`) rejects any value outside this set.",
      "enum": ["zstd", "none"]
    },
    "directory": {
      "description": "Pointer to the single immutable, content-addressed directory object (the v5 run-length index). The object is either a single whole-load buffer (default) or a paged container — root page + leaf pages — selected by `layout`. Either way the leaf codec is v5; `layout`, not `directoryVersion`, discriminates the container shape.",
      "type": "object",
      "required": ["key", "length", "directoryVersion"],
      "properties": {
        "key": {
          "description": "Object key relative to the dataset root. blake3-128 content address of the at-rest object bytes (the compressed bytes when `encoding` is set).",
          "type": "string",
          "pattern": "^index/[0-9a-f]{32}\\.sttd$"
        },
        "length": {
          "description": "Directory object length in bytes — the at-rest object, i.e. the compressed length when `encoding` is set. Readers validate the fetched body against it before any decode.",
          "type": "integer",
          "minimum": 0
        },
        "directoryVersion": {
          "description": "Directory (leaf) codec version. The packed format (formatVersion 1) emits v5 (per-run pack_id column + pack-relative offsets). Unchanged by the paged container.",
          "const": 5
        },
        "encoding": {
          "description": "OPTIONAL at-rest encoding of the directory object. `zstd` = the object is a zstd frame (for a paged directory: EACH page — root + every leaf — is its own zstd frame). Absent = raw codec bytes (the shape of every manifest written before this field existed). Readers MUST support both.",
          "enum": ["zstd"]
        },
        "layout": {
          "description": "OPTIONAL container layout. `paged` = a root page (fixed-width geo-bbox + zoom-range + temporal page descriptors) followed by leaf pages, so a cold reader fetches only the leaves its viewport/time-window touches. Absent or `single` = the whole-load object. Present (with the fields below) iff `paged`.",
          "enum": ["single", "paged"]
        },
        "rootLength": {
          "description": "Paged only: at-rest byte length of the root page (a prefix of the object). The reader range-GETs `bytes=0-(rootLength-1)` for the root, then leaf ranges on demand. Leaf descriptor offsets are relative to this (absolute = rootLength + rel_offset).",
          "type": "integer",
          "minimum": 0
        },
        "pageCount": {
          "description": "Paged only: number of leaf pages (informational / validation).",
          "type": "integer",
          "minimum": 0
        },
        "pageEntries": {
          "description": "Paged only: nominal entries-per-leaf-page used at build (informational; the sim-validated sweet spot is 1024–4096).",
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "packs": {
      "description": "Ordered table of immutable, content-addressed pack objects. The index of a pack in this array IS the `pack_id` the directory references. Must be non-empty.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["key", "length"],
        "properties": {
          "key": {
            "description": "Object key relative to the dataset root. blake3-128 content address.",
            "type": "string",
            "pattern": "^packs/[0-9a-f]{32}\\.sttp$"
          },
          "length": {
            "description": "Pack object length in bytes. Normally <= the pack target (64 MiB by default); a single blob larger than the target gets its own oversized pack and may exceed it.",
            "type": "integer",
            "minimum": 0
          }
        }
      }
    },
    "metadata": {
      "description": "The full stt-core `Metadata` JSON, folded in verbatim (snake_case keys) so a cold reader needs no separate metadata object. Opaque to this schema except for the OPTIONAL `style_hints` block pinned below; see crate::metadata::Metadata.",
      "type": "object",
      "properties": {
        "style_hints": {
          "description": "OPTIONAL build-time style hints (additive; written by `stt-build --style-hints`). Per-property statistics plus archive-level rendering DEFAULTS a renderer may adopt and a user can always override. Old readers are unaffected — absent means no hints.",
          "type": "object",
          "required": ["version", "properties"],
          "properties": {
            "version": {
              "description": "Style-hints block schema version. This schema describes version 1.",
              "const": 1
            },
            "properties": {
              "description": "Per-property statistics. NUMERIC properties carry the percentile fields plus `suggested_domain` (`cardinality` absent); CATEGORICAL (string) properties carry ONLY `name` + `cardinality` — the numeric fields are absent from the JSON, not null-filled.",
              "type": "array",
              "items": {
                "type": "object",
                "required": ["name"],
                "properties": {
                  "name": {
                    "description": "Property (tile column) name.",
                    "type": "string"
                  },
                  "min": { "description": "Minimum observed value (numeric only).", "type": "number" },
                  "p50": { "description": "50th percentile (numeric only).", "type": "number" },
                  "p90": { "description": "90th percentile (numeric only).", "type": "number" },
                  "p95": { "description": "95th percentile (numeric only).", "type": "number" },
                  "p97": { "description": "97th percentile (numeric only).", "type": "number" },
                  "p99": { "description": "99th percentile (numeric only).", "type": "number" },
                  "max": { "description": "Maximum observed value (numeric only).", "type": "number" },
                  "suggested_domain": {
                    "description": "Suggested render domain `[min, p97]`, each endpoint rounded OUTWARD to 2 significant figures. p97 (not max) encodes the project's manual 'domain clamps at ~p97' tuning convention — a single outlier must not dim the whole ramp (numeric only).",
                    "type": "array",
                    "items": { "type": "number" },
                    "minItems": 2,
                    "maxItems": 2
                  },
                  "cardinality": {
                    "description": "Distinct-value count (categorical only; the profiler caps it at 10000, so treat the cap value as 'at least this many'). Tolerates `null` for writer flexibility, but the canonical writer omits the key on numeric properties.",
                    "type": ["integer", "null"],
                    "minimum": 0
                  }
                }
              }
            },
            "suggested_playback_seconds": {
              "description": "Suggested playback duration in seconds: `clamp(round(sqrt(bucket_count)), 20, 90)` where `bucket_count` = time-range duration / `temporal_bucket_ms`. Absent when the bucket size is unknown/zero.",
              "type": "integer",
              "minimum": 20,
              "maximum": 90
            },
            "layer_hint": {
              "description": "Dominant produced layer kind (absent when none could be derived). Lines with per-vertex times hint `trips`; timeless lines hint `paths`.",
              "enum": ["points", "paths", "trips", "polygons"]
            }
          }
        }
      }
    }
  }
}
