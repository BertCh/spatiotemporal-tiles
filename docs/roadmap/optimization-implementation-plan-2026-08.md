# Optimization implementation plan: specs, tests, and evaluation (2026-08)

_2026-08-10 · third of the trilogy: [optimization-problems-2026-08.md](./optimization-problems-2026-08.md) is the diagnosis (66 formal problems), [optimization-informed-design-2026-08.md](./optimization-informed-design-2026-08.md) is the treatment (six defects, eight mechanisms M1–M8, phases 0–5, the do-not-touch register) — and this record is the execution spec: 68 work items with concrete code changes, test plans, and evaluation criteria, each grounded in the current tree._

_House-rules note: this is a decision record — it owns the **how**, not the schedule. Nothing here is scheduled until its line lands in [the backlog](./README.md); the design doc's §7 graduation lines (O1–O5) remain the vehicle, and P0-8 below resolves each O-line's acceptance criteria to concrete instruments. Where this record and the design doc disagree, the design doc's mechanism definitions and do-not-touch register win._

_Status 2026-08-26: 67 of these 68 items shipped in commit `d5163aa` — the same commit that added this file — so every item below is written in the PROPOSING tense about a tree that has since moved, and the `(new)` markers describe 2026-08-10. The item-level record, the open defects, and the source-document defects found on the way are in [optimization-conformance-2026-08.md](./optimization-conformance-2026-08.md). **DT-3** (`crates/stt-build/src/reduced_tier.rs`) is the one item not started. One artifact of a LANDED item is missing too: SH-1's **`scripts/validate-fleet.sh`** never shipped, so the fleet-wide `K2 → 0` acceptance in P0-8's O4 row has no instrument._

## How to read a work item

Every item follows one skeleton:

- **Change** — modules, functions with proposed signatures, CLI flags, schema fields. Symbols verified in the current tree are cited `file:line`; proposed ones are marked (new); anything the planning pass could not verify is marked (unverified) and must be checked before implementation.
- **Architecture** — where the piece sits, the invariants it must preserve (determinism, no-thinning, random access, bounded client memory), its byte status, and the rollback story. **BYTE-NEUTRAL** items may ship any time behind the existing gates; **BYTE-CHANGING** items ride rebuild window **R1** (one fleet republish, design doc P6) — no exceptions.
- **Tests** — enumerated unit / property / golden-fixture / simulation / integration cases, naming the existing test files to extend and the new ones to add. Every solver or encoder change carries a determinism test (byte-identical re-run); rejected-design boundaries get cheap guard tests.
- **Evaluation** — instrument, metric with units, recorded baseline where one exists, acceptance threshold, and the dataset or route to run on. **NEEDS-HARNESS** points into §1 (Phase 0), which must supply the instrument before the item's acceptance can be judged.
- **Risks & dependencies** — what invalidates the item, what it depends on, and which do-not-touch entries it borders.

Dependency spine: §1 (Phase 0) is the precondition for judging everything else; §2–§3 are the byte-neutral client program (O1); §4–§5 are the oracle/advisor upgrades (O2) that unlock the flagship (O3); §6 lands first inside Phase 4 and gates §7's rebuild window (O4); §8 is the trigger-gated spec and selection track (O5).

## Contents

| §   | Section                                                              | Mechanism · Phase        | Items                    |
| --- | -------------------------------------------------------------------- | ------------------------ | ------------------------ |
| 1   | Phase 0 — measurement infrastructure, harnesses & CI gates           | preconditions · Phase 0  | P0-1 … P0-8              |
| 2   | The client cost-oracle pass _(downstream)_                           | M5 · Phase 1             | CO-1 … CO-7              |
| 3   | Byte-honest budgets, decode continuity & cache repair _(downstream)_ | M6 · Phase 1             | BH-1 … BH-10             |
| 4   | Measurement oracle v2 & the `--target-size` flagship                 | M1 + M3 · Phases 2–3     | MO-1 … MO-9              |
| 5   | The workload model, ordering default & storage-layout DPs            | M4 · Phase 2             | WM-1 … WM-6              |
| 6   | Semantic validation & manifest honesty                               | M7 · Phase 4 gate        | SH-1 … SH-6              |
| 7   | The two-pass build & the R1 builder batch                            | M2 + design §4 · Phase 4 | TB-1 … TB-14             |
| 8   | Declared tiers & frustum selection _(FS-1 … FS-3 downstream)_        | M8 · Phase 5             | DT-1 … DT-5, FS-1 … FS-3 |

---

## 1. Phase 0 — measurement infrastructure, harnesses & CI gates

Everything later in this program is accepted or rejected against a number, so Phase 0 builds and repairs the instruments before any controller or encoder changes: the harness/gate inventory the design doc demands (design §6 Phase 0), the QoE counter set Phase 1's acceptance criteria assume, the K10 and §11.6 measurements whose absence currently blocks recorded decisions, and the evaluation matrix that binds every O1–O5 acceptance criterion to a concrete (harness, metric, baseline, threshold). Every item here is BYTE-NEUTRAL — no archive byte moves in Phase 0 — and everything respects the boundary stated at the end of this section: automated evaluation reads counters, bytes, and frame timings; the user verifies demo aesthetics in-browser themselves, and no automated screenshot-judgment loop is built.

#### P0-1 — Harness and CI-gate inventory, with the repair list `§13.4 · P1/P2 preconditions · Phase 0`

**Change.** A verification pass over every measurement instrument the later phases cite, recorded as a short standing section in a record this repository owns — `poopdeck:docs/roadmap/measurements-2026-08.md` moved downstream with the harnesses it inventories in the 2026-08-26 split, so the format-side half belongs here or in [optimization-conformance-2026-08.md](./optimization-conformance-2026-08.md) — plus the small repairs found. Verified state of each named harness:

- **Frame cost** — `poopdeck:tools/bench/src/frame-cost.mjs` EXISTS. Invocation: `node src/frame-cost.mjs <demo-id> [seconds] [port]` with `WARMUP_MS=` and `ROUTE=` for non-`/demo/:id` surfaces (`ROUTE=/drive node src/frame-cost.mjs drive 12 3000`, frame-cost.mjs:127–128; also `pnpm --filter @poopdeck.gl/bench bench:frame-cost`). Output: JSON to `OUT_DIR` (default `poopdeck:tools/bench/out`) — fps, p50/p95 frame ms, plus browser-boundary GL counters (`draws`, `bufferSubData`, `bufferData`, `bindVAO`, `useProgram`; frame-cost.mjs:84–115). Caveat carried in the header: dev-server only, so JS-side percentage shares are inflated; GL counters are build-independent. Owner: Phase 1 acceptance (O1).
- **Cold start** — `poopdeck:tools/bench/src/cold-start.mjs` EXISTS. Invocation: `pnpm --filter @poopdeck.gl/bench bench:cold-start -- [dataset ...] [--json] [--verbose] [--repeat N] [--base URL] [--cache-bust]` (`poopdeck:docs/roadmap/measurements-2026-07.md`:12–17). Drives the real `@poopdeck.gl/core` `STTArchive` over HTTP with an instrumented fetch (requests, byte ranges, `cf-cache-status`). Owner: P0-4 (K10), then M4/§7 pricing in Phases 2/4.
- **QoE counters** — `PlaybackGovernor.getQoeStats()` EXISTS (poopdeck:packages/playback/src/playback-governor.ts:1067; `PlaybackQoeStats` = `stallCount`, `totalStallMs`, `startupMs`, `degradedResumeCount`, `creepMs`, :261–278), snapshotted onto the `__sttProbe` `playback` channel at every state transition (:1407, :1485, :1611). Per-source runways via `getSourceRunways()` (`SourceRunway.bytesPending`, `runwaySimMs`). Owner: Phase 1 (O1); audited/extended in P0-2.
- **Ordering simulator** — `crates/stt-core/src/ordering_sim.rs` EXISTS: pure deterministic range-read simulator over the two canonical queries, reader-matched `DEFAULT_COALESCE_GAP_BYTES = 2 MiB` (:30) and per-pack coalescing close (`DEFAULT_PACK_BYTES`, :40). Two callers: `--blob-ordering measured` (`PackWriter::with_measured_ordering`) and `stt-optimize order-audit`. Owner: M4 (Phase 2) — the playback query lands here.
- **stt-optimize inspect/diff** — EXIST (crates/spatiotemporal-tiles/src/bin/stt-optimize.rs: `Inspect` :82 with `--sample N`, `--sample 0` = directory-only; `Diff` :104–130 with `--before/--after/--sample/--format/--fail-on-growth PCT`). The diff growth gate reads the exact directory metric — per design M1, **do not touch it**. Owner: M1 (Phase 2), R1 acceptance (Phase 4).
- **Pitch×bearing selection matrix** — EXISTS: `poopdeck:packages/layers/test/chassis-viewport-bounds.test.ts` (:208–234, "The pitch × bearing matrix — 24 × 18 real cameras" driven through a real `WebMercatorViewport` into `layer.getViewportBounds`), backed by `poopdeck:packages/core/test/viewport-bounds-normalize.test.ts`, `degenerate-bounds-scan.test.ts`, `polar-tile-row-collapse.test.ts`. Owner: O5 coverage-correctness gate.
- **Bench regression baseline** — `poopdeck:tools/bench/src/index.mjs --check poopdeck:tools/bench/baselines/earthquakes-ci.json --tolerance 0.15` against the committed 733 KB `earthquakes-ci.stt` fixture (ci.yml `bench-regression` job).
- **Showcase probe** — `@poopdeck.gl/render-test` (`poopdeck:tools/render-test`) load-probes every demo (ci.yml `showcase-probe`); it is a fail-closed _loads-at-all_ gate, not an aesthetics judge.

CI/test gates, re-verified 2026-08-26 in `.github/workflows/ci.yml` (295 lines) — ten jobs, all Rust, Python or Node: `rust` (default-feature `cargo test --workspace --locked`, ci.yml:9), `rust-all-features` (:27), `rust-lint` (`cargo fmt --check` + the curated clippy deny set, :49), `rust-feature-lanes` (six `cargo check` lanes, :96), `rust-package` (`cargo package --workspace` dry-run, :119), `rust-msrv` (**1.88** — `Cargo.toml:39`; :134), `rust-duckdb` (+ the ignored spatial SQL test, :151), `rust-postgres-parity` (PostGIS service container, :183), `python` (the AV-palette contract check plus two `av_common` suites, :212), and `gates` (:240 — sync-versions, project-status, `check-doc-links.mjs`, the **roadmap-citation gate** `.github/scripts/check-roadmap-citations.mjs` at :271, the golden-pin gate at :279 with its self-test at :283, the stt-generate inventory, then `pnpm lint` / `pnpm format:check`). The `typescript`, `typescript-lint`, `bench-regression` and `showcase-probe` jobs this item named in 2026-08-10 are **gone from this repository** — they moved to poopdeck.gl with the packages they gate, and so did their test counts; this repository's green definition is the "last whole-repo green baseline" paragraph in [the backlog](./README.md). **Corrected honesty note (backlog T2, corrected 2026-08-26): GitHub Actions HAS run — 174 times — and had been failing.** The claim this item carried for a month, that the gates were config only ever run by hand, was wrong; the correct diagnosis was never "the gates do not run" but "nobody was reading the runs", which is worse — real defects were reported into an empty room. Both standing reds are now fixed: `rust-msrv` (the published MSRV sat below `geo` 0.33's floor) and the `rust` lane's linker OOM (debuginfo disabled in CI). The repair list therefore reduces to: (a) read the runs — "CI-enforced" below now means what it says; (b) frame-cost gains no repair (ROUTE support already landed); (c) no other harness needed repair.

**Architecture.** Documentation plus at most script-level fixes; sits entirely outside the data path. Preserves determinism trivially (nothing executable changes). BYTE-NEUTRAL, ships anytime. Rollback: revert the doc edit.

**Tests.** None new — this item _is_ the audit. The inventory section records, per harness: exists?, invocation, output format, owner phase (the table above is its content).

**Evaluation.** Complete when every harness row in P0-8's matrix resolves to a verified path + invocation, and the T2 status (Actions run or still hand-run) is stated in the inventory.

**Risks & dependencies.** T2 is discharged (corrected 2026-08-26 — Actions had run 174 times and was standing red; both failing lanes are fixed), so "CI-enforced" in this program now holds. The residual risk is the one T2 actually exposed: a green definition nobody reads. Borders the do-not-touch register only in what it documents (e.g. diff's exact gate metric).

#### P0-2 — QoE counter audit and the telemetry extension `§9.4 §9.5 §10.1 §10.2 §11.2 §11.6 · serves M5/M6 · Phase 0`

**Change.** One telemetry work item covering the audit (what exists) and the additions (what Phase 1 acceptance needs). Exists today, verified:

- `PlaybackQoeStats` (`stallCount`, `totalStallMs`, `startupMs`, `degradedResumeCount`, `creepMs`) + `SourceRunway.bytesPending` — playback-governor.ts.
- Tileset `cacheStats = { hits, misses, evictions, runwayEvictions }` (poopdeck:packages/core/src/spatiotemporal-tileset.ts:948–956; `runwayEvictions++` at :3970 — the fetch-evict-refetch thrash signal O1 targets).
- Archive-level `cacheStats = { hits, misses, evictions }` + `getCacheStats()` (poopdeck:packages/core/src/archive.ts:1119, :3790).
- Probe channels: core `'decode' | 'tilePrepare'` (poopdeck:packages/core/src/telemetry.ts:29, 4096-sample ring), layers `'consolidations' | 'renderLayers'` (poopdeck:packages/layers/src/lib/telemetry.ts:33–35), governor `'playback'`. Decode samples already carry main-thread→worker→main-thread queue latency (tile-decoder.ts:331–336) — the §10.1 "emitted and never fed back" oracle.
- OPFS index entries carry `{ bytes, lastAccess }` only (poopdeck:packages/core/src/opfs-cache.ts:28, :43) — **no per-entry hit count**, which §9.5's GreedyDual-Size/admission work needs.

Additions (all (new), all behind the existing `__sttProbe.enabled` no-op guard so production paths stay a property-read):

1. `CoreProbeChannel` gains `'requests' | 'evict' | 'scrub'` members (poopdeck:packages/core/src/telemetry.ts:29). `'requests'`: one sample per tile fetch — `{ key, priority, bytes, enqueuedAt, dispatchedAt, completedAt, source }`, emitted from the request scheduler. `'evict'`: `{ key, tier, bytes, playheadMs }` emitted from `evictUnusedTiles` next to the existing `runwayEvictions` increment (spatiotemporal-tileset.ts:3970) — gives the loop-aware-eviction work (§9.4) its per-tier attribution.
2. `SpatioTemporalTileset.getCacheStats(): TilesetCacheStats` (new getter) exposing the existing private counters plus `evictionsByTier: Record<'b'|'c'|'d', number>` and `bytesEvicted` (new fields on the existing `cacheStats` object — extend, not parallel).
3. Scrub counters (`§11.6` / P0-5's instrument): `ScrubQoeStats` (new) `{ timeToFirstPixelMs, freshFrameFraction, bytesDuringScrub, settleMs, tierSwitchCount }`, accumulated between the governor's existing `scrubstart`/`scrubend` events and snapshotted on the `'scrub'` channel; byte attribution from the `'requests'` channel windowed over the drag.
4. Decode-queue aggregation: a `snapshot('decodeQueue', { p50WaitMs, p95WaitMs, pending })` roll-up of the already-emitted per-decode samples, so §10.2's pool-size adaptation has a cheap read.
5. OPFS `OpfsIndexEntry.hits: number` (new, additive JSON field defaulting 0 on read of older indexes) — required by §9.5's "re-access probability estimated from the persisted index at zero I/O".

**Architecture.** Pure client-side observation; no control-flow change, no format change. BYTE-NEUTRAL. Invariants: probe-off paths remain single-property-read no-ops (telemetry.ts:36–46 pattern); ring caps (`MAX_SAMPLES = 4096`) bound memory; the OPFS index field is additive and must round-trip older indexes unchanged — the `tileKey` format is an OPFS persistence contract and is NOT touched. Rollback: counters are additive; removing them breaks only the harnesses that read them.

**Tests.** Unit: extend `poopdeck:packages/core/test/eviction-playhead-tiers.test.ts` (exists) to assert `'evict'` samples carry the tier that freed the bytes; extend `poopdeck:packages/core/test/scrub-lod.test.ts` (exists) with `ScrubQoeStats` accumulation over a synthetic drag; extend `poopdeck:packages/playback/test/playback-governor.test.ts` (exists) for scrubstart/scrubend counter windows; new `poopdeck:packages/core/test/telemetry-channels.test.ts` for channel no-op-when-disabled and ring-cap behavior. Property: hits/lastAccess monotonicity in `poopdeck:packages/core/test/archive-opfs.test.ts` (exists). Golden-fixture: none (no bytes change). Guard test: probe disabled ⇒ zero allocations on the request path (assert bag untouched).

**Evaluation.** Meta-metric: every Phase 1 item in O1 names a counter that exists after this lands (checked against P0-8's matrix). Overhead check: frame-cost run on `storm-4d-greenfield` with probe on vs off, threshold: fps delta within run-to-run noise (< 3 fps against the 86.8 fps baseline, `poopdeck:docs/roadmap/measurements-2026-08.md`:33).

**Risks & dependencies.** Depends on P0-1 only. Feeds P0-3 (the trace format is these channels serialized) and P0-5. Borders §10.2's rejected designs (copy-at-enqueue; worker-side cancel queues) — observation only, no scheduling change rides in with the counters.

#### P0-3 — Deterministic client-policy trace-replay harness `§9.1–§9.5 §10.2 §11.1–§11.4 · M5/M6 acceptance instrument · Phase 0`

**Change.** The existing setup is _not_ sufficient for before/after policy comparison: frame-cost and live QoE runs are browser sessions against a real network — non-reproducible run-to-run, so a policy A/B drowns in transport noise; cold-start stops at first frame and never exercises playback policies. Since Phase 1 changes are precisely policy changes (horizon feasibility, byte-metered DRR, loop-aware eviction, decode priority), build the minimal offline replay: **record on the composite routes, replay against policy variants deterministically.**

- **Recorder:** `poopdeck:tools/bench/src/policy-record.mjs` (new), a frame-cost-mjs-style Playwright driver that enables `__sttProbe`, plays a route for N seconds, and dumps the P0-2 channels (`requests`, `decode`, `evict`, `playback`, plus viewport/playhead trajectory snapshots) as a JSON-lines trace to `poopdeck:tools/bench/out/traces/<route>-<date>.jsonl`. Routes: `severe-weather` (radar+fronts composite), `storm-4d-greenfield`, `ROUTE=/drive`.
- **Replayer:** `poopdeck:tools/bench/src/policy-replay.mjs` (new): `node src/policy-replay.mjs <trace.jsonl> [--archive <path-or-url>] [--variant <name>] [--json]`. Loads the real archive _directory_ through `@poopdeck.gl/core` in Node (reusing the existing `loader-hook.mjs` resolution shim the cold-start harness already registers), replays the recorded playhead/viewport trajectory against the pure policy surfaces on a mock clock, and reports: bytes fetched, request count, refetch cycles (fetch-evict-refetch of the same key), `runwayEvictions`, predicted runway violations, decode-queue wait distribution. The hook point is already right: `PrefetchPolicy` is a pure decision object — "the tileset supplies measurements and performs all the I/O the policy's answers imply; the policy itself never fetches or schedules" (spatiotemporal-tileset.ts:958–964, `poopdeck:packages/core/src/prefetch-policy.ts`) — and the eviction tiering and governor are plain TS classes already instantiated in Node by the vitest suites.

**Architecture.** Offline, deterministic: same trace + same policy variant ⇒ byte-identical report (integer counters, no wall clock, no RNG) — this is what makes it the arbiter for O1's "named QoE metrics move" claims. Sits beside bench in `poopdeck:tools/bench/src/`; consumes the archive read-only via the directory (the M5 oracle — replay cost estimates come from directory byte sums, never a size model, honoring the anti-model rejection). Reported cost is always the blended `bytes + g·reads` form — **never request count alone** (do-not-touch: the 669 MiB request-count incident, design §5). Replayed queries are the recorded client trajectory, so the rejected whole-map-instant surrogate cannot sneak back in. BYTE-NEUTRAL. Rollback: it is a tool; delete it.

**Tests.** Unit: trace parser round-trip; mock-clock monotonicity. Simulation: a hand-written 20-event micro-trace with known optimal behavior per policy (e.g. a loop-boundary trace where the current eviction demonstrably refetches — the §9.4 "inverse of Belady" pathology — asserted as the incumbent's baseline output, so the Phase 1 fix has a pinned before). Determinism: replay the committed micro-trace twice, assert byte-identical JSON output (the solver-determinism rule of P0-7 applied to this harness). Integration: record 10 s of the `earthquakes-ci.stt` fixture route locally, replay, assert conservation invariants (bytes fetched ≥ bytes decoded ≥ bytes evicted + resident). New files only: `poopdeck:tools/bench/src/policy-record.mjs`, `poopdeck:tools/bench/src/policy-replay.mjs`, `poopdeck:tools/bench/test/policy-replay.test.mjs`, one committed micro-trace fixture.

**Evaluation.** Self-evaluation: the determinism test above, plus one recorded trace per composite route checked into `poopdeck:tools/bench/out/traces/` metadata (traces themselves are local artifacts, listed not committed if large). Baseline `runwayEvictions` and refetch-cycle counts per route come out of the first recording pass — these are the NEEDS-BASELINE cells in P0-8's matrix, filled here.

**Risks & dependencies.** Depends on P0-2 (channels are the trace format). Risk: replay fidelity — the replayer models policy decisions, not GPU/render feedback; anything render-coupled (frame pacing) stays with frame-cost, and the harness must state that boundary in its header. Borders the §10.2 rejected designs and §11.4's aggregate-rate recovery formula (replay variants may not re-litigate them; the do-not-touch register is checked in review).

#### P0-4 — K10: the cold-start re-capture against the republished fleet `§13.4 · baseline for M4/§7 pricing · Phase 0`

**Change.** _Downstream since 2026-08-26: K10 now lives in `poopdeck:docs/roadmap/README.md` and the cold-start harness is `poopdeck:tools/bench/src/cold-start.mjs`, so this item is not this repository's to execute. The fleet it prices has served `formatVersion: 3` + directory v6 + a variants registry since 2026-08-14 ([the backlog](./README.md), entry B4; `crates/stt-core/src/pack/mod.rs:63`), so the v2-era numbers below describe a fleet that no longer exists — they are kept as dated evidence, not as baselines._ Execute backlog K10: re-run `poopdeck:tools/bench/src/cold-start.mjs` against the republished 68/68 `formatVersion: 2` fleet. Procedure, exact:

```sh
pnpm --filter @poopdeck.gl/bench bench:cold-start -- --verbose --repeat 5
pnpm --filter @poopdeck.gl/bench bench:cold-start -- --cache-bust --repeat 5   # edge-MISS state
```

over the original three (`earthquakes-v2`, `flights`, `goes-glm-lightning`) plus the datasets that 404'd during the 2026-07 capture (now live post-B2), against the default `--base` (tiles.poopdeck.gl). Restate or confirm the headline — this supersession has since happened: `poopdeck:docs/roadmap/measurements-2026-07.md` §1 is now a pointer and the live headline is `poopdeck:docs/roadmap/measurements-2026-08.md` §9.1 (eight datasets, 2026-08-10). Include the per-request breakdown that feeds M4: the directory-leaf share of cold-start bytes (54.1–68.6 % on sparse datasets, problems §13.4) is the number every §7 page-breakpoint/descriptor decision prices against.

**Architecture.** Pure measurement; no code change expected (the harness and cameras are recorded as unaffected by the byte break — K10). BYTE-NEUTRAL. The capture is the _pricing baseline_ for M4's directory-page DP (§7.2) and descriptor design (§7.3), and Phase 4 re-runs it after R1 ("re-run the K10 capture so the cold-start record describes the fleet that exists", design §6 Phase 4) — so this run is also the rehearsal of that procedure.

**Tests.** None (measurement run). If the harness needs any fix to run against the v2 fleet, that fix lands with a regression assertion in the harness itself and is recorded in P0-1's repair list.

**Evaluation.** Harness: `cold-start.mjs`. Metrics: requests to first frame (count), bytes to first frame (KiB), leaf share (%). Stale baselines being replaced: earthquakes-v2 **5 req / 335.4 KiB (0.718 %)**, flights **5 req / 5.69 MiB**, goes-glm-lightning **4 req / 250.9 KiB** (captured 2026-07-24/26; `poopdeck:docs/roadmap/measurements-2026-07.md` §1 has since been cut to a pointer at `poopdeck:docs/roadmap/measurements-2026-08.md` §9.1). Acceptance (K10's own): capture re-run, headline restated or confirmed; alarm threshold: any dataset regressing above 6 requests or > 1.5× its stale byte figure is investigated before Phase 2 prices anything on the numbers.

**Risks & dependencies.** Depends on P0-1 only; independent of P0-2/P0-3. Risk: live-fleet dependency (network, edge cache state — mitigated by `--repeat 5` and the `--cache-bust` MISS pass). L1's missing atlas sidecar does not block it (atlas is not in the capture set).

#### P0-5 — The §11.6 scrub measurements: keep-vs-delete evidence for scrub-LOD `§11.6 · design Phase 0 clause (c) · Phase 0`

**Change.** Run the measurement list that the scrub-LOD keep-vs-delete decision hinges on (problems §11.6 Gap: "the keep-vs-delete decision itself hinges on running the specified measurement"; criteria enumerated, unmet, at poopdeck:docs/roadmap/playback-and-loading.md:331–339). New driver `poopdeck:tools/bench/src/scrub-cost.mjs` (new), modeled on frame-cost.mjs: open a demo route, synthesize a pointer drag across the timeline scrubber at 2–3 recorded velocities, read the P0-2 `ScrubQoeStats` + `'requests'` window. Matrix per route: `scrubLod` **absent** (today's shipped state — the baseline) vs `scrubLod: { spatialZoomDrop }` enabled (the spatial axis; the temporal axis stays counted out per §11.6 until M8's reducing tier exists). Routes: the heavy demos the revival trigger names — NYC taxi (~10 M vertices), the AV cockpit (`ROUTE=/drive`), BIXI.

Metrics captured, per the recorded list: **scrub time-to-first-pixel** (`scrubTo` → frame reflecting the new instant), **fresh-frame fraction** (% of drag frames showing current-instant data, any tier), **bytes-during-scrub** vs the no-policy baseline, **settle-to-full-detail latency** (`endScrub` → fine tier resident), **pop/oscillation count** (tier switches per drag), and the rollback drill (**scrubLod off is byte- and behavior-identical**).

**Architecture.** Measurement only; `scrubLod` stays DEFAULT OFF and zero showcase call sites are added — enabling it in the harness page context does not ship. Hard invariants observed, not altered: **preview-never-gates (G7)** and the **restore invariant** are in the do-not-touch register (design §5); the harness asserts they held during every drag (no gate re-arm while the thumb is held, fine tier restored before commit readiness) rather than tuning them. BYTE-NEUTRAL. Output feeds two recorded decisions: the §11.6 keep-vs-delete of the scrub wiring, and M8's scrub-tier trigger (design §3 M8 item 4).

**Tests.** Unit: none beyond P0-2's counter tests. Integration: `scrub-cost.mjs` smoke against the `earthquakes-ci.stt` fixture route (drag executes, all five metrics non-null). Guard: the G7 assertion above is a hard failure of the run, not a metric.

**Evaluation.** Harness: `scrub-cost.mjs` (this item builds it — the only NEEDS-HARNESS entry in Phase 0, discharged here). Units/thresholds from the recorded criteria: time-to-first-pixel target **< one 60 Hz frame (16.7 ms)** from a resident coarse tile; pop count **~1–2** per drag; bytes-during-scrub **must drop** vs baseline (a motion tier that fetches more than full detail is worse than nothing — §11.6's byte-discipline constraint); no single-dataset regression with `scrubLod` off. Baselines: none exist (that is the point); the off-state run is the baseline. Decision rule recorded with the numbers: if enabled-state fails the byte-discipline or fresh-frame criteria on all three heavy routes, the wiring is slated for deletion per §11.6's standing clause.

**Risks & dependencies.** Depends on P0-2 (scrub counters). Risk: the drag synthesizer measuring the harness rather than the policy (mitigate: fixed recorded drag trajectories, replayed identically across variants). Borders do-not-touch: preview-never-gates, restore invariant, and the counted-out Funkhouser–Séquin controller — this item produces the evidence, it does not build a controller.

#### P0-6 — Golden-pin change policy as an enforceable gate `§13.1 · guards M2/R1 · Phase 0`

**Change.** The pins exist and are load-bearing: `crates/stt-core/tests/v2_golden.rs` + `crates/stt-core/tests/fixtures/v2-golden/**` (`expected-hashes.json`, manifests, packs; generator `crates/stt-core/examples/make-golden-fixture.rs`). The rule exists only as prose ("Golden byte pins may change only with intentional, reviewed encoder changes — the pin is the constraint's regression oracle", problems §13.1 Gap; "Golden byte pins change once, intentionally, reviewed", design §6 Phase 4). Make it mechanical: `.github/scripts/check-golden-pins.mjs` (_shipped 2026-08_, sibling of the verified `check-roadmap-citations.mjs`; it runs in the `gates` CI job at ci.yml:279, with its own self-test at :283 — not the `typescript` job named here, which no longer exists in this repository): fail when `git diff <base>..HEAD` touches `crates/stt-core/tests/fixtures/v2-golden/**` or `expected-hashes.json` unless the change is flagged as rebuild-window work — flag = a `Rebuild-Window: R1` commit trailer (greppable locally, works without GitHub PR metadata, consistent with T2's hand-run reality). The script prints the §13.1 rationale on failure so the gate teaches instead of merely blocking.

**Architecture.** Process gate; zero data-path impact. BYTE-NEUTRAL. Invariant protected: determinism's regression oracle cannot drift silently — exactly one intentional pin change is expected in this program (Phase 4/R1, M2's byte break), and this gate makes any other pin movement loud. Note honestly: the current working tree already carries in-flight golden churn from the committed byte-break wave (git status shows `expected-hashes.json` modified and old fixture packs deleted); the gate starts enforcing from the commit that lands it, and that landing commit itself carries the trailer for the in-flight change.

**Tests.** Unit (script-level, node:test or vitest in the scripts dir): touching a fixture path without the trailer fails; with the trailer passes; non-fixture diffs pass. Guard: the script's path list is asserted against the actual fixture directory existing, so a fixture relocation breaks the gate loudly rather than silently ungating.

**Evaluation.** Binary: gate present in ci.yml and in the local green-definition checklist; one deliberate red run (fixture touched, no trailer) recorded as the proof it fires. No dataset/route.

**Risks & dependencies.** Depends on P0-1 (gate inventory). T2 caveat: until Actions runs on GitHub's runners, enforcement is by the same local discipline as every other gate. Borders R1 (Phase 4): the gate must not make legitimate R1 work painful — the trailer is deliberately cheap.

#### P0-7 — Determinism lane: verify, extend to the CLI seam, and bind the rule forward `§13.1 · P5 spine · Phase 0`

**Change.** Verified: a determinism lane already EXISTS and runs under the default `cargo test` jobs — (a) per-tile: `same_tile_encodes_byte_identically` (crates/stt-core/tests/reproducible_build.rs:180, 200 repetitions; formerly `#[ignore]`d under Arrow 54, active since Arrow ≥ 59); (b) whole-dataset, order-independence: `v2_dataset_rebuild_is_byte_identical_including_schemas` (reproducible_build.rs:202 — two builds with different tile add-orders, byte-identical manifest + directory + packs); (c) full pipeline: `crates/stt-build/tests/reproducible_pipeline.rs` (parse → clip → bucket → encode → pack run twice over the same parsed stream, byte-identical archives across multiple packs, rayon-pool emission-order noise included); (d) the byte pin itself (`v2_golden.rs`). So no new lane is invented. Two gaps closed:

1. **The CLI seam.** Nothing double-builds through the _binary_ (arg parsing → input readers → parallel parse → pipeline). The 2026-07-28 ingest refactor's acceptance standard was literally byte-identical archives via `diff -r`; encode that standard as `crates/spatiotemporal-tiles/tests/build_cli_reproducible.rs` (_shipped 2026-08_): run the `stt-build` bin (assert_cmd / `CARGO_BIN_EXE_stt-build`) twice over a committed fixture parquet from `crates/stt-build/tests/fixtures/`, assert recursive byte equality of the two output directories. Feature-gated with the existing `cli` feature so it rides the `rust-all-features` lane.
2. **The rule, bound forward.** Adopt as a stated Phase 0 rule that every solver/encoder change in Phases 1–4 lands with a byte-identical re-run test — M1's stratified sampler (deterministic sampling is contractual, §12.1), M4's workload simulator (pack-name churn otherwise, ordering_sim.rs header), M3's sweep, and P0-3's replayer (already specified there). The rule is recorded in the inventory section from P0-1 and checked at review; it is what "zero nondeterminism (byte-identical re-runs)" in O2's acceptance resolves to.

**Architecture.** Test-only; BYTE-NEUTRAL. Preserves and _documents_ the spine (P5): determinism across processes/machines is the format's economic contract (§13.1's incremental-deploy objective). Rollback: tests are additive.

**Tests.** This item is tests: the new `build_cli_reproducible.rs`; plus one cheap negative guard in reproducible_pipeline.rs's module (exists — extend): assert the test would fail on a byte flip (mutate one pack byte, assert the comparison reports it) so the equality helper itself cannot rot into a tautology.

**Evaluation.** Lane green in `rust` + `rust-all-features` jobs; the CLI double-build runs in < 30 s on the small fixture. Baseline: the lane's historical catches (Arrow-54 metadata order; the FNV migration) are the recorded justification — each cost a full 29.3 GiB / 1,324-object fleet re-upload (§13.1).

**Risks & dependencies.** Depends on nothing in Phase 0; P0-6 references its fixtures. Borders do-not-touch: the total-tiebreak sort keys and pinned degenerate affines (§13.1 incumbent) are what these tests protect — extend, never replace.

#### P0-8 — The evaluation matrix: O1–O5 acceptance criteria resolved to instruments `design §7 · P1/P2 · Phase 0 (centerpiece)`

**Change.** The binding table. Every later section's work item must name its acceptance row here (or add one in the same shape). Lands as a section of a record this repository owns, next to the P0-1 inventory (`poopdeck:docs/roadmap/measurements-2026-08.md` moved downstream in the 2026-08-26 split; the format-side half belongs here or in [optimization-conformance-2026-08.md](./optimization-conformance-2026-08.md)). Baselines cite their measurement source; **NEEDS-BASELINE** cells are filled by the P0-3/P0-4/P0-5 runs before Phase 1 merges anything.

| #     | Acceptance criterion                              | Harness                                                                                           | Metric                                                    | Units                    | Baseline                                                                                                                          | Threshold                                                                                       | Dataset / route                                                                 |
| ----- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| O1    | Horizon feasibility kills eviction thrash (§9.1)  | P0-3 replay + `getCacheStats()` (new getter, P0-2)                                                | `runwayEvictions` per looped-playback session             | count                    | NEEDS-BASELINE (P0-3 recording pass)                                                                                              | **0** under looped playback                                                                     | `severe-weather` composite, `storm-4d-greenfield`, `/drive`                     |
| O1    | Byte-metered budgets / fairness (§9.2 §9.3 §11.3) | P0-3 replay                                                                                       | bytes fetched + refetch cycles at equal runway            | MiB, count               | NEEDS-BASELINE (same pass)                                                                                                        | bytes ↓, refetch cycles → 0, no runway loss                                                     | same three routes                                                               |
| O1    | Cadence-derived runway tolerance (§11.2)          | `getQoeStats()` via frame-cost page probe                                                         | `stallCount`, `totalStallMs` per 60 s                     | count, ms                | NEEDS-BASELINE                                                                                                                    | stallCount ↓ vs baseline; 0 regression on single-source demos                                   | radar+fronts composite (`severe-weather`)                                       |
| O1    | Decode priority continuity (§10.2)                | `'decode'` channel (queue-wait, tile-decoder.ts:331–336) via P0-3                                 | p95 queue-wait; priority inversions observed              | ms, count                | NEEDS-BASELINE                                                                                                                    | p95 ↓; inversions → 0                                                                           | `/drive`, `storm-4d-greenfield`                                                 |
| O1    | No frame-cost regression                          | `frame-cost.mjs`                                                                                  | fps, p95 frame time                                       | fps, ms                  | storm-4d **86.8 fps / p95 24.8 ms**; `/drive` **28.5 fps / p95 58.3 ms** (`poopdeck:docs/roadmap/measurements-2026-08.md`:33,208) | no regression beyond run noise; `/drive` gains ride the separate draw-call item                 | `storm-4d-greenfield`, `ROUTE=/drive`, `ocean-drifters` (vsync-bounded control) |
| O2    | Stratified sampling + dispersion (M1)             | `stt-optimize inspect --sample n` vs exhaustive decode                                            | max per-column share error at fixed n                     | percentage points        | singleton-proxy error unquantified (§12.1 Incumbent)                                                                              | error ≤ half of systematic-stride error at equal n; dispersion published                        | `earthquakes-ci.stt` fixture + `earthquakes-v2`                                 |
| O2    | `measured` ordering default (M4)                  | `stt-optimize order-audit` (ordering_sim)                                                         | archives whose pick changes; simulated blended read cost  | count; bytes + reads×gap | **12/36 mis-picked** by the fixed heuristic (design M4)                                                                           | re-audit shows ≥ the 12/36 correction; cost never ranked by request count                       | 36-archive fleet audit set                                                      |
| O2    | Zero solver nondeterminism                        | P0-7 lane + double-run of `recommend`                                                             | byte diff of re-runs                                      | bytes                    | 0 today (lane green)                                                                                                              | **0** (byte-identical)                                                                          | fixture datasets                                                                |
| O3    | `--target-size B` hits budget in one build        | `stt-optimize recommend --target-size` (new, M3) + `stt-optimize diff`                            | \|final − B\|/B; human iterations                         | %; count                 | ~10 hand-tuned knobs, multiple iterations today (§5.2: no target-size exists in the CLI)                                          | within 5 % (proposed) on first build; iterations → 1; lossy levers appear only as shadow prices | three showcase datasets incl. one categorical-heavy                             |
| O3    | No-thinning demonstrably intact                   | M3 acceptance run                                                                                 | behavior at infeasible B                                  | —                        | —                                                                                                                                 | reports lexicographic floor, drops nothing (guard test)                                         | synthetic infeasible-budget fixture                                             |
| O4    | Semantic fingerprints green fleet-wide (M7)       | `stt-validate` (extended)                                                                         | fingerprint mismatches                                    | count                    | scrambled-coords class invisible today (§13.2 Gap: 106 archives passed)                                                           | **0** across the 68-archive fleet; seeded-corruption fixture must FAIL                          | full fleet                                                                      |
| O4    | K2 noise → 0 (M2 global pin)                      | `stt-validate --sample 300`                                                                       | benign width-drift warnings                               | count                    | benign warnings fleet-wide (K2; wildfires carries 163 _real_-drift errors, excluded — K9)                                         | benign warnings **0** post-R1; real-drift errors unaffected                                     | full fleet minus `wildfires`                                                    |
| O4    | R1 pack-byte win (M2 hoist)                       | `stt-optimize diff --before --after --fail-on-growth 0`                                           | compressed pack bytes                                     | %                        | dictionary share **43.9 %** (earthquakes-v2) / **33.6 %** (hurricanes) bounds the prize (design M2)                               | packed-v2-magnitude reduction on categorical-heavy; growth gate 0 % everywhere else             | earthquakes-v2, hurricanes, + 3 controls                                        |
| O4    | K10 re-captured post-republish                    | `cold-start.mjs` (P0-4 procedure)                                                                 | requests / bytes to first frame                           | count / KiB              | 5 / 335.4 KiB (earthquakes-v2); 5 / 5.69 MiB (flights); 4 / 250.9 KiB (goes-glm) — pre-R1 refresh from P0-4                       | ≤ P0-4 baseline requests; bytes explained by content change only                                | capture set                                                                     |
| O5    | Pitched-camera fetch ~10× down (§8.1)             | pitch×bearing matrix (chassis-viewport-bounds.test.ts:208) + P0-3 byte capture at pitched cameras | fetched tiles & bytes at pitch ≥ 60° at verified coverage | count, MiB               | **754 vs 47 tiles** at equal coverage (§8.1)                                                                                      | ≥ 10× tile/byte reduction; 0 coverage misses across the 24×18 matrix                            | storm/BIXI volumetric routes                                                    |
| O5    | One reviewed tier-declaration mechanism (M8)      | spec review (not a harness)                                                                       | —                                                         | —                        | four divergent planned mechanisms                                                                                                 | one declaration design reviewed once                                                            | —                                                                               |
| §11.6 | Scrub keep-vs-delete                              | `scrub-cost.mjs` (P0-5)                                                                           | TTFP; fresh-frame %; bytes-during-scrub; settle ms; pops  | ms; %; MiB; ms; count    | off-state run = baseline (none exists)                                                                                            | TTFP < 16.7 ms from resident tile; pops 1–2; bytes ≤ baseline (hard)                            | NYC taxi, `/drive`, BIXI                                                        |

**Architecture.** A contract, not code: later sections' Evaluation blocks must resolve against a row (the orchestrating plan should treat an unresolvable Evaluation block as a defect in that section). Lives in the measurements doc so the roadmap-citation CI gate keeps source references to it honest. BYTE-NEUTRAL.

**Tests.** None executable; the table's own integrity checks are P0-1 (every harness cell verified) and the NEEDS-BASELINE burn-down (all such cells filled before the first Phase 1 merge).

**Evaluation.** Complete when zero NEEDS-BASELINE cells remain for O1 rows and every O1–O5 criterion from design §7 appears exactly once.

**Risks & dependencies.** Depends on P0-1 through P0-5 (instruments and baselines) and P0-7 (the determinism rows). Thresholds marked "proposed" (the O3 5 %) are set by the owning phase, not silently here. Do-not-touch alignment: the O2 ordering row's metric is the blended byte+reads cost by construction; the O4 diff rows use the exact directory gate unchanged.

---

**The browser-verify protocol boundary.** Two verification regimes coexist and must not be confused. _Automated_ evaluation — everything in this section — reads counters, bytes, requests, and frame timings (`__sttProbe`, QoE stats, GL call counts, cold-start captures); the Playwright drivers here exist to produce those numbers and the showcase probe exists to prove routes load. _Aesthetic_ verification — whether a demo looks right — is the user's own in-browser pass, a mandatory manual gate in this project (the backlog's L2 queue; renderer-architecture §2.9's finding that tiers 1–4 cannot prove compiled-shader pixels). No Phase 0 or later item builds a screenshot-judgment loop, and no automated gate in the P0-8 matrix claims to sign off appearance: when a change could alter pixels, its acceptance row covers the counters and its Risks block adds a line to the browser-verify queue for the user.

## 2. The client cost-oracle pass (M5)

_Downstream since 2026-08-26: every item in this section changes files in the poopdeck.gl repository; its open half lives in that register. The design rationale below is kept as the record._

M5 discharges defect D2 on the client: the directory prices any (cell set × time window) in exact bytes, and six controllers currently guess instead of asking. This section builds the one shared pricing utility (CO-1) and then rewires the six consumers — prefetch horizon (§9.1), governor gating (§11.1), auto-speed (§11.4), temporal-tier pick (§7.5), placeholder fetch/cover (§8.3), and range coalescing (§6.2) — onto it. All items are BYTE-NEUTRAL, ship incrementally in Phase 1 behind the existing test + browser-verify gates, and every incumbent path is retained as the fallback/kill switch; the register constraints (two-threshold gating shape, aggregate-rate recovery formula, preview-never-gates) are preserved by construction.

#### CO-1 — The shared selection-cost oracle `§9.1 §11.1 §11.4 §7.5 · M5 · Phase 1`

**Change.** The primitive exists in fragments today: every resident `DirectoryEntry` carries `length` (compressed bytes), `timeStart/timeEnd`, `temporalBucketMs?`, and `coverTMin?` (poopdeck:packages/core/src/directory.ts:267-300); `STTArchive.getTileByteSize(id)` resolves one tile synchronously (poopdeck:packages/core/src/archive.ts:2452-2454, tier-qualified via `findTileEntry`, archive.ts:2419-2442); and `SpatioTemporalTileset.estimateCost(range)` sums missing bytes — but only for the current viewport at the primary zoom, only via the async-built coverage index, and with no per-bucket breakdown (poopdeck:packages/core/src/spatiotemporal-tileset.ts:3283-3314). So bytes-for-(cells×window) is summable today _only_ for one (viewport, primary-zoom, base-tier) triple. Build the general form:

- `STTArchive.estimateSelectionCost(bounds: BoundingBox, zoom: number, timeRange: TimeRange, opts?: { bucketMs?: number; variantId?: number }): SelectionCost` (new, archive.ts), where `SelectionCost = { bytes: number; tiles: number; unknownTiles: number }` (new type). Implemented on the existing private `entryListsInBounds` (archive.ts:3453) with the same variant/tier/`coverTMin` filters as `getTileIdsInBounds` (archive.ts:3546-3574) and `getTileIdsInBoundsForTemporalLod` (archive.ts:3603-3632), summing `entry.length` instead of materializing `TileId[]`. Synchronous over resident leaf pages; on a paged archive, a leaf whose root descriptor (`PageDescriptor` bbox/zoom/tMin/tMax, directory.ts:67-84) intersects the query but is not resident increments `unknownTiles` — the honesty channel mirroring the governor's `anyBytesBlind` abstention contract (poopdeck:packages/playback/src/playback-governor.ts:1160-1199).
- Tileset side: extend the coverage index built in `maybeRebuildCoverageIndex` (spatiotemporal-tileset.ts:3739-3767) — which already stores per-bucket `keys[]`/`bytes[]` — with two aligned arrays computed in the same loop: `bucketByteTotals: Float64Array` and prefix sums `cumulativeBytes: Float64Array`. New methods `SpatioTemporalTileset.getByteDensityProfile(range): { bucketStarts: number[]; totalBytes: number[]; missingBytes: number[] } | null` (new) and `bytesForHorizon(time, direction, horizonSimMs): { bytes: number; exact: boolean }` (new, O(log buckets) via `lowerBound` on the prefix sums). Missing-vs-total split reuses `isCoverageReady` (spatiotemporal-tileset.ts:3106-3110) so write-offs stay excluded exactly as `estimateCost` excludes them today (:3304-3310).

**Architecture.** Pure read-side derived data; sits in core beside the structures it reads. Data flow: directory entries → (archive walk | coverage index) → oracle answers → CO-2..CO-6 consumers. Invariants: deterministic (pure function of resident entries + arguments, no clocks); bounded memory (two Float64Arrays per coverage index, one slot per bucket); conservative honesty (never invents a byte count — unknown pages are _counted_, not guessed at); the existing `estimateCost` bytes-0-when-`getTileByteSize`-unwired contract is preserved. Caching/invalidation: the coverage-index profile shares the index's existing lifecycle — rebuilt on quantized-spatial-signature change (spatiotemporal-tileset.ts:3728-3731), complete for its viewport because the underlying query faults needed leaves via `ensurePagesForBounds` (archive.ts:3541); the archive-level walk is uncached (cost ≈ one `getTileIdsInBounds` scan minus allocation). BYTE-NEUTRAL; rollback trivial (additive API, no caller until CO-2).

**Tests.** Extend poopdeck:packages/core/test/archive.test.ts, buffered-runway.test.ts, paged-directory.test.ts (all exist); new poopdeck:packages/core/test/cost-oracle.test.ts.

- Unit: sum equals Σ`length` over a synthetic `encodeDirectory` archive; tier isolation (bucketMs-tagged entries never leak into a base-tier sum and vice versa); `coverTMin` filter parity with `getTileIdsInBounds` (a tile entirely after the window is excluded from both); summary-variant filter parity.
- Unit (paged): non-resident intersecting leaf ⇒ `unknownTiles > 0`, resident-only sum still exact; after the leaf faults in, `unknownTiles` drops to 0 and bytes grow by exactly the leaf's matching entries.
- Property: for random entry sets and random windows, `getByteDensityProfile` prefix sums equal brute-force per-window sums; `bytesForHorizon` monotone non-decreasing in horizon.
- Determinism: two builds from identical inputs produce byte-identical Float64Array contents.
- Integration: `tileset.estimateCost(range).bytes` ≡ profile `missingBytes` sum over the same range.
- Golden-fixture: none — no archive bytes move; byte pins unchanged.

**Evaluation.** No user-facing metric of its own; acceptance is correctness plus walk cost: profile query ≤ 0.1 ms and index-build overhead ≤ 5% over the current build loop on the storm-4d viewport slice (vitest bench alongside the unit suite; no external harness needed).

**Risks & dependencies.** Depends on nothing; everything downstream depends on it. Risk: consumers treating `unknownTiles > 0` or bytes-0-unwired answers as authoritative — the API forces the flag into every return type. Borders no do-not-touch entry; deliberately does _not_ touch the diff gate's directory metric (M1 register note).

#### CO-2 — Prefetch horizon as a one-shot feasibility solve `§9.1 · M5 · Phase 1`

**Change.** Today `PrefetchPolicy.plan()` picks a horizon from speed×lookahead under three caps, then multiplies by the AIMD pressure scale that discovers "the runway does not fit" only after tier-C/D evictions occur (poopdeck:packages/core/src/prefetch-policy.ts:488-610, ladder at :414-419 and :545-573; enqueue budget counts _tiles_, :618-623). Replace discovery with computation:

- Extend `PrefetchPlanRequest` (prefetch-policy.ts:186-205) with `byteBudget: number | null` (new; the tileset passes `PREFETCH_CACHE_FRACTION × maxCacheByteSize`) and `bytesForHorizon: ((horizonSimMs: number) => { bytes: number; exact: boolean }) | null` (new; the tileset binds CO-1's method).
- In `plan()`, after the existing caps yield `effectiveAhead`: when the callback is present and `exact`, solve `h* = max{ h ≤ effectiveAhead : bytesForHorizon(h).bytes ≤ byteBudget }` by monotone bisection over bucket boundaries on the prefix sums (O(log buckets)), and use `h*` in place of the pressure-scaled horizon. The gate floor `max(bucketMs, timeWindow, speed × PREFETCH_CAP_FLOOR_REAL_MS)` (:566-572) still applies _under_ the solve — if even the floor's byte sum exceeds budget, keep the floor (a smaller horizon deadlocks the governor's speed-scaled gates; this is §9.1's hard-floor constraint) and let §9.4 eviction tiering absorb it.
- AIMD demoted to fallback, not deleted: the ladder (`noteRunwayEviction`, pressure recovery) runs exactly as today when the callback is null or `exact: false` (bytes-blind archive, index not built), and remains the safety backstop when an exact solve still produces runway evictions (decoded size ≫ compressed size can make the compressed-byte solve optimistic).
- `prefetchFutureTiles` (spatiotemporal-tileset.ts:2121-2299) supplies the two new fields; `anchorTruncatedRunway` (prefetch-policy.ts:637-645) and the tile-count `enqueueBudget` are untouched here — byte-metering the _enqueue_ is M6's D3 repair, and this item must not pre-empt it.

**Architecture.** The policy keeps its no-I/O contract (prefetch-policy.ts:20-24): measurements arrive as injected callbacks. The at-scale-1 byte-identical-horizon invariant (:558-563) generalizes: with a null callback the emitted plan is byte-identical to today's. Deterministic given injected clock + profile. BYTE-NEUTRAL; rollback = don't pass the callback.

**Tests.** Extend poopdeck:packages/core/test/prefetch-policy.test.ts, prefetch-runway.test.ts, prefetch-runahead-cap.test.ts (all exist).

- Unit: `h*` lands on the largest bucket-aligned horizon under budget; floor wins over an infeasible budget; `exact: false` routes to the pressure ladder; null callback ⇒ plan byte-identical to current behavior (extend the existing scale-1 no-op guard).
- Property: `h*` monotone non-decreasing in `byteBudget`; never exceeds any existing cap.
- Simulation: scripted session with cache smaller than the demanded runway — solve path holds `runwayEvictions` at 0 while the ladder path (same trace, callback withheld) records > 0; the ladder-path assertion doubles as the baseline pin.
- Guard: budget-infeasible floor keeps the governor gates satisfiable (plan never returns a horizon below the floor — the deadlock rejection).
- Determinism: identical inputs ⇒ identical plan objects across re-runs.

**Evaluation.** Metric: `cacheStats.runwayEvictions` → 0 under looped playback — counter verified: declared at spatiotemporal-tileset.ts:951-955, incremented at :3970, exposed via `getCacheStats()` at :4433. Secondary: refetched bytes per session. Routes: storm-4d, `/drive`, and a fast-playback long-span dataset (the drifters-style thrash case documented at prefetch-policy.ts:118-124). Baseline: not numerically recorded in the docs — captured by the Phase 0 QoE/trace inventory run. Harness: `getQoeStats()` + cache-stats probes; the deterministic trace-replay layer is NEEDS-HARNESS (Phase 0 harness-inventory item).

**Risks & dependencies.** Depends on CO-1. Compressed-vs-decoded byte skew can under-price residency (backstop: ladder retained). Borders M6 (byte-metered enqueue/DRR — deliberately out of scope here) and the do-not-touch entries "prefetch exemption from per-selection supersession" and "B→C→D tiering," both untouched.

#### CO-3 — Governor fluid feasibility in one directory walk, gate constants from estimator variance `§11.1 · M5 · Phase 1`

**Change.** The gate and watermark pass on a _one-window_ ETA check: `predictsPlaythrough` compares max per-source ETA of the gate window against the buffered runway (poopdeck:packages/playback/src/playback-governor.ts:1933-1960, called from the gate at :1567-1573 and the watermark at :1911-1920) — it passes the "thin now, byte-cliff two windows ahead" case. And the estimator publishes only the point value `min(fast, slow)` (poopdeck:packages/core/src/throughput.ts:115-123). Three coupled edits:

- `ThroughputEstimator` gains dispersion: a third duration-weighted EWMA of squared rate deviations; `getEstimate()` adds an optional `stdDev` field, plus `getConservativeRate(z = 1): number | null` (new) = `max(0, min(fast,slow) − z·stdDev)` — the quantile-not-point-min the mechanism entry names. Existing fields and semantics untouched.
- `BufferSource` (playback-governor.ts:53-66) gains optional `getByteDensityProfile?(range: {start,end}): { bucketStarts: number[]; missingBytes: number[] } | null` (new; the tileset implements it from CO-1's prefix sums). When every required source exposes it, `predictsPlaythrough` runs the full fluid check: one pass over bucket boundaries `T_k` up to `min(dataset end, FLUID_CHECK_HORIZON_WALL_MS × |s|)` (new constant, default 60 000 wall-ms), asserting cumulative missing bytes through `T_k` ≤ conservative rate × (buffered-runway wall time + `T_k`'s wall offset). Any source lacking the profile ⇒ today's one-window path, unchanged.
- Constants re-fit, shape kept: the two-threshold hysteresis (`startGateWallMs` 2000 / `lowWatermarkWallMs` 600 / `resumeFactor` 2, defaults at :616-618) keeps its structure per the register; the _effective_ watermark scales with measured link jitter — `G_low_eff = lowWatermarkWallMs × clamp(1 + k·cv, 1, 3)` with `cv = stdDev/rate` — so a jittery link stalls earlier and honestly, while configured values stay the base and the calm-link behavior is bit-for-bit today's.

**Architecture.** Governor remains a pure consumer of `BufferSource`; feature-detects the optional method, so mixed source sets degrade cleanly. Preview-never-gates and the restore invariant are untouched (scrub paths do not route through these probes). BYTE-NEUTRAL; rollback = source stops exposing the profile.

**Tests.** Extend poopdeck:packages/playback/test/playback-governor.test.ts and poopdeck:packages/core/test/throughput.test.ts (both exist).

- Unit (estimator): stdDev against hand-computed samples; `getConservativeRate` ≤ point estimate; null before first sample.
- Unit (governor): the byte-cliff case — flat density for two windows, spike in window three: one-window predictor passes, full check refuses to unfreeze until the rate covers the cliff; complete-runway short-circuit unchanged; zero required sources ⇒ never stall (existing invariant re-asserted).
- Guard: gate-shape test — start gate, watermark, and resumeFactor hysteresis remain three distinct behaviors (the re-fit cannot collapse the two thresholds); calm-link (`cv ≈ 0`) trace produces today's transitions exactly.
- Simulation: scripted arrival/throughput traces through the injected timers; determinism — same trace ⇒ identical `getQoeStats()` counters.

**Evaluation.** Metric: `getQoeStats()` — `stallCount`, `totalStallMs`, `startupMs` (interface verified at playback-governor.ts:260-276). Acceptance: stallCount strictly below baseline on cliff traces; startupMs regression < 10% on calm traces. Routes: the multi-source composite (radar + fronts — the §11.2 named route) and storm-4d. NEEDS-HARNESS: the throughput-trace replay harness — Phase 0 inventory item (the design doc's `getQoeStats()` line).

**Risks & dependencies.** Depends on CO-1 (profile) ; CO-4 consumes its quantile. Over-conservative quantiles inflate startup on noisy links — `z` and the cv clamp are the bounded knobs, and both get recorded fits per P1. Borders three register entries — two-threshold shape (kept), preview-never-gates (kept), and §11.2's cadence-tolerance fold (not touched here; its τ re-derivation is M6).

#### CO-4 — Auto-speed evaluates the full candidate ladder `§11.4 · M5 · Phase 1`

**Change.** `getAutoSpeedSuggestion` (poopdeck:packages/playback/src/playback-governor.ts:1130-1225) measures byte demand over a horizon scaled by the _current_ speed (`horizonSimMs = AUTO_SPEED_HORIZON_WALL_MS × absSpeed`, :1137) — a higher candidate speed sweeps a longer window that may cross a density spike the myopic estimate misses. Rewire to per-candidate evaluation:

- For each ladder multiplier `m` in `SPEED_STEPS` (poopdeck:packages/playback/src/auto-speed.ts:56-58), descending: candidate speed `s_c = m × baseSpeed`, candidate range `[p, p + d·H·s_c]`, demand `Σ_required estimateCost(range_c).bytes`; feasible iff `Σbytes_c / H ≤ η × Ĉ`. Return the sustainable value at the largest feasible candidate (consumers still snap/clamp — `decideAutoSpeedMultiplier`, auto-speed.ts:69-98, signature unchanged). The governor needs the base rate to enumerate candidates in speed units; `SttPlayer` already owns the multiplier↔speed conversion, so pass base speed in via the existing options rather than moving policy into the governor.
- Kept verbatim (register): the aggregate-rate recovery `max_i(bytes_i/eta_i)` on the no-`getThroughput` path (:1159-1219 — the alternative is the recorded racing-ahead bug); the honesty contracts — `Infinity` when nothing pending, `null` on `anyBytesBlind`/`anyEtaBlind` (:1160-1199), now enforced per evaluated candidate; the asymmetric downshift-now/upshift-damped hysteresis in `decideAutoSpeedMultiplier`.
- `η = AUTO_SPEED_SAFETY = 0.7` (:323) and the 0.25 upshift deadband become functions of CO-3's dispersion (`η` shrinks as cv grows), defaults unchanged when the estimator lacks variance.

**Architecture.** Thirteen `estimateCost` walks per evaluation epoch, each O(buckets-in-horizon) on CO-1's prefix sums — sub-millisecond; evaluation stays on the existing cadence (no new timers). Determinism: pure function of (sources, estimator, clock). BYTE-NEUTRAL; rollback: a `ladderEvaluation: false` option pin restores the single-point computation.

**Tests.** Extend poopdeck:packages/playback/test/playback-governor.test.ts and auto-speed.test.ts (both exist).

- Unit: density spike just beyond the current-speed horizon — myopic math would upshift into a stall, ladder evaluation holds/downshifts (the §11.4 gap case); storm-cell-entry pre-emptive downshift (spike inside the top candidate's window only).
- Unit (contracts): `Infinity` when `sumTiles = 0` at every candidate; `null` when the chosen candidate's range is bytes-blind; N equal-heavy required sources ⇒ suggestion ≈ 1/N of the single-source value (the multi-heavy contract, re-asserted).
- Property: feasibility monotone down the ladder (if `m` feasible, every `m' < m` feasible) for monotone byte profiles.
- Determinism: fixed fake estimator + profile ⇒ identical suggestion sequence.

**Evaluation.** Metric (problems doc §11.4): mean multiplier achieved × (1 − stall fraction) on throughput traces, units dimensionless. Baseline: current single-point policy on the same traces (not yet recorded — Phase 0 capture). Acceptance: metric non-decreasing on every trace, strictly up on spike traces. Routes: multi-source composite and storm-4d (cell-entry density spikes). NEEDS-HARNESS: same trace-replay harness as CO-3 (Phase 0).

**Risks & dependencies.** Depends on CO-1 (cheap walks) and CO-3 (variance, optional). Flapping risk if adjacent candidates disagree epoch-to-epoch — absorbed by the untouched asymmetric hysteresis. Borders register entries: aggregate-rate recovery formula (kept exactly) and the upshift-forbidden-on-'waiting' rule (kept, auto-speed.ts:33 semantics).

#### CO-5 — Temporal-tier pick as a 1-D argmin over the addressable set `§7.5 · M5 · Phase 1`

**Change.** The reader picks "coarsest applicable by zoom": `STTArchive.pickTemporalLodForZoom` (poopdeck:packages/core/src/archive.ts:3758-3771) and its tileset twin `scrubTemporalLodBucketMs` (poopdeck:packages/core/src/spatiotemporal-tileset.ts:1496-1514) never compare costs — a window narrower than the coarse bucket over-fetches; a wide window at a zoom just above a cutoff under-aggregates. Losslessness (§7.5 constraint (i)) makes every addressable tier _correct_, so this is pure cost:

- `STTArchive.pickTemporalLodByCost(bounds: BoundingBox, zoom: number, timeRange: TimeRange): Promise<{ bucketMs: number; bytes: number; tiles: number } | undefined>` (new, beside the zoom-threshold pick): enumerate the addressable set `{base} ∪ {levels with zoom ≤ maxZoomLevel}` from `metadata.temporalLod`; price each tier with CO-1's `estimateSelectionCost(bounds, zoom, timeRange, { bucketMs })`; objective `cost(b) = bytes(b) + tiles(b) × requestOverheadBytes` where `requestOverheadBytes` is the bytes-equivalent request price `L̂·θ̂` shared with CO-7 (static `DEFAULT_RANGE_COALESCE_GAP` = 2 MiB, archive.ts:149, until CO-7 lands); argmin, ties to the coarser tier for determinism. Any tier with `unknownTiles > 0` ⇒ fall back to `pickTemporalLodForZoom` — never guess.
- Consumers: (a) `scrubTemporalLodBucketMs` gains the cost comparison behind the same `scrubLod.temporal` kill switch (the wiring the layer already threads at poopdeck:packages/layers/src/layers/spatiotemporal-layer.ts:1877-1922); (b) a new tileset option `temporalTierPolicy: 'zoom-threshold' | 'cost-argmin'` (new, default `'zoom-threshold'`) for the general selection path — default preserved because temporal-LOD selection is currently scrub-only ("Inert until the `scrubLod.temporal` axis is switched on," spatiotemporal-layer.ts:1876); flipping the default is a separate, measured decision.

**Architecture.** Tier-qualified addressing already holds end-to-end (`findTileEntry`, archive.ts:2419-2442; the `bucketMs`-stamped `TileId`, archive.ts:3623-3630), so a tier switch can never collide caches. The coverage index and every readiness API stay pinned to the fine base tier (spatiotemporal-tileset.ts:3716-3719) — the preview-never-gates contract is structurally untouched. Deterministic (resident entries + args); BYTE-NEUTRAL; rollback = option default.

**Tests.** Extend poopdeck:packages/core/test/temporal-lod.test.ts and scrub-lod.test.ts (both exist).

- Unit: narrow window ≪ coarse bucket at low zoom ⇒ argmin picks base where zoom-threshold picked coarse (over-fetch case); wide window just above a cutoff ⇒ argmin picks coarse (under-aggregation case); tie ⇒ coarser, deterministically; non-resident pages ⇒ result identical to `pickTemporalLodForZoom` (fallback guard).
- Property: on random synthetic pyramids and windows, argmin cost ≤ zoom-threshold pick cost, always.
- Integration: scrub drag over a temporal-LOD fixture selects the cheaper tier and `getBufferedRunway`/`getBufferedRanges` never report the coarse tier's tiles (readiness isolation).
- Determinism: identical archive + query ⇒ identical pick across re-runs. Golden-fixture: none; byte pins unchanged.

**Evaluation.** Metric (§7.5 gap): bytes fetched during wide-window scrubbing and during narrow-window playback at low zoom, vs the zoom-threshold baseline; units MB per scrub gesture / per playback minute. Acceptance: ≥ 20% byte reduction on a wide-window scrub over a temporal-LOD carrier (storm-4d-isolines), zero regression on archives without tiers. Route: storm family (the temporal-LOD carriers). Harness: transfer/cache byte counters exist; per-gesture attribution is NEEDS-HARNESS (Phase 0 QoE inventory).

**Risks & dependencies.** Depends on CO-1; shares the request-price constant with CO-7 (one source of truth, not two guesses). Build-side pyramid design (`--temporal-lod` cutoffs, §2.4) is explicitly out of scope — that is M3/M4 territory. Borders §11.6's counted-out temporal scrub tier: this item prices _existing_ tiers only and must not be argued as the G5 feature-reducing tier (M8 owns that trigger).

#### CO-6 — Placeholder fetch as expected value; cover DP de-capped `§8.3 · M5 · Phase 1`

**Change.** Two coupled edits in the tileset.
_Fetch side:_ `isOversizedParent` (poopdeck:packages/core/src/spatiotemporal-tileset.ts:1743-1753) applies a flat cutoff (`DEFAULT_MAX_PARENT_TILE_BYTES` = 2 MiB, :85) with the throughput estimator already wired (`options.getThroughput`, read by `estimateTimeToReadyMs` at :3324-3330). Replace with an expected-value rule `placeholderWorthFetching(tileId, primaryZoom): boolean` (new private): fetch parent `u` iff `bytes(u)/θ̂ < λ · A(u) · Ê[coverMs]`, where `bytes(u)` comes from `getTileByteSize`, `A(u)` = visible primary child cells under `u` (the same viewport-clamped arithmetic pass 2 of `getVisibleTiles` already runs), and `Ê[coverMs]` = ETA of `u`'s missing children (child byte sum / θ̂); `λ` is the one new tuned constant (blank-cell-ms value), recorded with its fit per P1. Estimator cold or sizes unknown ⇒ the flat 2 MiB rule, bit-for-bit. The primary zoom is never skipped (invariant kept, :1748); the `PARENT_FALLBACK_LEVELS = 4` fetch ladder (:99, used at :1721-1728) is unchanged.
_Cover side:_ `getVisibleTiles` pass 3's stand-in walks are capped at depth 2 both directions (`CHILD_LOOKAHEAD_LEVELS` :117, `ANCESTOR_LOOKBACK_LEVELS` :226), discarding usable resident covers after multi-notch zooms. Replace pass 3 with one bottom-up DP over the resident loaded set across `[z*−4, z*+2]` — O(|L|), |L| bounded by the 2000-tile cache — emitting the maximum-detail antichain. Preserved contracts: single cover per visible cell (the antichain constraint); descendants resolved before ancestors so iteration order cannot yield two covers; an ancestor covers only when its whole visible block is blank; fail-open on degenerate viewport; temporal-LOD tiles never stand in for base cells (the `@bucketMs` tile-key suffix, per §8.3's constraint list). Passes 1–2 (primaries + parent retention with the viewport clamp and slack ring) are untouched.

**Architecture.** Register note, addressed head-on: the register pins "the fallback cover greedy _given its caps_" — within-family optimality. The M5 mechanism entry is the sanctioned family move ("the cover DP also drops its depth caps… O(|L|) and raises covered area at zero fetch cost"); what must survive is the greedy's _contracts_ (antichain, fail-open, base-tier-only stand-ins), and the DP is specified against exactly those. Per-frame cost bounded and allocation-free (reuse pooled maps). Deterministic per resident set. BYTE-NEUTRAL; rollback: constants `CHILD_LOOKAHEAD_LEVELS`/`ANCESTOR_LOOKBACK_LEVELS` retained as a `coverSearch: 'capped'` escape hatch for one release.

**Tests.** Extend poopdeck:packages/core/test/zoom-out-child-standin.test.ts, ancestor-standin-overlap.test.ts, parent-fallback-clamp.test.ts (all exist).

- Unit (cover): a resident tile 3 levels finer / 3 coarser is now reused where the caps discarded it (the multi-notch zoom case); ancestor emitted only over a fully blank visible block; fail-open on non-finite viewport.
- Unit (fetch): big parent + fast link + slow-arriving children ⇒ fetched; big parent + slow link ⇒ skipped; cold estimator ⇒ decisions identical to the flat 2 MiB rule on a scripted sequence (fallback guard).
- Property: for random resident sets, DP output is an antichain (no visible cell covered twice) and total covered detail ≥ the capped greedy's on the same set.
- Guard: the z10/z11 double-draw regression — a parent with < 16 children in view is never delivered alongside its own loaded children (re-assert the fixed single-cover contract against the DP).
- Determinism: same resident set ⇒ identical delivered list, stable order.

**Evaluation.** Metrics: covered-area-at-zero-fetch (fraction of visible cells with some resident cover after a multi-notch zoom), blank-frame time, and placeholder bytes fetched per session. Harness: `poopdeck:tools/bench/src/frame-cost.mjs` (exists; `ROUTE=` support) on `/drive` and storm-4d — `/drive` is the route where cover pathologies previously manifested as draw-cost. Acceptance: covered area strictly up at equal placeholder bytes; no fps regression on either route. Blank-frame/coverage instrumentation is NEEDS-HARNESS (Phase 0).

**Risks & dependencies.** Depends on the wired throughput estimator; CO-1 only marginally (child byte sums via `getTileByteSize`). EV mispricing during estimator warm-up is bounded by the flat-rule fallback. Borders do-not-touch: the fetch-set ladder and §9's dispatch/cancellation machinery (untouched); `lodMode: 'additive'` bypasses all of this by design (spatiotemporal-tileset.ts:1747, :1715-1719) and must keep doing so — covered by the existing additive-lod.test.ts.

#### CO-7 — Adaptive coalesce gap G ← L̂·θ̂ `§6.2 · M5 · Phase 1 (gated on M7 for measured-ordered archives)`

**Change.** The fuse threshold is a fixed `DEFAULT_RANGE_COALESCE_GAP = 2 MiB` (poopdeck:packages/core/src/archive.ts:149) applied at both fuse sites — directory leaf pages (`fetchAndMergePages`, archive.ts:2164) and pack range groups (:3191) — while the optimal `G* = L·θ` varies an order of magnitude across sessions and the dual-EWMA estimator that could calibrate it is already fed (samples at archive.ts:1729, :1776-1801; exposed via `getThroughputEstimate`, :1748). The threshold _rule_ is exactly optimal for its cost model (register: extend, never replace) — only the constant becomes fitted:

- New `LatencyEstimator` (new, poopdeck:packages/core/src/throughput.ts): an EWMA of per-request time-to-first-byte, sampled where `beginTransferSample`/`endTransferSample` (archive.ts:1776, :1792) already stamp response timing; exposes `getLatencyMs(): number | null`.
- `STTArchive.effectiveCoalesceGap(): number` (new) = `clamp(L̂ × θ̂, 256 KiB, 4 MiB)`; both fuse sites read it in place of the raw field. Either estimator cold ⇒ `DEFAULT_RANGE_COALESCE_GAP`. An explicit `ArchiveOptions.coalesceGapBytes` (accepted at archive.ts:1168-1171) pins the gap and disables adaptation entirely — the back-compat kill switch.
- Co-versioning guard (the D6 hazard): a `measured` blob ordering was optimized against exactly G = 2 MiB, and a reader-side gap change silently invalidates that layout. Until M7 lands the build-assumed gap in the manifest next to `blobOrdering` (cross-referenced, _not_ specified here), adaptation is disabled for archives whose manifest declares `blobOrdering: 'measured'` and enabled for `'spatial'`/`'temporal'` orderings — which is most of the published fleet, so the safe population is also the large one. Once M7's field exists, `measured` archives adapt within a declared band around the build gap and order-audit flags drift.

**Architecture.** BYTE-NEUTRAL — archive bytes never move; only the session's request pattern does. Determinism nuance: the request _plan_ becomes a function of injected estimator state, so tests pin estimators; archive decode results are unaffected (every fused member still CRC-verifies through the existing slice-and-verify path). `ordering_sim` continues to price layouts at the build-assumed constant, which stays correct for build-time decisions — the divergence is documented at the constant's definition and closed by M7/M4, not here. Rollback: option pin, or cold-estimator default.

**Tests.** Extend poopdeck:packages/core/test/tile-batch-coalescing.test.ts, throughput.test.ts, archive-integrity.test.ts (all exist).

- Unit: warm-edge profile (L̂ ≈ 20 ms, θ̂ ≈ 12.5 KB/ms) ⇒ G ≈ 250 KB; cold-origin (≈ 200 ms) ⇒ ≈ 2.5 MB, clamped to 4 MiB ceiling; explicit `coalesceGapBytes` pins; cold estimators ⇒ request boundaries byte-identical to today on a scripted fetch sequence (legacy guard).
- Unit (gate): a manifest with `blobOrdering: 'measured'` ⇒ adaptation off, 2 MiB behavior (the co-versioning guard test — cheap, and the boundary the design doc names).
- Property: fuse decisions monotone in G; per-object boundary never bridged; every member of a fused buffer CRC-verifies regardless of G.
- Determinism: fixed injected samples ⇒ identical range-request plan across re-runs.

**Evaluation.** Metrics (§6.2 gap sentence): request count and wall time-to-frame on `poopdeck:tools/bench/src/frame-cost.mjs`, plus cold-start request/byte counts on `poopdeck:tools/bench/src/cold-start-bench.mjs` (both exist) — the leaf-page fuse site is a cold-start lever. Routes: storm-4d, `/drive`, multi-source composite, warm and cold. Acceptance: warm-session request count down with over-fetched gap bytes ≤ +1%; no cold-start wall-time regression. Baseline: the Phase 0 K10 cold-start re-capture is the named baseline for exactly this decision family.

**Risks & dependencies.** Depends on M7 for the `measured`-archive population (designed around via the ordering gate, so the item still ships in Phase 1); shares L̂·θ̂ with CO-5's request price (single estimator, one exchange rate). Risks: TTFB conflation under high concurrency (queueing inflates L̂ — the §6.2 `L/k` concurrency refinement is recorded as the follow-on, not attempted here); estimator feedback loops (larger G → fewer, longer transfers → different samples) bounded by the clamp band. Borders do-not-touch: the fuse rule itself and the no-max-fetch-size stance (archive.ts:146) — both untouched; the reader/simulator constant-mirroring constraint is honored by leaving the simulator on the build-assumed gap.

## 3. Byte-honest budgets, decode continuity & cache repair (M6)

_Downstream since 2026-08-26: every item in this section changes files in the poopdeck.gl repository (BH-10's builder-side `styleHints` emission excepted); its open half lives in that register. The design rationale below is kept as the record._

This section implements mechanism M6 — the D3 repair: everywhere the client meters slots, tiles, or entries while the constrained resource is bytes, the unit of account becomes bytes; everywhere upstream priority or playhead structure is erased downstream (decode FIFO, wrap-blind eviction, recency-only caches), the information is carried through. All items are client-side and BYTE-NEUTRAL except BH-10's builder-side `styleHints` emission, which is manifest-additive metadata (packs byte-identical) and batches its emission flip with rebuild window R1 per P6. Register boundaries respected throughout: DRR's crediting scheme, B→C→D tiering, copy-at-enqueue/worker-side-cancel rejections, and prefetch supersession-exemption + batch-abort semantics are all extended, never replaced.

#### BH-1 — DRR byte metering in the shared scheduler `§9.3 · M6 · Phase 1`

**Change.** Generalize `SharedRequestScheduler`'s deficit unit from slot-credits to byte-credits. `ScheduleOptions` (poopdeck:packages/core/src/request-scheduler.ts:113) gains `costBytes?: number` (new); `Entry` (:191) carries it. `creditRound` (:584-593) credits `weight × byteQuantum` instead of `weight`, keeping the exact crediting scheme — at most once per round via `roundCredited`, deficit clamped to one quantum, new round only when no credited source can dispatch, top-up so no slot idles. `pickEligible` (:599-610) admits a source when `deficit ≥ min(candidate.costBytes, quantum)` — the `min` is the progress guarantee for groups larger than a quantum, mirroring the "slice takes at least one tile" rule (spatiotemporal-tileset.ts:2422-2424). `selectNext` (:479) spends `costBytes` instead of 1. `SharedRequestSchedulerOptions` gains `byteQuantum?: number` (new; default `512 * 1024`), plumbed through `configureSharedScheduler` (shared-scheduler.ts:77). Entries without `costBytes` bill at one quantum, so directory-page fetches and custom callers behave exactly as today. The producer side: `runGroupFetches` (archive.ts:2891) gains a `groupCostBytes: (group: G) => number | null` callback; the range-group's size is known exactly at enqueue — `group.end - group.start + 1` from the coalesced `Group {packId, start, end, members}` (archive.ts:3165-3199) — answering the open question in the work-item list affirmatively.

**Architecture.** Pure unit change inside the mechanism layer; policy stays in the §11 controller (BH-3), which continues to write weights via `setSourceWeight` (request-scheduler.ts:317) unchanged. Slot concurrency (`maxRequests` = 24) still bounds parallelism; DRR now bounds _byte share_ per round, so a source whose groups are 100× heavier no longer gets 100× the link at equal weight. Invariants preserved: work conservation, single-source draws-all (one source is always eligible after top-up), no starvation, running requests never re-ranked, priority-within-eligibility (`compareCandidates` untouched). Deterministic: same enqueue sequence → same dispatch order. BYTE-NEUTRAL (no archive bytes involved). Rollback: `byteQuantum: null` (or omitting `costBytes`) degenerates to the current slot semantics — ship behind that default-on config with the existing `multiSourceFairness` kill switch unaffected.

**Tests.** Extend existing `poopdeck:packages/core/test/request-scheduler.test.ts`, `scheduler-dynamic-weight.test.ts`, `shared-scheduler-archive.test.ts` (all verified present). Unit: (1) two sources, equal weights, 10× byte-skewed groups → granted bytes converge to ~1:1 (was ~1:10 in slots→bytes terms); (2) a group larger than a quantum still dispatches when its source's deficit is full (progress guarantee); (3) `costBytes` omitted → dispatch order byte-identical to today's slot DRR (regression pin, table-driven against the current implementation's recorded order); (4) deficit clamp: an idle-then-active source banks ≤ one byte quantum. Property: for random weight/size streams, over any backlogged interval the byte-share error is bounded by max quantum (the DRR bound, checked empirically). Guard test (rejected-design boundary): assert the per-dispatch-crediting starvation bug stays dead — heavy+light source stream, light source always eventually dispatches (extends the existing regression in request-scheduler.test.ts). Determinism: two identical scripted runs → identical dispatch sequences.

**Evaluation.** Metric: per-source granted bytes/s vs weight share (new counter on `SchedulerStats` — add `dispatchedBytesBySource`), and laggard time-to-gate on a multi-source composite. Baseline: none recorded per-source; capture before/after via `getQoeStats()` (playback-governor.ts:1067) `stallCount`/`totalStallMs` on the radar+fronts composite route and the `/drive` cockpit (multi-source AV). Acceptance: byte-share error under contention ≤ 2× quantum; no stallCount regression on single-source demos. Byte-share telemetry itself is NEEDS-HARNESS (a scripted multi-source playback bench) — covered by the Phase 0 harness-inventory item in the design doc §6.

**Risks & dependencies.** Borders the pinned "DRR's crediting scheme" register entry — the scheme (per-round credit, clamp, top-up) is preserved verbatim; only the currency changes; any review that finds the crediting order altered is a blocker. Risk: quantum too small → many rounds per dispatch (CPU); mitigate with the 512 KiB default ≈ median coalesced group. Depends on nothing; BH-3 consumes it.

#### BH-2 — Enqueue and prefetch budgets denominated in bytes `§9.1, §9.2 · M6 · Phase 1`

**Change.** `PrefetchPolicy.enqueueBudget(maxCacheSize)` (poopdeck:packages/core/src/prefetch-policy.ts:618-623) gains a byte sibling `enqueueBudgetBytes(maxCacheByteSize: number): number` (new) = `max(PREFETCH_MIN_BUDGET_BYTES, maxCacheByteSize × PREFETCH_CACHE_FRACTION)` with `PREFETCH_MIN_BUDGET_BYTES = 4 MiB` (new const). The tileset's prefetch-enqueue loop (spatiotemporal-tileset.ts:2199-2277) accumulates per-tile bytes via the already-wired `this.options.getTileByteSize` (verified in use at :2413, falling back to `PREFETCH_UNKNOWN_TILE_BYTES` = 64 KiB, :41) and stops when _either_ the byte budget or the existing tile-count budget is exhausted — the count budget survives as a guard for byte-blind directories. `anchorTruncatedRunway` (prefetch-policy.ts:637) is called exactly as today with the honest frontier. The rationale is D3 verbatim: the budget exists to keep the runway resident in the LRU, and the LRU's binding constraint is `maxCacheByteSize` (2 GiB, tileset:817-818), not tile count — one satellite tile ≈ 17 MB and one sparse leaf ≈ 5 KB currently count equally.

**Architecture.** Sits entirely inside the policy/tileset pair; no scheduler or archive change. Preserves: nearest-first ordering (:2195-2197), the oversized-parent skip (:2227), the dead-header revival + quarantine logic (:2246-2272), the pressure ladder's byte-identical-at-scale-1 contract (prefetch-policy.ts:558-573 untouched). The slice sizing path is _already_ byte-denominated (`sliceBytes`, :656-666) — no change there, and the one-slice-in-flight rule is out of scope (its double-buffering is a §4 batch item). BYTE-NEUTRAL. Rollback: `enqueueBudgetBytes` returning `Infinity` restores count-only behavior; one-line revert.

**Tests.** Extend `poopdeck:packages/core/test/prefetch-policy.test.ts`, `prefetch-runway.test.ts`, `prefetch-slicing.test.ts` (verified present). Unit: (1) byte budget truncates before count budget on a heavy-tile stream and `anchorTruncatedRunway` lands on the last _enqueued_ bucket; (2) count budget still binds when `getTileByteSize` is absent; (3) floor: budget never below `PREFETCH_MIN_BUDGET_BYTES` for tiny cache configs. Simulation (extends the fetch-evict-refetch scenario in `prefetch-runway.test.ts`): a synthetic archive with 100× byte-skewed tiles under a small byte cache — assert the enqueued runway's byte sum ≤ fraction × cache bytes, and that `cacheStats.runwayEvictions` stays 0 where the count-only budget provoked evictions. Guard test: prefetch batch abort still requires all-members-superseded (extends `prefetch-supersede.test.ts`, verified present) — this item must not touch supersession semantics.

**Evaluation.** Metric: `cacheStats.runwayEvictions` (tileset:955, :3970) per playback loop, and refetched bytes per session. Baseline: the design doc's O1 acceptance is `runwayEvictions → 0` under looped playback; current values not recorded per-route — capture in Phase 0. Routes: drifters (fast multi-year playback, the recorded thrash case at prefetch-policy.ts:119), storm-4d, satellites (extreme byte skew). Acceptance: runwayEvictions → 0 on drifters at default cache; no fps regression on frame-cost.mjs (`poopdeck:tools/bench/src/frame-cost.mjs`, exists).

**Risks & dependencies.** Borders the pinned "prefetch exemption from per-selection supersession and all-members-superseded batch abort" — untouched, guard-tested. Risk: `getTileByteSize` returning 0/undefined for summary tiers would under-count; the 64 KiB fallback bounds it. Independent of BH-1; both should land in the same phase so budgets and metering agree on units.

#### BH-3 — Fairness weights gain β_i and progressive filling `§11.3 · M6 · Phase 1`

**Change.** In `applyMultiSourceFairness` (poopdeck:packages/playback/src/playback-governor.ts:1727-1805), replace the `1/x` shed formula (:1789-1795) with exact progressive filling informed by per-source byte density. New pure exported function `computeProgressiveFillWeights(probes: {id: string; runwaySimMs: number; betaBytesPerSimMs: number; baseWeight: number}[], slackSimMs: number): Map<string, number>` (new, in playback-governor.ts or a sibling `fairness.ts`): water-fill capacity toward equalized _time-to-gate_ — each incomplete required source's byte need is `N_i = β_i × max(0, r_target − r_i)`; weights are set proportional to `N_i`, normalized so the laggard's weight lands on `4 × base` and surplus sources shed toward `0.25 × base`, iterating the standard progressive-filling loop over sources sorted by runway until clamps bind. β_i comes from a new private probe helper `frontierByteDensity(p: SourceProbe): number | null` = `p.source.estimateCost({start: frontier, end: frontier + Δ}).bytes / Δ` using the existing `BufferSource.estimateCost` (playback-governor.ts:62-65; tileset implementation at spatiotemporal-tileset.ts:3283) with `Δ = max(bucketMs, slackSimMs)`; a `null` β (bytes-blind source) falls back to β = 1 for that source, degrading gracefully toward the current runway-only shed. Everything around the formula is kept verbatim: laggard identification (:1754-1761), enter/exit hysteresis bands (:1774-1786), the cap write `sendRunAheadCap` (:1808-1828), the 20% deadband `sendBandwidthWeight` (:1835-1845), one-shot restore `deactivateFairness` (:1855-1869), and the `[0.25, 4]` clamp.

**Architecture.** Upper-level policy only; the mechanism realizing the weights is BH-1's byte-metered DRR (weights now denominate byte share, which is exactly what β-aware shedding wants — a laggard behind because its tiles are 10× heavier now receives 10× the bytes, not 10× the slots it can't fill). Invariants: control writes stay deadbanded and cadence-driven (never a new timer, :1722-1749); dead-weight cap semantics unchanged; single-source and paused paths unchanged; deterministic given probe inputs. BYTE-NEUTRAL. Rollback: `computeProgressiveFillWeights` is swappable for the old shed behind the existing `multiSourceFairness` kill switch; keep the old formula as a named fallback for one release.

**Tests.** Extend `poopdeck:packages/playback/test/playback-governor.test.ts` (verified present). Unit on the pure function: (1) two sources, equal β → reproduces the qualitative order of the current shed (laggard ≥ leaders); (2) laggard with 10× β gets ~10× the byte-share weight of the equal-runway alternative; (3) clamps bind correctly at 0.25/4 and the laggard is never shed below base; (4) all-complete / single-source → deactivation path unchanged. Integration (governor + mock BufferSources with scripted `estimateCost`): weight writes respect the 20% deadband and to/from-null cap transitions; hysteresis prevents identity flapping on a near-tie (extends the existing fairness tests). Guard tests: aggregate-rate recovery formula (§11.4, pinned) untouched — existing auto-speed tests must pass unmodified (`poopdeck:packages/playback/test/auto-speed.test.ts`, verified present); two-threshold gate shape untouched. Determinism: identical probe sequences → identical weight sequences.

**Evaluation.** Metric: laggard time-to-gate (wall-ms from gate entry to gate satisfaction, derivable from `getQoeStats().startupMs` / stall durations) and `runwayEvictions` on multi-heavy composites — the §11.3 gap names both. Baseline: unrecorded; capture in Phase 0 on the radar+fronts composite and a storm-4d + fronts two-source scene. Acceptance: laggard time-to-gate reduced with no increase in total stallCount; leaders' caps unchanged in write frequency (deadband telemetry).

**Risks & dependencies.** Depends on BH-1 (weights must buy bytes for β to mean anything). Borders pinned entries: the two-threshold gating shape, the aggregate-rate recovery formula, and cap-bounds-run-ahead-only semantics — all untouched. Risk: `estimateCost` per probe per source adds directory-walk cost on the probe cadence; bound Δ to one bucket and memoize per (frontier bucket, viewport) — `stt-optimize inspect --sample 0`-style directory-only math is cheap (memory note confirms directory-only is fast).

#### BH-4 — Runway tolerance τ derived from per-source cadence `§11.2 · M6 · Phase 1`

**Change.** `BufferSource` (playback-governor.ts:53-108) gains optional `getTemporalBucketMs?(): number` (new); the core tileset implements it returning `this.options.temporalBucketMs` (field verified in use at spatiotemporal-tileset.ts:3894). `foldRequiredRunways` (playback-governor.ts:1328-1380) changes its lift band from the global `tolSimMs` to a per-source band: for source _i_ vs leader _L_, `τ_i = max(Δ_i, Δ_L) + π|s|` sim-ms, where `Δ` are the declared bucket sizes and `π|s|` is the probe-staleness term (`TICK_PROBE_INTERVAL_MS` × |speed|, the current default's only legitimate residue). The runways array entries (the `BufferedRunway[]` fold input) are extended to carry `bucketMs` collected at probe time. Precedence: an explicitly authored `opts.runwayToleranceMs` (constructor, :624-627) still wins globally — the cadence derivation replaces only the _default_; `0` still reproduces the exact raw min. The fold's structure is preserved: lift measured between sources against the leader, never against the gate or watermark (the §11.2 structural constraint), complete sources excluded, safety bound `r̂ ≤ min r_i + τ` holds per-source.

**Architecture.** Pure estimator change on the fold used by the gate (:1553-1559), watermark (:1901-1907), and frontier probe (:1657-1665) — all three consume the same fold, so consistency is automatic. Sources without `getTemporalBucketMs` fall back to the current 200 wall-ms behavior (no regression for custom sources). BYTE-NEUTRAL, no format change. Rollback: authored `runwayToleranceMs` restores today's behavior exactly.

**Tests.** Extend `poopdeck:packages/playback/test/playback-governor.test.ts` (the tolerance Phase 1 tests live there). Unit: (1) coarse-bucket source (Δ = 1 h) + fine source (Δ = 1 min) at low speed — jitter of up to Δ_coarse no longer stalls (the false-stall case the 200 ms constant under-absorbs); (2) fine-bucket pair at high speed — a genuine 150-wall-ms laggard is _not_ masked (the over-absorb case); (3) safety property: for randomized runway/jitter vectors, `foldedSimMs ≤ min(incomplete r_i) + max_i τ_i` (one-sided robustness); (4) explicit `runwayToleranceMs: 0` → exact raw min (existing test, must stay green); (5) sources lacking the new method → byte-identical fold vs today. Property: fold is monotone in every `r_i`. Determinism: pure function, same inputs → same output (trivial, but pin it).

**Evaluation.** Metric: `getQoeStats().stallCount` on multi-cadence composites — the §11.2 gap names "radar + fronts" explicitly (weather-suite route). Baseline: unrecorded; capture in Phase 0 (the design doc Phase 1 line assigns exactly this metric/route). Acceptance: stallCount strictly reduced on the composite vs the 200 ms default at equal network trace; zero change on single-source demos (fold degenerates to one source).

**Risks & dependencies.** Independent. Borders the pinned lift structure (between-sources, never vs the gate) — preserved. Risk: an archive with `temporalBucketMs = 0` (undeclared) must fall back to the wall-ms default, not to τ = 0; test 5 covers it.

#### BH-5 — Decode priority continuity: pool-wide queue, DecodeArgs priority, cancel wiring `§10.2 · M6 · Phase 1`

**Change.** Three coupled edits to `WorkerTileDecoder` (poopdeck:packages/core/src/tile-decoder.ts:227-527) and one to `DecodeArgs` (:25-71). (a) **Single pool-wide host queue with pull-on-idle**: delete the least-pending-count assignment (:314-323) and per-worker `queue: number[]` (:217); add `private hostQueue: number[]` (new, pool-level) with a lazily-sorted dirty flag; `dispatchNext` (:419-434) becomes `pullNext(entry: PooledWorkerEntry)` — an idle worker pulls the most-urgent host-queued job, makes the transferable `slice(0)` at that moment exactly as today (copy-at-enqueue stays rejected), and dispatches one job per worker. Worker crash blast radius shrinks to the one active job — `handleWorkerError` (:493-526) fails only `activeRequestId` and leaves host-queued jobs in the pool queue to be re-pulled (the doc's named benefit). (b) **Priority into DecodeArgs**: `DecodeArgs` gains `priority?: number` (new; lower = more urgent, the scheduler's Cesium semantics) and the host queue serves by `(priority, requestId)` instead of FIFO. Producer side: `decodeBytes` (archive.ts:2535-2576) gains `priority?: number`, threaded from the fetch path's already-computed `groupSchedulerPriority` value (archive.ts:2782-2803 / the `ranked` entries at :2926-2942) so the decode stage stops randomizing what the network stage established. (c) **Wire the dormant mid-pipeline cancel**: in the abort handler (:374-391), when the aborted request is the owner's `activeRequestId`, post `{type: 'cancel', requestId}` — the worker's `CancelRequest` handling (tile-decoder.worker.ts:45-48, :92-95) and post-decompress check (:116, :142) already exist with no sender; this saves the IPC parse + extraction, "the biggest chunk of wasted work a cancel can still save" (worker.ts comment :66-76). Also record per-job `costBytes = compressed.byteLength` (observable at enqueue, :329) in pool accounting for BH-6's telemetry and for size-aware tie-breaking within equal priority (least-bytes-first among ties, subsuming the doc's least-pending-bytes item — with a central queue there is no per-worker assignment left to balance).

**Architecture.** The decode stage becomes the CPU twin of the request scheduler: same priority scale, work-conserving, cancellation free before commitment. Preserved contracts: template-registry distribution on every spawn/respawn before any decode (:277-284) — the pull model must keep templates as the first message; zero-copy transfer boundaries (worker.ts:10-14, :127-139); `onPayload` OPFS hand-back; the two recorded rejections stay rejected — the queue is host-side only, and the cancel message goes only to the _active_ worker for its _active_ request (never queued behind work, the exact failure the rejection documents at :414-418). `InlineTileDecoder` unchanged. BYTE-NEUTRAL. Rollback: the central queue is internal — a config flag can restore per-worker assignment, but prefer full revert of the module (self-contained file).

**Tests.** Extend `poopdeck:packages/core/test/tile-decoder.test.ts` (verified present; it already mocks Workers). Unit: (1) head-of-line: worker A busy with a long decode, three jobs queued → an idling worker B pulls them (previously stranded on A); (2) priority order: jobs enqueued with priorities [5, 1, 3] complete in [1, 3, 5] order under a single worker; (3) no-priority jobs behave FIFO (back-compat); (4) abort of a host-queued job removes it with no copy and no worker message (existing test, must stay green); (5) abort of the _active_ job posts exactly one `cancel` message and the worker skips the parse (assert via the mock worker's message log + no `tile` response); (6) crash: host-queued jobs survive a worker crash and complete on the replacement (new — inverts the current all-fail expectation, which is the intended behavioral change); (7) finalize rejects everything with cancellation errors (existing). Guard tests (rejected-design boundaries): assert the compressed buffer is _not_ copied at enqueue (spy on `slice`) and that no `cancel` is ever posted for a host-queued job. Determinism: scripted enqueue/complete sequence → identical dispatch order across runs. Integration: `archive-incremental-delivery.test.ts` (verified present) extended to assert priority-tier tiles decode before prefetch-tier tiles when both are queued.

**Evaluation.** Metric: decode telemetry already emitted — worker-path round-trip ms (tile-decoder.ts:358-363) and inline decompress/IPC split (:175-183); add `queueWaitMs` (host-enqueue → dispatch) to the worker sample. Objectives: p95 time-to-ready for priority-class tiles, and aborted-decode CPU (count of cancel messages that landed before parse). Baseline: unrecorded (§10 notes "the measurable objectives all exist already" but no numbers) — capture in Phase 0 via the telemetry probe on storm-4d and `/drive` (the draw-call-bound routes with the heaviest decode streams). Acceptance: priority-tile p95 time-to-ready improves under a saturated pool with no throughput loss; zero late-response errors (discard path still clean).

**Risks & dependencies.** Borders two pinned rejections (copy-at-enqueue; worker-side cancel queueing) — both guard-tested. Risk: the crash-semantics change (6) alters an existing documented behavior (host-queued jobs currently fail with the worker, :500-511) — this is the intended dominance argument from §10.2 gap 1, but call it out in review. Independent of BH-1–4; BH-6 depends on its `queueWaitMs` telemetry.

#### BH-6 — Decode pool sizing adapted from queue-wait telemetry `§10.1 · M6 · Phase 1 (late)`

**Change.** Replace the fixed `m = max(1, min(4, cores − 1))` (tile-decoder.ts:244-255) as the _permanent_ answer with an adaptive controller: `WorkerTileDecoder` gains `private maybeResizePool(): void` (new), invoked on decode completion, maintaining EWMAs of `queueWaitMs` (from BH-5) and service ms. Grow by one worker when `EWMA(queueWait) > GROW_RATIO × EWMA(service)` sustained over `RESIZE_MIN_SAMPLES` completions; shrink by one after `SHRINK_IDLE_MS` of a worker never being pulled to. Bounds: `m ∈ [1, κ − 1]` (the render-core reservation is a hard constraint from the provisioning model, :252-254 rationale preserved); the current rule remains the _initial_ size. Growth cost is bounded: each spawn re-ships the template registry (:277-284, already handled on every spawn). Expose `getPoolStats(): {poolSize: number; pendingBytes: number; queueWaitEwmaMs: number}` (new) for the perf HUD. `poolSize` option (:244-247) becomes a pin that disables adaptation.

**Architecture.** Closes the D2 loop the problems doc names: "queue-wait samples are emitted and never fed back" (§10.1 gap). The singleton shared pool (:534-571) means resizing serves all archives; per-source decode fairness stays out of scope (no backlog item, and BH-5's priority continuity already carries the upstream DRR's decisions into the queue order). Bounded memory: cap at `κ − 1` and shrink-on-idle bound standing worker cost `m × M_w`. BYTE-NEUTRAL. Rollback: `poolSize` pin, or disable the controller by constants (`GROW_RATIO = Infinity`).

**Tests.** Extend `poopdeck:packages/core/test/tile-decoder.test.ts`. Unit (mock workers with scripted latencies): (1) sustained queue-wait dominance grows the pool exactly to the point queue wait falls below the ratio, never past `κ − 1`; (2) idle shrink terminates a worker only when it holds no active job; (3) a fresh grown worker receives the template registry before any decode (extends the existing spawn/respawn registry test); (4) `poolSize` option pins the size — controller never fires; (5) hysteresis: oscillating load does not flap the pool (grow and shrink thresholds separated). Simulation: Poisson arrivals at 2× single-worker service rate → pool settles at ~2-3 and flow time approaches the M/G/m expectation qualitatively. Determinism: controller decisions are pure functions of the sample stream (no wall-clock randomness beyond injected timestamps).

**Evaluation.** Metric: mean and p95 decode round-trip (queue wait + service) at fixed tile-delivery rate, from the existing decode telemetry channel. Baseline: unrecorded — Phase 0 capture on storm-4d (heavy tiles) and a many-small-tiles route (earthquakes-v2). Acceptance: p95 round-trip reduced on ≥ 8-core machines with no regression on 4-core (where the cap already binds); pool never exceeds `κ − 1` in any trace.

**Risks & dependencies.** Depends on BH-5 (queue-wait split; central queue makes "worker idle" well-defined). Risk: worker spawn latency during a burst — mitigated by growing at most one per evaluation. Borders no register entry.

#### BH-7 — Loop-aware eviction rotation + byte-density scoring within tiers `§9.4 · M6 · Phase 1`

**Change.** Two edits to `evictUnusedTiles` (spatiotemporal-tileset.ts:3811-3982) plus loop plumbing. (a) **Loop rotation**: `BufferSource` gains optional `setLoopWindow?(range: {start: number; end: number} | null): void` (new); the tileset implements it, storing `private loopRange`. The `PlaybackGovernor` — which already subscribes to the clock's `wrap` event (`wrapHandler`, playback-governor.ts:589-598), proving it knows the loop boundary — pushes the looping range to all sources on source-add and loop-mode change (exact TimeController loop-range accessor to be confirmed at implementation — (unverified) which method exposes it). In the over-budget tier classification (tileset:3926-3943), when `loopRange` is set, `behind`/`ahead` are computed in loop-modular arithmetic: `aheadMod = ((d × (t − playhead)) mod span + span) mod span` — a tile far "behind" the head is near-imminent across the wrap, so it classifies into tier C/D by its wrapped ahead-distance instead of tier B's evict-first, fixing the "exact inverse of Belady" wrap pathology. With no loop set, behavior is byte-identical to today (guard-tested). (b) **Byte-density within tiers**: within tiers B and C, quantize the distance metric to bucket-sized bands and sort within a band by descending `header.byteSize` — equal-distance candidates free the byte budget fastest first, so fewer tiles are evicted per over-budget event; tier A keeps LRU order, tier D untouched (last resort stays purely recency-ordered). The tier concatenation, truncation loop (:3956-3973), `runwayEvictions` accounting (:3969-3979), needed/pinned/in-flight exclusions (:3880-3890), and grace-timer under-budget path (:3829-3865) are all preserved.

**Architecture.** The register pins "B→C→D tiering under one-directional playback" — kept: rotation is an _addition_ activated only when a loop is declared, and non-loop ordering is unchanged; byte-density is a within-tier refinement that never crosses a tier boundary. Invariants: never evict needed/pinned/in-flight (unchanged); bounded memory (same budgets); deterministic given (tiles, playhead, direction, loopRange). BYTE-NEUTRAL. Rollback: `setLoopWindow(null)` disables rotation; band-sort behind a constant.

**Tests.** Extend `poopdeck:packages/core/test/eviction-playhead-tiers.test.ts`, `eviction-buffered-timeline.test.ts`, `eviction-inflight.test.ts` (all verified present). Unit: (1) loop set, playhead near loop end → tiles just past the loop start are _protected_ (tier D/C), tiles mid-loop-distance evict first — the Belady order on the wrapped metric; (2) loop unset → eviction plan byte-identical to today's for a fixed fixture (regression pin); (3) within-tier: two tier-B tiles in the same distance band, 10× byte-skewed → big one evicts first; across bands, distance still dominates; (4) tier boundaries (`keepBehind`, `protectedAhead`, :3914-3915) unchanged by both features; (5) in-flight/pinned/needed exclusions hold under rotation (extends eviction-inflight). Property: for random cache states, the plan under loop rotation never evicts a tile whose wrapped ahead-distance is smaller than that of a surviving same-tier tile (Belady-order consistency). Simulation: two full loops over a synthetic archive with cache = 60% of loop working set — refetched bytes per loop with rotation ≪ without (the §7.6/§9.4 pathology measurably repaired).

**Evaluation.** Metric: `cacheStats.runwayEvictions` and refetched bytes per playback loop (the §9.4 gap's named measurables). Baseline: O1's acceptance line is `runwayEvictions → 0` under looped playback; current per-route values uncaptured — Phase 0. Routes: any looping showcase demo under a constrained `maxCacheByteSize` (drifters, storm-4d with lowered budget to force pressure). Acceptance: runwayEvictions → 0 across a full loop at a cache sized to ~70% of the loop working set; no change in eviction counts on non-looping routes. Refetched-bytes-per-loop counter is NEEDS-HARNESS (add to the Phase 0 harness inventory alongside the QoE counters).

**Risks & dependencies.** Borders the pinned tiering register entry — the addition-not-replacement framing is the compliance argument; the regression pin (test 2) is the enforcement. Depends on the governor→source plumbing (same release as BH-4's `BufferSource` extension to avoid two interface churns). Risk: modular arithmetic near `span ≈ timeWindow` degenerates (everything protected) — clamp: rotation only when `span > protectedAhead + keepBehind`.

#### BH-8 — Playhead-aware score in the compressed-byte LRU `§7.6 · M6 · Phase 1`

**Change.** The per-archive compressed cache (`byteCache`, archive.ts:3326-3404) and the process-shared byte LRU (:422-464) gain a bounded playhead-aware victim selection. `ByteCacheEntry` (:466-470) gains `timeStart: number` (new, from the tile's directory entry at store time); the archive caches the last threaded playhead (`this.lastPlayhead = {time, direction}` (new), updated in `getTiles` when `options.playheadTime` is present — the field already rides `TileRequestOptions`, archive.ts:2814-2820) and the loop range via a new archive method `setLoopWindow(range | null)` (new; the tileset forwards its BH-7 value). `evictIfNeeded` (:3386-3404) changes from evict-the-single-oldest to: scan the `EVICT_SCAN_LIMIT = 8` (new const) oldest entries and evict the one maximizing loop-aware directional distance-to-playhead (BH-7's `aheadMod` metric; ties → oldest), keeping O(1)-amortized eviction (constant scan). The shared LRU's `SharedByteCacheEntry` (:422-425) gains `score?: () => number` (new; higher = evict sooner) supplied by the owning archive using the same metric; `registerSharedCacheEntry`'s over-budget loop (:452-463) applies the same K-oldest scan. When no playhead was ever threaded (non-playback consumers), both paths degenerate to today's exact LRU.

**Architecture.** This is §7.6's minimal repair: LRU's cyclic-scan worst case (looping playback evicts exactly what the playhead needs next loop) is broken by making the victim choice playhead-aware _within a bounded window of the LRU order_, rather than replacing the structure (full GDS/ARC here is explicitly not attempted — the memory tier's costs are near-uniform per byte, and the K-oldest scan captures the loop win at a fraction of the complexity). Correctness side-constraints preserved: poisoned-entry eviction/self-heal (`dropCachedBytes`, :3370-3376), fingerprint invalidation (owned by the OPFS tier), per-archive count cap and shared byte cap unchanged (archive.ts:395-414 device constants untouched). BYTE-NEUTRAL. Rollback: `EVICT_SCAN_LIMIT = 1` is exactly today's LRU.

**Tests.** Extend `poopdeck:packages/core/test/archive.test.ts` (verified present; byte-cache tests live there — confirm exact describe block at implementation). Unit: (1) looped playback trace over a cache at 50% of working set: byte hit rate strictly above plain LRU's (construct the cyclic-scan adversary); (2) no playhead threaded → eviction order byte-identical to today (regression pin); (3) scan bound: victim always among the K oldest (LRU-order safety — a recently-touched entry is never evicted); (4) shared-LRU: two archives, one looping — the looping archive's imminent entries survive shared-budget pressure; (5) poisoned-entry drop path unaffected. Property: for random traces, hit rate ≥ plain LRU minus ε on non-cyclic traces (the scan can only reorder within the K-window). Determinism: same trace → same evictions.

**Evaluation.** Metric: byte-weighted hit rate and refetched bytes per playback loop (the §7.6 gap's named measurables; offline benchmark = Belady/MIN on the realized trace). Baseline: unrecorded. NEEDS-HARNESS: a trace-replay micro-bench over the byte cache (pure, no browser) — small addition to the Phase 0 inventory; browser-side, second-loop network bytes on drifters via the QoE/network counters. Acceptance: refetched bytes on loop 2 reduced ≥ 2× on the constructed cyclic trace; zero regression on pan-only traces.

**Risks & dependencies.** Depends on BH-7's loop plumbing (shares the metric and the `setLoopWindow` signal). Borders no register entry. Risk: `timeStart` widening `ByteCacheEntry` is negligible (8 bytes/entry); the `score` callback on the shared LRU must not close over evicted state — wire it to read the owner's live playhead field.

#### BH-9 — OPFS GreedyDual-Size eviction + admission filter `§9.5 · M6 · Phase 1 (last)`

**Change.** In `OpfsTileCache` (poopdeck:packages/core/src/opfs-cache.ts): (a) `OpfsIndexEntry` (:41-44) gains `hits?: number` (new, additive JSON — absent reads as 0, so `INDEX_VERSION` stays 1 and no cache wipe occurs); `get()` (:376-399) increments it on hit. (b) `evict()` (:457-478) replaces the pure lastAccess sort with GreedyDual-Size: evict ascending `H_j = L + (1 + hits_j) × cHit(b_j) / b_j`, where `cHit(b) = RTT_EST_MS × linkBytesPerMs + b` (bytes-equivalent of one saved round trip plus the transfer itself; `RTT_EST_MS = 80` (new const), link rate from a new optional constructor hook `getThroughput?: () => number | null` (new) defaulting to a fixed nominal rate), and `L` is the floating GDS baseline persisted as `index.lFloor` (new, additive). `hits` is exactly the "re-access probability estimable from the persisted index at zero extra I/O" the §9.5 gap names. Ties → ascending lastAccess (deterministic, preserving the monotonic-counter property, :156-165). (c) **Admission filter**: `set()` (:408-450) gains `opts?: {zoom?: number}` (new; the archive passes `id.z` from its write sites, archive.ts:2568-2573); admission rule: always admit `zoom ≤ ADMIT_ALWAYS_MAX_ZOOM = 2` (overview/low-zoom tiles — highest cross-session re-access, every revisit starts there); otherwise admit iff `b ≥ ADMIT_MIN_BYTES = 4096` _or_ the key is in a session `doorkeeper: Set<string>` (new, bounded to 4096 entries, insert-on-first-rejection) — a TinyLFU-lite second-touch gate so tiny-but-hot tiles get in while one-touch tiny tiles skip the write + index churn.

**Architecture.** The cache remains opt-in default OFF (`opfsCache: false` — preserved; the cold-path duplicate-decompress rationale is untouched by this item). Multi-tab correctness unchanged: last-writer-wins index + Web Locks (:317-329) + orphan sweep (:345-368) — `hits`/`lfloor` ride the same index writes on the same coalesced flush. Fingerprint self-invalidation (key covaries with the directory hash, archive-side) unchanged. Silent no-op degradation unchanged. BYTE-NEUTRAL (client-only). Rollback: constants (`ADMIT_MIN_BYTES = 0`, GDS behind a flag reverting to lastAccess sort); the additive index fields are ignored by old code.

**Tests.** Extend `poopdeck:packages/core/test/opfs-cache.test.ts` and `archive-opfs.test.ts` (both verified present, running against the Node OPFS shim). Unit: (1) GDS: a 17 MB one-hit entry evicts before ten 5 KB multi-hit entries at equal recency (the satellite-z0-vs-leaf case from the gap); (2) `hits` persists across instances (rehydrated index) and absent-hits entries read as 0; (3) admission: sub-4 KiB tile skipped on first touch, admitted on second (doorkeeper); z ≤ 2 always admitted; (4) eviction determinism: same index state → same victim order (monotonic-counter tie-break); (5) multi-tab: concurrent flush with the new fields keeps `totalBytes` accounting exact (extends existing Web-Locks tests); (6) corrupt/legacy index without new fields loads cleanly (INDEX_VERSION unchanged — regression pin). Property: resident-set byte sum ≤ budget after every evict. Guard: `set()` still never throws to the data path (:444-449).

**Evaluation.** Harness: `poopdeck:tools/bench/src/cold-start-bench.mjs` + `cold-start.mjs` (verified present) — the second-visit scenario is exactly what these measure; the §9.5 gap's metric is "cold-start bytes fetched on second visit at fixed D". Baseline: to be captured in Phase 0's K10 cold-start re-run (the design doc ties the cold-start capture to Phase 0 explicitly). Acceptance: second-visit network bytes reduced at fixed 512 MB budget vs admit-all/LRU on a mixed-size fleet dataset (satellites + earthquakes-v2); OPFS write count (write amplification) reduced ≥ 30% with byte hit rate not lower.

**Risks & dependencies.** Independent of BH-1..8 (last in the Phase 1 order per the design doc). Borders the recorded default-OFF decision — this item must not flip the default; flipping is a separate decision gated on the cold-path cost. Risk: `cHit` mis-calibration inverts small/large preference — the constants are documented and the bench arbitrates; inclusive-vs-exclusive placement vs the memory tier stays explicitly unresolved (out of scope, per §9.5).

#### BH-10 — Derived playback params emitted via styleHints and consumed by derive-params `§11.5 (+§12 wiring) · M6 · Phase 1 reader / R1 emission`

**Change.** Cross-package, three sites. **Rust emission**: `suggested_playback_seconds` (crates/stt-optimize/src/analysis/properties.rs:139, currently `clamp(round(sqrt(bucket_count)), 20, 90)`) is refit to the frame-count band the §11.5 gap specifies: `P = clamp(K / F_TARGET_FPS, K/30, K/12)` further clamped to `[5, 300]` s, with `F_TARGET_FPS = 20` (new const) — a 10-frame dataset no longer plays 1 frame per 3 s, a 100k-frame one no longer implies 3,333 data-fps. `StyleHints` (crates/stt-core/src/metadata.rs:179, `suggested_playback_seconds` at :190) gains `suggested_time_window_ms: Option<u64>` (new, `skip_serializing_if = "Option::is_none"`), computed in stt-build after tiling as `argmax{W : β̄ × W ≤ M_REF}` capped at `24 × Δ`, where `β̄` = total payload bytes / archive span (both known post-encode from the directory the builder just wrote) and `M_REF = 256 MiB` (new const, documented reference client budget). Wiring flows through `compute_style_hints` (crates/stt-build/src/style_hints.rs:59) / `profile_properties` (properties.rs:69) with an extended signature carrying the byte total. **stt-optimize side**: `inspect`/`recommend` report the derived pair (fields surface in the dataset report so the recommend→LLM JSON carries them — extends the existing `layer_hint` plumbing). **TS consumption**: `StyleHints` (poopdeck:packages/core/src/types.ts:224-236) gains `suggestedTimeWindowMs?: number`; `resolvePlaybackParams` (poopdeck:packages/playback/src/derive-params.ts:305) step 3 precedence becomes `overrides.timeWindow ?? styleHints.suggestedTimeWindowMs ?? bucket × DEFAULT_TIME_WINDOW_BUCKETS` (:376-387), with the wake invariant (:389-400) still applied after; `PlaybackMetadataInput.styleHints` (:68-69) widens accordingly. `suggestedPlaybackSeconds` already flows (:369-373) — no TS change needed for duration.

**Architecture.** Closes the §11.5↔§12 wiring gap: both parameters are computable at build time from stats the pipeline already has, and `resolvePlaybackParams` stays the single pure resolver (determinism constraint: identical inputs → identical outputs, preserved). Authored-range semantics (pinned register entry) untouched — overrides still beat hints beat defaults, and editorial sub-windows are respected verbatim. **Byte-break status, resolved as the task asks**: packs and directories are byte-identical — only `manifest.json` gains optional fields / changed hint values, so this is _not_ R1-class pack-breaking work; however, it _is_ byte-changing for any manifest golden pin covering an archive built with hints, and it changes fleet manifests on republish. Verdict: the TS reader side is BYTE-NEUTRAL and ships in Phase 1 (absent fields → behavior unchanged); the Rust emission (both the new field and the changed `suggested_playback_seconds` values) batches with rebuild window R1's single fleet republish per P6 — not because packs move, but so manifests change once, reviewed, alongside R1's other manifest work (M7 coupling fields, K11 bounds). The stt-core v2-golden fixtures (crates/stt-core/tests/fixtures/v2-golden/, `expected-hashes.json`) are built by `make-golden-fixture.rs` without stt-build's hints pipeline, so those pins should not move — verify at implementation; stt-build's own `style_hints_e2e.rs` assertions _will_ change (e.g. the `Some(20)` expectations in style_hints.rs:239, :262). Rollback: reader ignores unknown fields; emission is flag-gated until R1.

**Tests.** Rust unit (extend inline tests in crates/stt-build/src/style_hints.rs:169-340 and properties.rs:250-271): (1) band math — K=10 → P clamps so data-fps ∈ [12, 30] (subject to the [5, 300] floor/ceiling); K=100,000 → likewise; K=0/unknown Δ → `None` (unchanged); (2) `suggested_time_window_ms` = min(byte-feasible W, 24Δ) on synthetic densities, absent when bytes unknown; (3) determinism: `compute_style_hints` twice → equal (extends the existing determinism test at style_hints.rs:312). Golden/E2E: extend `crates/stt-build/tests/style_hints_e2e.rs` (verified present) asserting the new manifest field end-to-end; extend `crates/stt-build/tests/reproducible_pipeline.rs` (verified present) — byte-identical re-run with the new emission on (the mandatory encoder-determinism test); assert stt-core `expected-hashes.json` pins unchanged (fixture builder doesn't emit hints — state loudly in the test if this assumption breaks). stt-validate: confirm the manifest schema accepts the new field (crates/spatiotemporal-tiles/src/bin/stt-validate/schema.rs — no style_hints-specific constraint found by search; (unverified) whether it validates hint fields strictly; add a schema case either way in `crates/spatiotemporal-tiles/tests/validate_cli.rs`, verified present). TS unit (extend `poopdeck:packages/playback/test/derive-params.test.ts`, verified present): (4) precedence chain for `timeWindow` incl. hint; (5) wake invariant still lifts a hint-derived window; (6) absent hint → byte-identical resolution to today (regression pin); (7) authored `timeRange` subset semantics untouched (existing tests stay green — the pinned-register guard).

**Evaluation.** Metric (the §11.5 gap's own): fraction of showcase datasets carrying a hand `targetPlaybackSeconds`/`timeWindow` override — measurable by grepping showcase `DEMO_META`/spec configs before and after; plus buffer-stall rate at defaults via `getQoeStats()` across the showcase fleet. Baseline: "today many carry explicit targetPlaybackSeconds" (recorded in §11.5; exact count captured at Phase 0 by the same grep). Acceptance: hand overrides → 0 for datasets where the derived defaults are visually acceptable (M6's stated metric), with the user browser-verifying aesthetics per the project's visual-verify preference — no Playwright screenshot loop; no stallCount regression at defaults.

**Risks & dependencies.** Depends on M1/M7 only socially (shares the R1 manifest window), not technically. Borders pinned "authored range semantics" — guarded by test 7. Risks: changed `suggested_playback_seconds` values alter default pacing of rebuilt archives fleet-wide — this is the point, but it makes the R1 republish the moment showcase demos change feel, so the browser-verify pass must cover pacing; `M_REF` is a new constant-where-a-function-could-be (a knowing P1 debt — record it as derived-once-from-device-budget with its revival trigger rather than silently hand-set).

## 4. Measurement oracle v2 & the `--target-size` flagship (M1, M3)

This section turns `stt-optimize`'s sampled measurement (§12.1) into the universal post-zstd objective evaluator — stratified, noise-quantified, efficiency-honest attribution — points the five D4 decision sites and the advisor/doctor loops at it, and then ships the program's most user-facing move: a `--target-size B` budget solver over the reversible lever set with shadow-priced lossy proposals. Everything here is measurement- and recipe-side: no archive format bytes change, so every item is BYTE-NEUTRAL and ships in Phases 2–3 ahead of the R1 rebuild window. The do-not-touch lines it borders — `COLUMN_ZSTD_LEVEL = 19` (inspect.rs:33), the diff gate's exact directory metric (diff.rs:120-121), no analytic size models, contractual deterministic sampling, and the lexicographic no-thinning filter — are designed around, not through.

#### MO-1 — Stratified deterministic decode sampling by (zoom, time-bucket) proportional to blob bytes `§12.1 · M1 · Phase 2`

**Change.** Replace the flat systematic stride in the decode pass of `inspect` and the doctor with byte-proportional stratified sampling. Today `sample_stride` (crates/stt-optimize/src/analysis/inspect.rs:149-151) picks every `ceil(total/n)`-th directory entry, and `doctor` decodes `DOCTOR_SAMPLE_TILES = 8` the same way (crates/stt-optimize/src/doctor.rs:86); entries are sorted by the space–time curve, so a stride near the bucket count can sample one bucket phase (the aliasing risk §12.1 names). New function `stratified_sample_indices(entries: &[&DirEntry], n: usize) -> Vec<usize>` (new) in inspect.rs: group entries by `(zoom, time_start)` stratum — both fields already on every entry, exactly the stats the directory pass computes for free (inspect.rs:255-292) — allocate `n_h = n · bytes_h / bytes_total` per stratum with largest-remainder rounding (deterministic tie-break: ascending `(zoom, time_start)`), then take a stride within each stratum starting at its first entry. `inspect(tileset, sample)` (inspect.rs:251) and the doctor's `sample_decode` switch to it; `--sample 0` keeps its directory-only fast-mode semantics (the memory-recorded `inspect --sample 0` contract). `DecodeStats` (inspect.rs:69-83) gains `design: String` (new, `#[serde(default)]`, values `"stride-v1"`/`"stratified-v2"`) so a report is self-describing.

**Architecture.** Pure measurement-side change inside `stt-optimize`; archive bytes untouched (BYTE-NEUTRAL, ships anytime). Invariants: sampling stays a pure function of the directory (no RNG, no arrival-order dependence — contractual per §12.1, inspect.rs:145-151); directory-derived stats remain exhaustive and exact; the diff gate metric (`compressed_bytes`, diff.rs:120-121) is directory-exact and is not touched by any sampling change — this item only changes which tiles feed the _per-column_ side of diff. Rollback: the old stride path stays behind the `design` discriminator for one release; deleting `stratified_sample_indices` restores v1 behavior.

**Tests.** Unit (extend the existing inline `mod tests` in crates/stt-optimize/src/analysis/inspect.rs and crates/stt-optimize/src/doctor.rs — both verified present): (1) allocation sums to `n` and is proportional to stratum bytes on a synthetic 3-zoom directory with skewed bytes; (2) anti-aliasing guard: a directory whose entry order interleaves k buckets with stride ≈ k gets samples from every bucket (the exact failure mode of stride-v1 — cheap guard for the rejected design); (3) `n ≥ strata count` touches every non-empty stratum; (4) `--sample 0` decodes nothing. Determinism: two `inspect` runs over the same archive serialize byte-identical JSON (extend the report-serialization tests in crates/stt-optimize/src/report.rs's inline tests). Golden fixtures: none change (no encoder involvement). Integration: new crates/stt-optimize/tests/inspect_sampling.rs building a small packed archive via `stt_build` dev-dep (pattern from crates/stt-optimize/tests/export_roundtrip.rs, verified present) asserting stratified-vs-exhaustive share agreement within a tolerance.

**Evaluation.** Metric: max-abs per-column share error vs exhaustive decode at fixed n (share points), per §12.1's "Measurable". No baseline is recorded in the docs for stride-v1 error — NEEDS-HARNESS: an exhaustive-vs-sampled comparison sweep belongs in the Phase 0 harness inventory (design doc §6 Phase 0). Run on the local fleet archives (earthquakes-v2, hurricanes, storm-4d-isolines). Acceptance (O2): stratified error ≤ systematic error at equal n on every archive tested; no archive where aliasing makes stride-v1 win.

**Risks & dependencies.** Depends on nothing; MO-2 and MO-7 consume it. Risk: very fragmented strata (thousands of buckets) make `n_h < 1` everywhere — largest-remainder rounding must still return exactly `n` indices deterministically (unit test 1 covers it). Borders the diff-gate do-not-touch entry: any refactor that routes the _directory totals_ through the sampled path is forbidden.

#### MO-2 — Dispersion published with every per-column share `§12.1 · M1 · Phase 2`

**Change.** Both `ColumnCost` structs — measure.rs:86-95 and inspect.rs:88-103 — gain `share_stderr: f64` (new, `#[serde(default)]`): the standard error of the share estimate computed from per-decoded-tile (inspect) / per-synthetic-tile (measure, after MO-4) share observations under the MO-1 stratified design. `ColAcc` (inspect.rs:300-307) additionally accumulates per-tile `(bytes_j, bytes_total)` pairs to feed it. Consumers: (a) the doctor's `raw-f64-column` rule (doctor.rs:321-383) flags on `share − 2·share_stderr ≥ RAW_F64_MIN_SHARE` so tiny archives stop tripping noise (§12.5 gap 3); (b) `ColumnDiff` (diff.rs:97-107) gains `significant: Option<bool>` (new) — `|Δ| > 2·(stderr_before + stderr_after)` when either side sampled — a graded replacement for the binary `decode_sampled` caveat (diff.rs:130), which stays for compatibility; (c) MO-8 reports `projected_stderr` on its size estimate.

**Architecture.** BYTE-NEUTRAL, report-schema append-only (serde defaults keep old JSON deserializing, the same pattern `Recommendations.dominant_type` uses, recommend.rs:31-32). Invariant: `COLUMN_ZSTD_LEVEL = 19` stays fixed (inspect.rs:30-33) — dispersion is computed _at_ that level, never used to argue for changing it. The diff gate's exact `compressed_bytes` metric is untouched; `significant` annotates the per-column decision-support rows only. Rollback: drop the field; consumers all treat it as optional.

**Tests.** Unit (extend inline tests in measure.rs:385-500, inspect.rs, diff.rs, doctor.rs — all verified present): (1) homogeneous synthetic tiles → stderr ≈ 0; heterogeneous tiles → stderr > 0; (2) doctor noise-gating: a 3.5%-share column with stderr 1% no longer fires, with stderr 0.1% still fires; (3) diff `significant` flips exactly at the 2σ boundary; (4) exhaustive decode (`sample: None`) publishes stderr from the full population (finite, not zero — document the interpretation). Property: shares still sum to 1 ± 1e-9 (existing assertion in measure.rs:433-434 extends). Determinism: repeated runs byte-identical (stderr math must avoid HashMap iteration order — use the existing `BTreeMap` accumulators, inspect.rs:307).

**Evaluation.** Metric: empirical coverage — over the fleet archives, the fraction of columns whose exhaustive-decode share falls inside `share ± 2·stderr` (target ≈ 95%). Shares the MO-1 exhaustive-vs-sampled harness (NEEDS-HARNESS, Phase 0). Baseline: none recorded (the quantity did not exist).

**Risks & dependencies.** Depends on MO-1 (stratified per-tile observations) and MO-4 (multi-tile measure side). Risk: with n = 8 doctor tiles the stderr is itself noisy; the doctor gate must use it conservatively (subtract, never add). Borders `COLUMN_ZSTD_LEVEL` do-not-touch as stated.

#### MO-3 — Leave-one-out attribution (m+1 oracle calls) `§12.1 · M1 · Phase 2`

**Change.** Replace the singleton attribution proxy with leave-one-out in both call sites. Today each column is re-encoded _alone_ (single-field IPC + zstd — `attribute_columns`, measure.rs:344-383; `ipc_zstd_len` per field, inspect.rs:232-241,340-358), which violates efficiency — the per-column sum exceeds whole-tile bytes, documented at inspect.rs:10-15 and measure.rs:90-95. New function `attribute_columns_loo(batch: &RecordBatch, zstd_level: i32) -> Result<Vec<(String, LooCost)>>` (new, shared via a small `attribution` module both files use): compute `v([m])` = zstd(IPC(all columns)) plus, for each column j, `v([m]\{j})` = zstd(IPC(batch minus column j)) — m+1 compressions, comparable cost to today's m singleton encodes. Per column publish two numbers on `ColumnCost`: `marginal_bytes: u64` (new) = `max(0, v([m]) − v([m]\{j}))` — the _actual_ post-zstd bytes the tile sheds if the column vanishes, the honest unit for doctor what-ifs — and `share` = normalized marginals (Σshare = 1, restoring the fairness axioms' efficiency property at the leave-one-out compromise §12.1 names as standard).

**Architecture.** BYTE-NEUTRAL; attribution operates on decoded Arrow batches exactly as today, so all columns — geometry, `id`, times, properties — are handled uniformly without touching the production encoder. Invariants: deterministic column order = schema order; metadata stripped before IPC exactly as the current code does to stay byte-stable (inspect.rs:341-346). Shares change value versus the singleton proxy — every downstream threshold consumer (doctor `RAW_F64_MIN_SHARE`, quantize advisor's `column_share` citation, quantize.rs:195-201) re-reads against the new definition; MO-2's stderr makes that re-read noise-aware. Rollback: the singleton path remains as `attribute_columns_singleton` for one release behind the `design` discriminator from MO-1.

**Tests.** Unit (extend measure.rs and inspect.rs inline tests): (1) efficiency: Σ marginal-derived shares = 1; (2) a duplicated column pair (perfect cross-column redundancy) gets near-zero marginals for each copy while the singleton proxy over-charged both — the misattribution case §12.1 documents; (3) a high-entropy column dominates marginals; (4) `m = 1` degenerates sanely. Property (new, in the shared attribution module): for random small batches, `0 ≤ marginal_bytes ≤ v([m])`. Golden fixture: none change. Determinism: byte-identical re-runs (existing sorted-descending assertions in measure.rs:441-445 extend to the new fields).

**Evaluation.** Metric: attribution error vs a sampled-Shapley reference (computable offline for m ≤ 12 columns) on three fixture datasets, LOO vs singleton — acceptance: LOO strictly closer on every dataset. Secondary: doctor projections built on `marginal_bytes` (MO-7) vs realized rebuild deltas. Uses the MO-1 harness (NEEDS-HARNESS, Phase 0).

**Risks & dependencies.** Independent of MO-1/MO-2 but shares the `ColumnCost` shape — land the serde changes together. Risk: zstd subadditivity can make Σ marginals ≪ `v([m])` on highly redundant tiles, making normalized shares spiky; MO-2's stderr quantifies exactly this. Borders the "analytic size models" rejection favorably: LOO is more measurement, not a model.

#### MO-4 — Synthetic-tile layout from the observed features-per-tile distribution `§12.1 · M1 · Phase 2`

**Change.** `measure_sample` (measure.rs:136-181) currently packs the whole 5000-feature stride sample (loader.rs:85,158-163) into ONE synthetic tile via `build_layer` (measure.rs:237-338), amortizing per-tile encoder + zstd framing overhead over the entire sample rather than real tile occupancy (§12.1 Incumbent). New: `pub struct SyntheticLayout { pub tile_sizes: Vec<usize> }` (new) with constructor `SyntheticLayout::from_density(result: &AnalysisResult) -> Self` (new) — deterministic evenly-spaced quantiles of the per-`(x,y,t)` feature-count distribution the density model already computes at the recommended max zoom (crates/stt-optimize/src/analysis/density.rs buckets features into exactly those cells; `ZoomDensity` carries median/p95/max, density.rs:39-73) — and `pub fn measure_sample_layout(sample: &[SampledFeature], settings: &MeasureSettings, layout: &SyntheticLayout) -> Result<Option<MeasuredEncoding>>` (new): split the sample into `layout.tile_sizes.len()` tiles of proportional sizes, encode each through `encode_tile_with` (the existing production-encoder path, measure.rs:163-168), sum compressed bytes. `MeasuredEncoding` gains `tiles: usize` (new, serde default 1) and feeds per-tile dispersion to MO-2. `measure_sample` becomes a single-tile wrapper; `recommend_for` (crates/stt-optimize/src/lib.rs:35-58) and the advisors migrate to the layout form, all against the same layout so trials stay comparable. `MIN_MEASURE_FEATURES = 24` (measure.rs:36) now applies per measurement, with a floor of 8 features per synthetic tile to keep framing noise bounded.

**Architecture.** BYTE-NEUTRAL. Invariant: one layout per analysis run, derived deterministically from the density scan, shared by baseline and every trial — otherwise trial deltas confound layout with lever. Bounded memory: same sample, just partitioned. Rollback: the single-tile wrapper _is_ the rollback.

**Tests.** Unit (extend measure.rs inline tests): (1) multi-tile bytes_total > single-tile bytes_total for the same sample (overhead is real — this is the bias being corrected); (2) layout quantiles are deterministic and reproduce a hand-computed case; (3) baseline and trial under the same layout: the existing `quantized_coords_never_larger` test (measure.rs:447-468) re-run through `measure_sample_layout`; (4) tiny sample still returns `None`. Determinism: byte-identical `MeasuredEncoding` JSON across runs. Integration: extend the advisor tests (quantize.rs:347-420, layout.rs:431-601 inline tests) to the layout path — assertions on advice content must survive unchanged.

**Evaluation.** Metric: `|Ŝ − S*| / S*` — density-extrapolated archive size (features × measured bytes/feature, density.rs:66-72 path) vs actual built archive bytes, across fixture datasets built at defaults. Baseline: unrecorded (the docs record only that the estimator is "unbiased only if … per-tile fixed overheads … cancel", §12.1). Acceptance: layout-based estimate strictly closer than single-tile on every fixture. Harness: build-and-compare script — NEEDS-HARNESS (Phase 0 inventory).

**Risks & dependencies.** Depends on the density analysis being available at measure time — it is, in `recommend_for`'s ordering (lib.rs:42-43 computes `measured` _before_ density; reorder so density's occupancy scan runs first, or derive the layout from the spatial coverage scan which precedes it). MO-5/6/8 consume the layout. Risk: per-tile splits change measured numbers everywhere; advisor threshold behavior shifts — MO-9's noise-aware gates are the principled absorber, so land MO-4 in the same release train.

#### MO-5 — Post-zstd trial-encode objective as an API for the five D4 sites `§3.3, §3.4, §2.6, §4.3, §5.1 · M1 · Phase 2`

**Change.** New module crates/stt-optimize/src/oracle.rs (new) exposing the sample oracle as a typed candidate-evaluation API other code (advisors, doctor, the MO-8 solver, later the M2 two-pass build) calls instead of re-deriving pre-zstd surrogates:

```rust
pub enum Candidate {
    ZstdLevel(i32),                                  // §5.1 per-dataset sweep, capped at 19
    CompactTimes(bool),                              // §2.6 menu on/off today; U16/bucket-anchored tiers when M2/R1 adds them
    QuantizeCoords(Option<f64>),
    QuantizeAttr { column: String, step: f64 },      // §3.3 width/step ladder per column
    QuantizeAttrsAuto(bool),
    CategoricalDict { column: String, dict: bool },  // §3.4 boundary calls, column-level trial
    FeatureBudgetBytes { max_features: usize },      // §4.3: measured marginal compressed B/feature so a byte cap prices wire bytes
}
pub struct TrialResult { pub candidate: Candidate, pub bytes_total: usize,
    pub delta_bytes: i64, pub delta_frac: f64, pub stderr: f64 }
pub fn run_trials(sample: &[SampledFeature], layout: &SyntheticLayout,
    baseline: &MeasureSettings, candidates: &[Candidate]) -> Result<Vec<TrialResult>>
```

Whole-tile candidates thread through `MeasureSettings` → `EncoderConfig`, which already carries every needed knob: `compact_times` (crates/stt-core/src/arrow_tile/config.rs:191), `quantize_attrs: HashMap<String, f64>` (config.rs:164), `quantize_coords_m`, `quantize_attrs_auto` — `MeasureSettings` (measure.rs:42-50) grows matching fields with today's defaults. Column-level candidates (`CategoricalDict`, `QuantizeAttr` steps) trial via the MO-3 attribution surface (encode the column both ways, IPC + zstd), replacing decision-support use of the pre-compression model `categorical_dictionary_is_smaller` with its unmeasured `CATEGORICAL_DICTIONARY_IPC_OVERHEAD = 192` (crates/stt-core/src/arrow_tile/encode.rs:51-71, verified) and the raw-width arithmetic of §3.3.

**Architecture.** BYTE-NEUTRAL and deliberately _not_ wired into the per-tile encoder: `choose_time_forms` (encode.rs:189, verified), `build_quantized_numeric_auto` (crates/stt-core/src/arrow_tile/quantize.rs:462, verified), the dict comparison, and `TileBudget::enforce_indexed` (crates/stt-core/src/budget.rs:98, verified) keep their current behavior until the M2 two-pass build consumes this oracle for _dataset-global_ verdicts in rebuild window R1. That split is load-bearing: §13.2's rejection of sample-dependent type decisions means the oracle may only inform decisions that are functions of the dataset domain, never per-tile flips — the API returns dataset-level verdicts from the deterministic sample by construction. The dead `max_compressed_size` (budget.rs:34, verified never enforced) is _not_ resurrected here; §4.3's enforce-or-delete call rides M7/R1. Rollback: the module is additive; consumers fall back to their current heuristics.

**Tests.** Unit (new inline `mod tests` in oracle.rs): (1) `ZstdLevel(19)` vs `(3)` reproduces the sign of the advisor's existing publish trial (layout.rs:63-122); (2) `CategoricalDict` on a heavy-repeat plain-Utf8 column measures the post-zstd gap _smaller_ than the pre-compression model claims — the recorded §3.4 overstatement, pinned as a guard test; (3) `CompactTimes` trial runs both settings through `encode_tile_with` and returns a signed delta; (4) every candidate list evaluates in input order with deterministic results. Determinism: byte-identical `Vec<TrialResult>` serialization across runs (required — solver input). Golden fixture: unchanged. Integration: extend crates/stt-optimize/tests/export_roundtrip.rs's sibling (new file trials_oracle.rs) running trials on a built fixture's re-loaded source.

**Evaluation.** Metric (M1's stated objective): count of decisions that flip when measured post-zstd. Known recorded inversion to reproduce: compact times measured **+3.4% wire / −13.1% uncompressed** (§13.4 via design-doc D4) — acceptance: the oracle reports the wire-side sign correctly on the dataset class where that was measured. Secondary: §3.4 boundary flips on earthquakes-v2/hurricanes (dictionary share 43.9%/33.6% recorded). Harness: exists — this _is_ the measure.rs machinery, extended.

**Risks & dependencies.** Depends on MO-4 (shared layout). MO-6/MO-8/M2 consume it. Risk: `EncoderConfig` growth must not disturb its determinism contract (config.rs:150-157 documents why configs travel explicitly — keep it that way; no process globals). Borders three register entries: sample-dependent type decisions (designed around, above), analytic size models (this replaces surrogate models with measurement), and zstd 22 (the sweep hard-caps at 19).

#### MO-6 — Composed-recipe re-measurement and 2–3 coordinate-descent rounds in `recommend` `§12.2 · M1 · Phase 2`

**Change.** Today `run_all` (crates/stt-optimize/src/advisors/mod.rs:70-77) runs quantize → temporal → layout → budget once, each against ONE shared default baseline (quantize.rs:40-51: "ONE baseline at build defaults, shared by both trials"), and `to_command` (recommend.rs:156-197) composes the recipe without ever re-measuring it (§12.2 Incumbent). New: `pub fn run_iterative(result: &AnalysisResult, data: &LoadedData, layout: &SyntheticLayout, rounds: usize) -> Result<IterativeAdvice>` (new, in advisors/mod.rs) where `IterativeAdvice { advice: Vec<Advice>, composed: ComposedMeasurement }` (new). Round 0 = the existing pass. Then, per round (fixed `rounds = 2`, const): assemble the composed non-lossy point θ (the `!lossy && !suggestion_only` subset plus zoom/bucket), re-measure it via MO-5's oracle, and re-trial each measured lever _against the composed incumbent_ — e.g. the zstd-19 win is re-priced on quantized data and vice versa, the exact interaction §12.2 names as silently assumed away (layout.rs zstd trial, quantize.rs trials become one-lever perturbations of θ). Advice whose measured shrink changes sign or significance updates its `projected`/`confidence` in place. `Recommendations` (recommend.rs:17-41) gains `composed_projected: Option<String>` (new, serde default) — "recipe measures N B on the sample vs M B at defaults (measured)" — and a parallel composed-with-lossy figure for the report only. Measurement caching keyed on `MeasureSettings` (a `BTreeMap` cache in `IterativeAdvice` construction) keeps total oracle calls ≤ ~15.

**Architecture.** BYTE-NEUTRAL; `recommend_for` (lib.rs:35-58) calls `run_iterative`; the existing `advise(result, data)` free functions remain as round-0 building blocks (contract documented at mod.rs:6-7 — extended, not replaced). Invariants preserved: fixed advisor order, fixed round count, deterministic candidate sets ⇒ byte-identical re-runs; lossy and `suggestion_only` classification is untouched — iteration refines _numbers_, never admissibility; `stt-build --auto` behavior (stt-build.rs:1662-1743, verified) is unchanged by this item (it consumes the same `Recommendations`). Rollback: `rounds = 0` reproduces today's single pass exactly (guard test below).

**Tests.** Unit (extend recommend.rs:199-388 and the advisor inline tests): (1) `rounds = 0` output equals `run_all` output field-for-field; (2) a constructed sample where quantization only clears `MIN_COORD_SHRINK` at zstd 19 (not at 3): round 1 flips the advice in, pinning the §12.2 non-additivity case; (3) `composed_projected` present iff the sample measured; (4) cache: composing twice with identical settings performs one encode (count via a test hook). Determinism (solver-change rule): two full `recommend_for` runs on the same file produce byte-identical JSON — new integration test crates/stt-optimize/tests/recommend_determinism.rs. Existing guards that must keep passing untouched: `lossy_advice_never_joins_to_command`, `suggestion_only_advice_never_joins_to_command` (recommend.rs:261-273,368-387, verified).

**Evaluation.** §12.2's stated measurable: gap between recommended-recipe archive bytes and best-of-grid over the same knob set on fixture datasets. NEEDS-HARNESS: a grid-sweep builder (build the fixture at every point of a small knob grid, compare) — Phase 0 infrastructure item. Baseline: today's one-pass recipe's gap (unrecorded; the harness establishes it). Acceptance (O2): iterative recipe ≤ one-pass recipe bytes on every fixture; never worse.

**Risks & dependencies.** Depends on MO-4, MO-5. MO-8 reuses its composed-point machinery. Risk: analysis wall-time grows ~5–10× in oracle calls — bounded by the cache and the 5000-feature sample; document in `--explain` output. Borders the §6.3 "axis rebasing" and §6.4 "whole-map-instant surrogate" rejections only in the sense that the ordering advisor's simulation (layout.rs:137-196) is _re-run unchanged_ — its playback caveat and `suggestion_only` handling (layout.rs:176-195) must survive iteration verbatim.

#### MO-7 — Doctor: byte-ranked joint what-ifs with measured shrink estimates `§12.5 · M1 · Phase 2`

**Change.** Three coupled edits to doctor.rs. (1) **Measured shrink:** replace the fixed `RAW_F64_SHRINK = 0.6` prior (doctor.rs:73, applied at doctor.rs:349,373-381) by re-encoding each flagged column from the already-decoded sample tiles under the proposed remediation (range-adaptive U16 via the MO-5 column-level trial) — milliseconds, the standard §12.5 gap 2 sets. This deliberately relaxes the "never re-encode" doctrine (doctor.rs:8-13, 34-36) to "never re-encode _tiles beyond the existing sample_"; module docs and `ESTIMATE_LABEL` (doctor.rs:36) change to `"(measured on sampled tiles)"` for measured projections, keeping the old label for the remaining prior-based ones. (2) **Byte units:** `Finding` (doctor.rs:132-147) gains `projected_bytes: Option<u64>` (new, `skip_serializing_if`), computed from MO-3 `marginal_bytes` scaled by the density extrapolation; the deterministic sort (doctor.rs:187-192) becomes `(severity, projected_bytes desc, code, message)`. (3) **Joint what-ifs:** `DoctorReport` gains `joint: Vec<JointWhatIf>` (new) — `{ remediation_set: Vec<String>, projected_bytes: u64, note: String }` — fusing overlapping projections so they stop double-counting: the recorded pair is z0-bomb × oversized-blobs (raising `--min-zoom` removes zoom rows whose oversized blobs would otherwise also be counted, §12.5 Incumbent); fusion = one re-projection over the directory with the min-zoom raise applied _before_ the blob rule, plus one combined column re-encode for raw-f64 + expensive-ids (disjoint by column, so additive — assert it).

**Architecture.** BYTE-NEUTRAL. Invariants: deterministic output ordering; the `--strict` CI gate (crates/spatiotemporal-tiles/src/bin/stt-optimize.rs:155-156, verified) keeps keying off severity alone, so re-ranking cannot flip CI; lossy remediations stay last with the DROP warning (doctor.rs preference-order contract); `RESERVED_COLUMNS` (doctor.rs:53-63) untouched. Rollback: `projected_bytes`/`joint` are additive; the 0.6 prior remains the fallback when a column's sampled decode is absent.

**Tests.** Unit (extend doctor.rs inline tests, verified present): (1) measured shrink replaces the prior — a synthetic archive whose f64 column actually shrinks 75% reports ~75%, not 60%; (2) fallback to the prior when `--sample 0` skipped decode, with the old label; (3) joint z0-bomb + oversized-blobs what-if < sum of individual projections on a constructed overlap; (4) ordering: within one severity, larger `projected_bytes` first, ties by code; (5) `--strict` exit behavior unchanged (extend the CLI-level check alongside crates/spatiotemporal-tiles/tests/validate_cli.rs's `CARGO_BIN_EXE` pattern, validate_cli.rs:115, in a new optimize_cli.rs). Determinism: byte-identical doctor JSON re-runs. Golden fixture: none change.

**Evaluation.** §12.5's measurable: bytes reclaimed per applied finding vs projected. Harness: build a fixture, apply the top remediation, and compare with `stt-optimize diff` — the exact-metric gate already exists (`--fail-on-growth`, stt-optimize.rs:128-129); only the scripted apply-and-rebuild loop is new (NEEDS-HARNESS, Phase 0, shares the MO-6 grid harness). Baseline: the 0.6-prior's projection error (unrecorded; docs record measured passes landing 50–75%). Acceptance: measured projections within ±15% of realized deltas on the fixtures; prior-based projections demonstrably worse or equal.

**Risks & dependencies.** Depends on MO-1 (sample), MO-2 (noise-gated 3% floor), MO-3 (`marginal_bytes`), MO-5 (column trials). Risk: the doctrine relaxation must not creep into full re-encodes — cap total re-encode work at `DOCTOR_SAMPLE_TILES × flagged columns` and assert the cap in a test. Borders: the diff gate metric (used only as the _evaluation_ oracle, untouched); lossy-last remediation ordering (preserved).

#### MO-8 — `--target-size B`: the flagship budget solver `§5.2, §12.2, §12.3, §12.4, §5.1 · M3 · Phase 3`

**Change.** New flag on the `Recommend` subcommand (crates/spatiotemporal-tiles/src/bin/stt-optimize.rs:51-78, verified): `--target-size <SIZE>` (new; integer bytes or `K/M/G`/`KiB/MiB/GiB` suffix), plus `--fail-if-over-target` (new; CI symmetry with diff's `--fail-on-growth`). Today no target archive size exists anywhere in the CLI — the only "target bytes" is pack sizing (stt-build.rs:1017-1018, verified; §5.2 Setting). New module crates/stt-optimize/src/budget_solver.rs (new): `pub fn solve(result: &AnalysisResult, data: &LoadedData, layout: &SyntheticLayout, target_bytes: u64) -> Result<BudgetReport>` (new). Feasible set Θ₀ per M3: zoom clamp (candidates priced from the density model's per-zoom `estimated_size_compressed`, density.rs:66-72, calibrated by the MO-4 measured bytes/feature — turning §12.3's constraint-equality pick into a Pareto choice), temporal bucket width (coarser candidates re-priced by re-bucketing the density scan), temporal-LOD tier sets (their byte cost now _computed_ — `added_tier_bytes` (new) from the density model, closing §12.4's "never computed" gap; candidate (width, cutoff) pairs by budgeted greedy instead of the fixed ladder), zstd level (MO-5 sweep, capped 19), blob ordering (the existing simulator verdict, layout.rs:137-196 — but a playback-caveated `spatial` pick stays `suggestion_only` even under budget pressure), pack size (object-count constraint only; does not enter S). Search: distortion classes are few and the ladders tiny, so enumerate lexicographically — ascending `(D_zoom, D_time)` per §5.2's distortion order — and within each class take the min-Ŝ point; first class whose best point satisfies `Ŝ(θ) ≤ B` wins; the frontier's ~≤12 candidate points are re-measured through the composed oracle (MO-6 machinery) rather than trusted from density pricing (an MCKP/Lagrangian sweep degenerates to exactly this at these cardinalities, and enumeration is deterministic by construction). **Shadow prices:** at θ\*, price each Θ₁ lossy lever (`--quantize-coords` ladder pick, `--quantize-attrs-auto`, per-column `--quantize-attr`) via MO-5 trials _at the composed settings_ — fixing the recorded blindness that quantize wins were only ever measured at level 3 (§12.2). **Infeasible B:** if even the lexicographic floor over Θ₀ exceeds B, emit `feasible: false`, the floor recipe and its `floor_bytes`, and the shadow-price table showing what each human-gated lossy lever would additionally buy — features are never dropped and no lossy flag ever joins the command (the `to_command` filter, recommend.rs:184-195, is inherited verbatim). Output: `Recommendations` gains `budget: Option<BudgetReport>` (new, serde default) — `{ target_bytes, projected_bytes, projected_stderr, feasible, chosen: Vec<ChosenLever>, floor_bytes, shadow_prices: Vec<ShadowPrice> }` — rendered in text mode as a table after the recipe.

**Architecture.** BYTE-NEUTRAL as shipped (it emits recipes; archives change only when a human runs the emitted command). Sits strictly on top of M1: the solver's only size oracle is MO-4/MO-5 sample measurement plus the measurement-calibrated density extrapolation — no analytic size model enters (register). Invariants: P5 spine — the lexicographic no-thinning filter is structural (Θ₁ never enters the argmin; `Advice.lossy` semantics from advisors/mod.rs:53-64 unchanged); determinism — pure function of the input file (byte-identical `BudgetReport` re-runs); `Ŝ` is honest about error (MO-2 stderr; the report states when `B − Ŝ` is within noise). Rollback: flag absent ⇒ `budget: None` and the pipeline is exactly MO-6's.

**Tests.** Unit (new inline tests in budget_solver.rs): (1) generous B ⇒ no clamp, recipe equals the unconstrained recommendation; (2) tight B ⇒ zoom clamp chosen before bucket coarsening (lexicographic order pinned); (3) infeasible B ⇒ `feasible: false`, floor reported, command still non-lossy — the no-thinning guard test; (4) shadow prices present, sorted by marginal bytes desc, all `lossy: true`; (5) LOD tiers only chosen when `added_tier_bytes` fits the remaining budget (the §12.4 "recommended for free" fix); (6) size-suffix parsing. Property: monotonicity — `B1 < B2` ⇒ distortion class at B1 ≥ class at B2. Determinism: byte-identical report re-runs (integration, extends recommend_determinism.rs from MO-6). Guard (register): a test asserting the zstd sweep never evaluates level > 19. CLI: extend crates/spatiotemporal-tiles/tests/cli_reference_doc.rs (verified — the doc gate that fails on undocumented flags, cli_reference_doc.rs:427-441) by documenting `--target-size`/`--fail-if-over-target` in docs/api/cli-reference.md; add the flag to the new optimize_cli.rs integration test.

**Evaluation.** O3 acceptance verbatim: one-build publish-budget hit on three showcase datasets — metric `|built_bytes − B| / B` with the build from the emitted command, targets set at ~80% of each dataset's current size; lossy levers surface as shadow prices only; infeasible-B path demonstrated on one dataset. Secondary (§5.2 measurable): human build iterations to reach a publish size → ~1. Harness: the MO-6/MO-7 apply-and-rebuild scripts (NEEDS-HARNESS, Phase 0) plus `stt-optimize diff` as the exact validator. Baseline: the docs record the manual loop as the incumbent (no iteration count recorded).

**Risks & dependencies.** Depends on MO-2 (error bars), MO-4/MO-5 (oracle), MO-6 (composed re-measurement). Risk: extrapolation error at fixture scale — the report must carry `projected_stderr` and the acceptance run measures realized error; if extrapolation misses badly on a dataset class, the recorded fallback is to widen the sample, never to fit a formula. Borders: silent-thinning rejection (structural guard test 3), zstd 22 (guard test), diff gate (evaluation only), and M4's workload model — ordering choice here reuses the _existing_ simulator and inherits M4's later improvements without solver changes.

#### MO-9 — `stt-build --auto` handshake and retirement of the 0.05/0.03 acceptance constants `§5.2, §12.2 · M3 · Phase 3`

**Change.** Two edits. (1) **Handshake:** `stt-build` gains `--target-size <SIZE>` (new), implying `--auto encode`. Plumbing: `recommend_for` (lib.rs:35-58) gains a sibling `pub fn recommend_with(source: &DataSource, opts: &RecommendOptions) -> Result<Recommendations>` (new) with `RecommendOptions { target_size: Option<u64> }` (new); `recommend_for` becomes the `None` wrapper. `apply_auto_recommendations` (stt-build.rs:1662, verified) extends: when `rec.budget` is present, fold the budget-chosen Θ₀ levers into `args` under the existing `user_set` explicit-flag-wins guard (stt-build.rs:1674-1675) — zoom/bucket via the existing paths (stt-build.rs:1677-1692), plus new match arms for `--temporal-lod` and coarsened `--temporal-bucket` _only in budget mode_: these are additive/lossless and M3 places them in the auto-searched feasible set, overriding the current blanket "semantic lever, never auto-applied" arm (stt-build.rs:1760-1767) for the budget path only, each application info-logged with its measured byte price. Lossy shadow prices reuse the existing loud warn-path verbatim (stt-build.rs:1712-1716); a playback-caveated ordering stays suggestion-only in every mode. After the build, `stt-build` logs built-vs-target bytes. (2) **Constant retirement (P1):** delete `MIN_COORD_SHRINK = 0.05` and `MIN_ATTR_SHRINK = 0.03` (quantize.rs:29-33, verified) — the quantize advisor's accept gate becomes noise-aware: emit lossy advice iff measured shrink `> 2 × stderr` of the trial (MO-2/MO-4), and `HIGH_CONFIDENCE_SHRINK = 0.15` (quantize.rs:36) likewise becomes a stderr multiple; in budget mode the same trials feed MO-8's shadow prices, so the thresholds retire _in favor of_ shadow prices rather than accreting alongside them — M3's explicit constraint.

**Architecture.** Recipe-affecting but not format-byte-breaking: archives built via `--auto`/`--target-size` after this change may differ from before because _different existing levers_ are chosen — determinism holds (same input ⇒ same recommendation ⇒ byte-identical build, the stt-core reproducibility contract), and no encoder byte changes ride this item, so no R1 dependency; a fleet re-recipe under budget mode should still batch with R1 per P6. Invariants: `--auto` applies only reversible levers (spine — unchanged, now tested harder); explicit user flags always win; the `Advice` contract (flag names must exist in docs/api/cli-reference.md, advisors/mod.rs:45-47) extends to the new flags. Rollback: omit `--target-size` and behavior is exactly pre-MO-9 except the noise-aware quantize gate; that gate's rollback is re-pinning the two constants (one-line).

**Tests.** Unit (extend quantize.rs inline tests): (1) high-shrink sample still emits both quantize advices (existing tests re-based on the noise gate); (2) shrink below 2σ emits nothing — replaces the constant-threshold test; (3) constants gone (compile-time). Integration (new crates/spatiotemporal-tiles/tests/build_auto_target.rs, `CARGO_BIN_EXE_stt-build` pattern from validate_cli.rs:115): (4) `--target-size` build on a small GeoParquet fixture completes with built bytes ≤ target × (1 + tolerance) and logs the comparison; (5) explicit `--max-zoom` survives budget folding; (6) no lossy flag reaches the effective build config, and the warn-log carries the shadow-price lines — the no-thinning guard at the binary boundary; (7) budget-mode `--temporal-lod` folding respects a user-passed `--temporal-lod`. Determinism (solver rule): two identical `--target-size` builds produce byte-identical archives (extends crates/stt-build/tests/reproducible_pipeline.rs, verified present). Golden fixtures: stt-core pins unchanged (no encoder edits). Doc gate: cli_reference_doc.rs forces the new stt-build flag into docs/api/cli-reference.md.

**Evaluation.** Same O3 run as MO-8 but end-to-end through `stt-build --target-size` (the "one build" is literal): metric = publish-budget hit rate 3/3 showcase datasets, plus zero hand-edited flags in the resulting showcase build recipes (the memory-recorded from-source recipes are the checklist). For the retirement: count of quantize advices whose emit/suppress verdict changes on the fixture fleet, each explainable by measured stderr — reviewed once, recorded in the campaign memory.

**Risks & dependencies.** Depends on MO-8 entirely; keep both in one release so `--target-size` never ships against the constant-threshold advisors. Risk 1: auto-applying `--temporal-lod` in budget mode is a real policy change — it is bounded (lossless, additive, logged, budget-priced, user-flag-overridable) but needs the reviewer's eyes on the stt-build.rs:1760-1767 arm specifically. Risk 2: `--auto` requires a GeoParquet input (stt-build.rs:1664-1666) — `--target-size` inherits that limitation; error message must say so. Borders: the spine's "auto touches Θ₀ only" (this item is its enforcement point at the binary), and the blob-ordering playback gotcha (memory-recorded): the caveated-spatial suggestion-only rule must hold in budget mode — test 6/7 territory, guard it explicitly.

## 5. The workload model, ordering default & storage-layout DPs (M4)

This section builds M4: replace the ordering picker's fixed two-query surrogate (§6.4) with a real workload model — a third canonical _playback_ query plus per-dataset query weights — and then spend that same deterministic simulator three more times, as the pricing oracle for directory page breakpoints (§7.2), pack-boundary nudging (§6.5), and leaf-descriptor design (§7.3). The recorded gate this discharges: `measured` ordering becomes the build default only once the §6.4 weighting question closes (decisions doc :320-323), verified against the fleet re-audit in which the fixed `auto` heuristic mis-picked 12 of 36 archives. Bucket-width selection (§2.1) and temporal-tier cutoffs (§2.4/§7.5) are _future consumers_ of the same weighted workload — named here so their owners know the feed exists, but not in this section's scope.

#### WM-1 — The playback canonical query: sliding window with a buffered-runway cost term `§6.4, §6.3 · M4 · Phase 2`

**Change.** Extend `crates/stt-core/src/ordering_sim.rs` with the third canonical query the incumbent deliberately omits (ordering_sim.rs:164-166 evaluates only `scrub_hits` + `pan_hits`; the omission is patched by prose in `playback_caveat_for`, order_audit.rs:176-188). Concretely:

- `fn simulate_playback(order: &[u32], samples: &[TileSample], band: &HashSet<u64>, gap_bytes: u64, packs: &[u64], window_buckets: u64) -> PlaybackCost` (new). Semantics: the central band (`central_band`, ordering_sim.rs:343-354 — unchanged, shared integer math with spaceCurve.ts) plays a sliding window of `window_buckets` consecutive time buckets advanced one bucket at a time from `tb_min` to `tb_max`. Advance _k_ delta-fetches exactly `band × bucket τ_k` (already-buffered trailing buckets are not refetched), and — the structural difference from scrub — coalescing may not span buckets the playhead has not reached, so each advance is costed as its own per-pack coalesced read set. Implementation: one pass over the linearized order using prefix sums of blob lengths and per-bucket last-hit positions, so all per-advance costs come out in O(n + Σ hits) total, not O(n × buckets).
- `pub struct PlaybackCost { pub total: QueryCost, pub worst_advance_cost: u64 }` (new). The **buffered-runway term** is `worst_advance_cost` — the max over advances of the blended cost `bytes_k + G·reads_k`. The player must complete each advance inside one bucket of playhead time while holding a multi-window runway, so the stall proxy is the worst advance, weighted by `runway_multiplier`.
- `SimOptions` (ordering_sim.rs:70-86) gains `pub weights: QueryWeights` (new: `{ scrub: u32, pan: u32, playback: u32 }`), `pub playback_window_buckets: u64` (new, default 1), `pub runway_multiplier: u64` (new, default 4 — the production reader's 4×timeWindow prefetch runway recorded in the flow-and-riders campaign; exact reader symbol (unverified), the constant is a P1 fit target, not a law). **Default weights are `(1, 1, 0)`** so every existing pick — `--blob-ordering measured` at pack/mod.rs:946-971, `order_audit` at order_audit.rs:96-103, the layout advisor at layout.rs:143-144 — is bit-for-bit unchanged until WM-2/WM-3 turn the term on.
- `OrderingCost` (ordering_sim.rs:108-122) gains `pub playback: QueryCost` and `pub playback_worst_advance: u64`; `cost` becomes the weight-blended sum `Σ_q w_q·(bytes_q + G·reads_q) + w_playback·runway_multiplier·worst_advance_cost`. With default weights this reduces exactly to today's `total_bytes_read + total_reads*gap` (ordering_sim.rs:186).
- `OrderingCostRow` in `crates/stt-optimize/src/order_audit.rs:20-32` gains `playback_reads`, `playback_bytes`, `playback_worst_advance_cost` columns, reported **unconditionally** (even at weight 0) so the fleet re-audit in WM-3 has data before the flip; `format_text` (order_audit.rs:201-267) grows the column.

All call sites construct `SimOptions` by functional update (`..SimOptions::default()`, verified at order_audit.rs:96-99, layout.rs:143, pack/mod.rs:967-970), so the added fields compile through; only the ordering_sim inline-test `OPTS` literal (ordering_sim.rs:399-402) needs updating.

**Architecture.** Pure additive extension of the shared simulator both the writer and stt-optimize already consume; no new module. Invariants preserved: determinism (all-integer arithmetic, stable sorts, no RNG — contractual because the resolved ordering names every content-addressed pack, ordering_sim.rs:14-19); the playback query is client-realizable (hits ⊆ the central band × one bucket per advance — the whole-map-instant surrogate is a register-banned corrected design, ordering_sim.rs:335-341); the ranking never degenerates to request count alone (the blended form is preserved; the 669 MiB "2 reads = cheapest" incident is the recorded anti-lesson). BYTE-NEUTRAL as shipped: default weights reproduce today's picks, so no archive byte changes. Rollback: revert the module; no persisted state.

**Tests.** Extend the existing inline suite at `crates/stt-core/src/ordering_sim.rs:391-520`:

- _Unit:_ `playback_delta_fetch_never_refetches` (Σ advance `bytes_needed` = band bytes, each blob needed once); `playback_hits_are_confined_to_band` (guard test for the whole-map-instant ban: every counted blob is in `central_band`); `spatial_ordering_pays_worst_advance` (two cells × 24 buckets: spatial's `worst_advance_cost` strictly exceeds time-major's — the order-audit caveat, now a number); `default_weights_reproduce_legacy_cost` (for a grid of fixtures, `evaluate` with default `SimOptions` returns identical `cost` and identical ranking to the pre-change formula).
- _Property:_ per-advance costs summed equal a reference O(n×buckets) brute-force `simulate_query` run per bucket (small random-but-seeded fixtures, deterministic).
- _Determinism:_ extend `evaluate_is_deterministic` (ordering_sim.rs:486-501) to nonzero playback weights — two runs byte-identical including `worst_advance_cost`.
- _Golden-fixture:_ none change (byte-neutral); assert so by running `crates/stt-core/tests/reproducible_build.rs::v2_dataset_rebuild_is_byte_identical_including_schemas` unchanged.
- _Integration:_ extend `crates/stt-optimize/src/order_audit.rs` inline tests (`audit_ranks_and_recommends_over_a_real_archive`, order_audit.rs:278-345) to assert the playback columns are populated and the JSON round-trips.

**Evaluation.** Harness: `ordering_sim` itself plus `stt-optimize order-audit --format json` (wired at crates/spatiotemporal-tiles/src/bin/stt-optimize.rs:213-232, 617-628) — both named in the Phase 0 harness inventory. Metric: per-ordering `worst_advance_cost` (bytes-equivalent) and playback blended cost on the drifters/earthquakes-v2/flights trio. Baseline: the current two-query costs the audit already emits; the qualitative baseline is the production observation that `blobOrdering=spatial` silently stalls playback (recorded gotcha). Acceptance: on every multi-bucket fixture where the caveat currently fires, the playback term ranks the caveated ordering measurably worse than the playback-safe one — the prose caveat becomes a dominated cost row.

**Risks & dependencies.** Depends on nothing; everything else in this section depends on it. Risk: the runway term's constants (`runway_multiplier`, window width) are hand-set at birth — acceptable because weight 0 keeps them inert until WM-2, and they are P1 fit targets against telemetry later. Borders the do-not-touch register at three points and stays clear of all: the coalescing fuse rule is reproduced, not altered (simulate_query, ordering_sim.rs:266-330); axis rebasing of curve inputs is not touched (`linearize`/`space_time_key` unchanged); the pan-bias correction (bounded band) is inherited by construction.

#### WM-2 — Per-dataset query weights from `layer_hint`, co-versioned in the manifest `§6.4, §6.3 · M4 · Phase 2`

**Change.** Close the §6.4 weighting question with a deterministic weight table derived from build inputs the pipeline already computes:

- `pub fn workload_weights(layer_hint: Option<&str>, distinct_buckets: usize) -> QueryWeights` (new, in `ordering_sim.rs` so all three consumers share one table). Proposed initial table (constants are P1 fit targets, recorded in the function doc): `distinct_buckets <= 1` → `(1, 1, 0)` (no playback dimension — same rule `playback_caveat_for` uses, order_audit.rs:176-188); `layer_hint` ∈ {`"trips"`, `"points"`} and multi-bucket → `(1, 1, 2)` (playback-dominant — the project's headline use case); {`"paths"`, `"polygons"`} or absent, multi-bucket → `(1, 1, 1)`. Never a zero-weight scrub/pan pair: every dataset still pays its pan/scrub terms, so no ordering can win purely by starving a query family.
- Sources, all verified: the writer's finalize receives `&Metadata` whose `style_hints.layer_hint` is populated by default on every non-streaming build (`compute_style_hints` full=false path, crates/stt-build/src/style_hints.rs:44-49, 136-167; field at crates/stt-core/src/metadata.rs:196, 285); `order_audit` reads it via `PackedTileset::metadata()` (crates/stt-optimize/src/packed.rs:66); the pre-build layout advisor reads the analysis-side hint (`layer_hint: layer_kind_hint`, crates/stt-optimize/src/analysis/properties.rs:70). Wire all three: pack/mod.rs:965-971 passes `weights: workload_weights(metadata.style_hints.as_ref().and_then(|h| h.layer_hint.as_deref()), distinct_buckets)`; order_audit.rs:96-99 and layout.rs:143-144 likewise (the advisor keeps its `MIN_TILES_TO_SIMULATE = 8` fallback, layout.rs:127).
- **Manifest co-versioning (D6 repair, M7 hook):** new optional manifest field written next to `blob_ordering` (pack/mod.rs:390, written at :1311): `pub ordering_workload: Option<OrderingWorkload>` (new) — `{ scrub: u32, pan: u32, playback: u32, playback_window_buckets: u64, runway_multiplier: u64, coalesce_gap_bytes: u64 }`, serde camelCase, `skip_serializing_if = "Option::is_none"`. This records the workload _and_ the build-assumed coalesce gap, giving `order-audit` the drift flag §6.2 gap 4 asks for (a future reader-side gap change no longer silently invalidates `measured` layouts) and M5's adaptive-gap work its co-version anchor.
- `order_audit` compares the archive's recorded workload against the current table and flags drift the same way it flags `current != recommended` today (order_audit.rs:247-261).

**Architecture.** The ordering becomes a pure function of (blob key set, `layer_hint`, CLI flags) — still deterministic across identical rebuilds because `layer_hint` is itself a deterministic function of the input features (majority kind with fixed tie order, style_hints.rs:154-167). The manifest field is additive and optional: archives without it are byte-identical (the existing `blob_ordering: None` omission precedent, pack/mod.rs:2751), and the manifest is the mutable root pointer, not content-addressed, so no pack or directory address moves. BYTE-CHANGING only for archives built with `--blob-ordering measured` whose pick flips under the new weights — that is opt-in today, and the fleet effect lands with the R1 republish (P6). Rollback: weights fall back to `(1,1,0)` behind a `--ordering-workload legacy` escape flag on stt-build (new); the manifest field is ignored by all existing readers.

**Tests.**

- _Unit_ (ordering_sim.rs inline): `weights_table_is_total` (every `(hint, buckets)` combination returns a defined weight triple; unknown hint strings — the vocabulary is deliberately open, metadata.rs:191-196 — fall to the default row); `single_bucket_zeroes_playback`; `weighted_pick_flips_on_playback_dominant_fixture` (a fixture where `(1,1,0)` picks spatial and `(1,1,2)` picks time-major/hilbert3 — the mechanism the caveat prose described, now selecting).
- _Unit_ (order_audit.rs inline): extend `playback_caveat_fires_only_for_spatial_over_multiple_buckets` (order_audit.rs:348-358) — with playback-weighted ranking the caveat demotes from "warning about the model" to "note about the tradeoff"; assert the report carries the workload used.
- _Unit_ (layout.rs inline): extend `simulated_deep_time_recommends_spatial_with_playback_caveat` (layout.rs:518-558) — under trips/points hints the advisor's pick changes and `suggestion_only` clears when the playback term is _in_ the cost rather than beside it.
- _Property:_ weight monotonicity — increasing `playback` weight never improves the rank of the ordering with the worst `worst_advance_cost` (seeded fixtures).
- _Determinism:_ build the same dataset twice with measured ordering + weights on (extend `crates/stt-core/src/pack/mod.rs` inline test at :2931 and `crates/stt-build/tests/reproducible_pipeline.rs`) — byte-identical archives including the new manifest field.
- _Golden-fixture:_ v2-golden pins unchanged (fixtures are not built with `measured`; manifest additions to _newly built_ fixtures would change `expected-hashes.json` only if the fixture generator is rerun — do not rerun before R1).

**Evaluation.** Harness: `stt-optimize order-audit --format json` batch-run over the local fleet (the 36-archive set of the 2026-07 audit). Metric: pick distribution and blended cost delta per archive. Baseline: measured picks spatial 16 / time-major 12 / hilbert3 8 / morton3 0; `auto` mis-picked 12/36 (decisions doc :318-320). Acceptance: the weighted picker's picks are stable under re-run (deterministic), every playback-caveated spatial pick on a playback-class dataset (trips/points) flips or is explicitly cost-justified in the report, and no archive's pick is decided by a query family with zero weight.

**Risks & dependencies.** Depends on WM-1. Risk: `layer_hint` is absent on old archives (pre-hints builds) and on streaming builds if the hints pass is skipped there ((unverified) whether the streaming path populates style_hints) — the `None` row of the table must therefore be a sane generalist default, and `order_audit` on hint-less archives reports Low-confidence weighting. Risk: the weight constants are guesses until telemetry exists; they are recorded in the manifest precisely so a later re-fit can identify which fleet archives were laid out under which table. Borders: never rank by request count alone (weights scale blended costs, never raw read counts).

#### WM-3 — Promote `measured` ordering to the build default, gated on the fleet re-audit `§6.3, §6.4 · M4 · Phase 2 (fleet effect rides R1)`

**Change.** Flip the stt-build default for `--blob-ordering` from `auto` to `measured` (resolution site: crates/spatiotemporal-tiles/src/bin/stt-build.rs:1006-1011, writer wiring `.with_measured_ordering(measured_ordering)` at :1188; `PackWriter::with_measured_ordering` at crates/stt-core/src/pack/mod.rs:798). `auto` remains selectable and remains the fallback inside `measured` for degenerate inputs (empty native tier; fewer native tiles than a floor mirroring the advisor's `MIN_TILES_TO_SIMULATE = 8`, layout.rs:127). Update `BlobOrdering` doc comments (crates/stt-core/src/curve.rs:38-42) and the CLI reference; `curve.rs`'s `choose` heuristic (curve.rs:83-97) is untouched — it is still what `auto` means and what `order_audit` reports as `auto_choice` (order_audit.rs:121-137).

The **recorded gate** (§6.3: "promoting `measured` to default is PLANNED, gated solely on the workload-weighting question of §6.4") is closed by WM-2 plus this item's re-audit procedure, run _before_ the flip merges:

1. Batch `stt-optimize order-audit --format json` over the 36-archive fleet with WM-2 weights active (a ~20-line loop over the local archive roots; add `tools/fleet-order-audit.sh` (_shipped 2026-08_) so the run is reproducible).
2. Required showing (O2 acceptance): the weighted picker corrects **at least the 12/36** archives the fixed heuristic mis-picked, with zero regressions on the 24 it got right — "correct" now meaning cheapest under the _weighted three-query_ cost, so a former "win" that stalls playback is allowed to flip, and each flip must be explained by the playback column, not by a tie.
3. Zero nondeterminism: the batch run twice produces byte-identical JSON.

Also promote the CI teeth that already exist: `order-audit --strict` (exit non-zero when recorded ordering ≠ recommendation, stt-optimize.rs:225-231) becomes meaningful fleet-wide once the default produces recorded-and-recommended agreement by construction.

**Architecture.** Build-time-only policy change; no format, reader, or spec change. Determinism is preserved because `measured` is itself a pure function of the blob set + metadata (WM-1/WM-2). Cost: `measured` runs 4 simulator passes per query family at finalize over native-tier tiles — with WM-1's O(n + hits) playback pass this stays linear-ish; the 9.5 h national storm build must not regress measurably (budget: ordering resolution < 1% of build wall time; the simulator is directory-scale, not payload-scale). BYTE-CHANGING for any newly built archive whose measured pick differs from `auto`'s — by fleet measurement that is ~12/36 datasets — so the _fleet_ flip rides rebuild window R1 with the single republish (P6); the default flip itself can merge earlier since it only affects new builds. Known approximation, documented not fixed here: the build path simulates on pre-dedup uncompressed lengths (pack/mod.rs:960-964) — uniform across candidates, ranking-preserving; WM-5 improves the cut model, not this ranking.

**Tests.**

- _Unit:_ stt-build arg-resolution test — no flag → `measured_ordering == true`; explicit `--blob-ordering auto|spatial|...` still wins (extend the CLI arg tests near stt-build.rs:1006; file-level test location (unverified), else add to `crates/stt-build/tests/reproducible_pipeline.rs`).
- _Unit_ (pack/mod.rs inline, extending the test at :2931): tiny-input fallback — below the native-tile floor, `measured` resolves to `auto`'s choice and records it.
- _Determinism:_ extend `crates/stt-build/tests/reproducible_pipeline.rs` — default-flag double-build byte-identical (this is now the _default_ path, so the determinism test must cover it, per the every-solver-change rule).
- _Golden-fixture:_ `crates/stt-core/tests/v2_golden.rs` + `fixtures/v2-golden/*/manifest.json` — the fixture generator (`crates/stt-core/examples/make-golden-fixture.rs`) must pass an _explicit_ ordering so golden pins do not silently depend on the default ((unverified) whether it currently does — verify and pin explicitly as part of this item; if pins change they change once, reviewed).
- _Integration:_ `crates/spatiotemporal-tiles/tests/validate_cli.rs` — a default-built archive validates clean and records `blobOrdering` + `orderingWorkload`.

**Evaluation.** Harness: the WM-3 fleet re-audit script (new; the Phase 0 inventory item covers cataloguing it). Metric: corrected-pick count (target ≥ 12/36, zero regressions), plus blended-cost improvement per corrected archive in bytes-equivalent. Baseline: the 2026-07 fleet audit distribution (decisions doc :318-320). Post-R1: the K10 cold-start re-capture (backlog K10, docs/roadmap/README.md:306-314) is the fleet-level before/after for layout-sensitive cold reads — run it after the republish per the Phase 4 procedure, never against the stale 2026-07 numbers.

**Risks & dependencies.** Depends on WM-1 + WM-2; blocks nothing but feeds WM-4/WM-5 their workload. Risk: the published fleet is mostly `spatial`-ordered while local rebuilds are time-major-leaning (blob-ordering campaign memory) — the default flip therefore _widens_ local-vs-fleet divergence until R1 republishes; acceptable and already the standing state, but the R1 window is the deadline for convergence. Borders the register: the 4-candidate family is not expanded here (per-zoom hybrids are a §6.3 gap left for later); axis rebasing stays banned (lost three times — do not let the re-audit re-open it).

#### WM-4 — Directory page-breakpoint DP: structure-aware leaf cuts priced by the simulator `§7.2 · M4 · Phase 4 (R1)`

**Change.** Replace uniform fixed-size directory chunking — `sorted.chunks(page_entries)` at crates/stt-core/src/directory_page.rs:397 with `DEFAULT_PAGE_ENTRIES = 4096` (directory_page.rs:61) — with a deterministic DP over candidate breakpoints, priced by the same query families WM-1/WM-2 defined:

- `pub fn plan_page_breakpoints(entries: &[TileEntry], opts: &PageBreakOptions) -> Vec<usize>` (new, directory_page.rs). Candidate cut positions: every **zoom transition** in directory order (the §7.2 incumbent's named pathology: a leaf straddling a zoom boundary or spanning sparse low-zoom world tiles gets a near-global bbox descriptor that overlaps every viewport) plus **byte-balance candidates** every ~1024 entries. DP over candidates minimizes `Σ_q w_q · [α·|coalesce(F(q))| + β·Σ_{j∈F(q)} L_j]` — the §7.2 objective — where the query set is generated from the archive's own inventory by the WM-2-weighted canonical families (band viewports × {all-time, densest-bucket, sliding-window}), `F(q)` uses the exact `PageDescriptor::overlaps` predicate (directory_page.rs:111-129), and `coalesce` applies the reader's leaf-range fuse (`fetchAndMergePages` semantics, same gap constant the simulator mirrors). `L_j` is estimated as `entry_count × calibrated bytes/entry` during the DP (one whole-corpus encode calibrates the rate), with the final chosen segments exactly encoded — the at-rest inflation constraint `Σ_j L_j ≤ (1+δ)·L_whole` is checked against the _real_ encodes (measured slicing overhead is +6-19%, §7.2; δ defaults to 0.25).
- `pub fn encode_paged_directory_planned(entries: &[TileEntry], breakpoints: &[usize], zstd: bool, level: i32) -> Result<EncodedPagedDirectory>` (new) — the loop body of `encode_paged_directory_level` (directory_page.rs:375-488) refactored to take arbitrary contiguous segments; the existing uniform function becomes a thin wrapper.
- stt-build flag `--page-breakpoints {uniform|planned}` (new; default `uniform` until R1 flips it to `planned`), alongside the existing `--page-entries` / `--paged-directory-min-entries` (stt-build.rs:145, wiring at :1197).

The container already supports variable leaves: per-descriptor `entry_count`, `rel_offset`, `length` are authoritative and the root's `page_entries` is "Nominal … (informational)" (directory_page.rs:136-137), so **no reader format change** — but the TS reader must be audited for any uniform-chunking assumption in its leaf binary search before the default flips ((unverified) — an explicit checklist item, closed by the integration test below).

**Architecture.** Sits entirely inside the builder's directory-encoding step; entries' global sort order (directory_page.rs:388) is untouched, so leaves remain contiguous slices of one monotone order and `verify_paged_structure` (directory_page.rs:613) passes unchanged. Invariants: descriptors stay exact outward-rounded min/max over their segment (soundness — the accumulation at directory_page.rs:413-449 is reused verbatim per segment); breakpoints are a pure function of `(entries, opts)` — deterministic; the all-or-nothing cover rule (directory_page.rs:390-391) is global and unaffected. BYTE-CHANGING: the `.sttd` bytes and content address change, hence the manifest's directory pointer, root hash, and page hashes — rides R1, one republish. Rollback: `--page-breakpoints uniform` restores today's bytes exactly (guarded by test).

**Tests.**

- _Unit_ (extend the inline suite at directory_page.rs:745+): `planned_cuts_respect_zoom_transitions` (no leaf spans a zoom boundary when a candidate existed there); `planned_encode_roundtrip` (decode_paged_directory over planned cuts yields the identical entry list as whole-load — extending the existing round-trip tests); `uniform_mode_is_byte_identical_to_today` (the wrapper refactor changed nothing — regression pin); `inflation_budget_enforced` (a pathological corpus where fine cuts blow δ falls back to coarser candidates).
- _Property:_ for random-but-seeded entry sets, every query in the pricing family that matches ≥1 entry faults ≥1 planned leaf (zero false negatives — soundness), and `Σ entry_count == N`.
- _Determinism:_ `plan_page_breakpoints` twice on the same input → identical cuts; full archive double-build with `planned` byte-identical (extend `crates/stt-core/tests/reproducible_build.rs` and `crates/stt-build/tests/reproducible_pipeline.rs`).
- _Golden-fixture:_ v2-golden pins change when the default flips at R1 — regenerate via `make-golden-fixture.rs` once, in the R1 window, with the WM-3 explicit-ordering pin in place (`crates/stt-core/tests/v2_golden.rs`, `fixtures/v2-golden/expected-hashes.json`).
- _Integration:_ `crates/spatiotemporal-tiles/tests/validate_cli.rs` — planned-cut archive validates clean; TS-reader conformance (the poopdeck:packages/core test suite's paged-directory cases) against a planned-cut fixture, closing the uniformity-assumption audit.

**Evaluation.** Harness: the cold-start capture of `poopdeck:docs/roadmap/measurements-2026-07.md` (method and harness intact per K10; numbers stale — the Phase 0 K10 re-run is the baseline refresh this item prices against). Metric: surviving-leaf bytes and directory request count on a cold open, per dataset. Baseline (stale, to be re-captured): surviving leaves are **68.6%** of earthquakes-v2's cold-start bytes and **54.1%** of goes-glm-lightning's (`poopdeck:docs/roadmap/measurements-2026-07.md` §4, since cut to a pointer at `poopdeck:docs/roadmap/measurements-2026-08.md` §9.3) — the 54-69% band the design doc cites. Acceptance: planned cuts beat the _best uniform_ `page_entries ∈ {1024, 2048, 4096}` (the doc's "halving pageEntries is the obvious lever" strawman) on simulated cold leaf bytes for the K10 trio, at-rest inflation within δ; confirmed on the real fleet by the post-R1 K10 re-capture.

**Risks & dependencies.** Depends on WM-1/WM-2 for the pricing workload; rides R1 (P6) so it also depends on the R1 window opening. Risk: the DP's query family is still a surrogate — mitigated because it shares the WM-2 weights and the client-realizability constraint, and because losslessness means a bad cut costs bytes, never correctness. Risk: `L_j` estimation error skews cuts — bounded by the final exact-encode check. Borders the register: page hashes scale with leaf count and ride the same manifest (§7.2 notes they belong in the objective — the DP's α term covers the request side; manifest growth is checked in the inflation budget). Does not touch the leaf codec or the directory sort order (§7.1 is a format constant).

#### WM-5 — Pack-boundary nudging: shortest-path DP over cuts, next-fit untouched for its own model `§6.5, §6.2, §6.3 · M4 · Phase 4 (R1)`

**Change.** Add an opt-in planned-cut mode for pack objects. The incumbent next-fit cut — seal when the next blob would overflow `pack_target_bytes`, lone oversized blob owns its pack (`PackStreamWriter::append`, crates/stt-core/src/pack/mod.rs:1558-1574; `DEFAULT_PACK_TARGET_BYTES = 64 MiB` at :176) — is **register-pinned as exact for its own cost model** (fewest segments under capacity). This item changes the _cost model input_, not the cut rule for that model: the new objective adds the workload term §6.5 formalizes, and the solver for _that_ objective is the shortest-path DP the problems doc names as polynomial (`O(m·C/ℓ_min)`).

- `pub fn plan_pack_cuts(blobs: &[PlannedBlob], workload: &WorkloadQueries, opts: &PackCutOptions) -> Vec<usize>` (new, pack/mod.rs or a sibling `pack/cuts.rs`), where `PlannedBlob { len: u64, key: TileSampleKey }` carries the **post-dedup compressed** lengths in final layout order — fixing §6.5 gap 3 (the build-path simulation's pre-dedup-uncompressed drift) for the cut decision itself. Edge cost of a segment: `λ_obj` (default priced at one coalesce gap `G` in bytes-equivalent — a pack boundary costs at most one extra request per crossing query, P3's bytes-as-unit-of-account) plus the incremental blended workload cost of every WM-1/WM-2 query run the boundary splits, computed from the same per-query hit structures `ordering_sim` builds. Constraints: segments contiguous in σ (content addressing), `S_k ≤ C_max = 512 MB` hard (Cloudflare per-object cap), soft target `C` respected in the sense that cuts only _advance_ (a pack may be underfull, never overfull beyond the singleton exemption).
- `PackStreamWriter` gains `fn with_cut_plan(self, cuts: Vec<usize>) -> Self` (new) and an appended-unique-blob counter; `append` (pack/mod.rs:1558) consults the plan — cut before blob _i_ iff `i ∈ cuts` — with the next-fit rule retained verbatim as the no-plan path _and_ as a safety net (a planned cut may never produce an overfull pack; debug-assert).
- Sequencing inside finalize: the parallel-compress pass already produces every blob's blake3 and compressed length before the strictly sequential dedup/stream pass (pack/mod.rs:1020-1030 region); insert the planning step between them — dedup-resolve the unique-blob sequence (first-occurrence order, unchanged), plan cuts on its lengths, then stream. Only per-blob metadata (~24 B/blob, the existing budget) is resident during planning; whether compressed payloads are re-derived or spilled between the two passes follows the existing `--pack-memory-budget` spill discipline ((unverified) exact spill mechanics — implementation must not regress the "compressed bytes never all resident" invariant, pack/mod.rs:1025-1027).
- stt-build flag `--pack-cuts {next-fit|planned}` (new; default `next-fit` until R1). `order_audit`'s pack-target inference (`max pack length`, order_audit.rs:88-95) still holds under planning (cuts only advance, so the max pack remains ≈ target; floor at 1 MiB unchanged).

**Architecture.** Pure builder-side; readers see only different pack boundaries, which they already treat as opaque coalescing walls (spec §6 step 4). Determinism: cuts are a pure function of `(σ, ℓ, C, workload)` — all inputs deterministic, so content addresses are reproducible. BYTE-CHANGING (every pack address moves) — rides R1. Rollback: `--pack-cuts next-fit` reproduces today's packs byte-for-byte. The `ordering_sim` pack model (`pack_assignment`, ordering_sim.rs:245-259) gains a matching planned-cut variant so `measured` ordering and the cut planner price the same layout — keeping the simulator/writer mirror contractual (ordering_sim.rs:31-38's own precedent).

**Tests.**

- _Unit_ (extend pack/mod.rs inline tests, near the existing measured-ordering test at :2931): `plan_never_overfills` (every planned pack ≤ target except singletons; ≤ C_max always); `plan_keeps_hot_run_whole` (constructed fixture: a coalesced run straddling a next-fit boundary is kept whole by a slightly-early cut, and the workload cost strictly drops); `no_plan_path_is_byte_identical` (next-fit default unchanged — regression pin); `oversized_blob_still_owns_a_pack`.
- _Property:_ DP optimality cross-check — on small seeded instances, exhaustive enumeration over cut vectors matches the DP's objective value; monotonicity — with `λ_obj → ∞` the plan degenerates to exactly next-fit's cut count (guard test that the pinned incumbent is the limit case, i.e. we extended its cost model rather than replacing its rule).
- _Determinism:_ double-build with `--pack-cuts planned` byte-identical, extending `crates/stt-core/tests/reproducible_build.rs` (`v2_dataset_rebuild_is_byte_identical_including_schemas` pattern) and `crates/stt-build/tests/reproducible_pipeline.rs`.
- _Golden-fixture:_ pins change only when the R1 default flips; same one-time regeneration event as WM-4.
- _Integration:_ `verify_packed_objects` (pack/mod.rs:1638) green on planned archives; `stt-optimize order-audit` runs cleanly and its inferred pack target stays sane.

**Evaluation.** Harness: `ordering_sim` blended cost with WM-2 weights, comparing planned vs next-fit cuts at fixed ordering, per fleet archive (extend the WM-3 fleet script with a cut-plan column). Metric: blended workload cost (bytes-equivalent) and pack-object count. Baseline: next-fit cost per archive from the same run (no recorded absolute baseline exists — the audit computes both sides in one pass). Acceptance: strictly non-worse blended cost on every archive (the DP contains next-fit's solution in its feasible set, so regressions indicate a bug), material improvement (>2% blended cost) on at least the datasets whose hot viewport runs straddle boundaries today; object count within +10% of next-fit (λ_obj enforces this). Post-R1 confirmation via the K10 re-capture's pack-request traces (`poopdeck:docs/roadmap/measurements-2026-08.md` §9.3 format).

**Risks & dependencies.** Depends on WM-1/WM-2 (workload), and its default flip rides R1 with WM-4 so the fleet republishes once (P6). Risk: the two-pass finalize interacts with the spill budget — the invariant "only the open pack's handle resident" (pack/mod.rs:1500-1511 region) must survive; if the spill rework is expensive, the fallback is planning on first-occurrence _uncompressed_ lengths (today's simulator approximation) which keeps the DP but reintroduces boundary drift on dedup-heavy datasets — ship the honest version or record the approximation in the manifest. Borders the register hard: the next-fit cut is pinned _for its own cost model_ — the limit-case guard test is the fence; the 64 MiB target constant itself (frozen decision, spec §7 D1) is not re-litigated here (principled `C` is §6.5 gap 2, out of scope).

#### WM-6 — Leaf-descriptor v2: the 8-byte min-bucket-start column `§7.3 · M4 · Phase 4 (R1; reader lands first)`

**Change.** Add descriptor kind 1 to the paged-directory root, carrying the one field §7.3 identifies as eliminating the `b_max` slack: the leaf's minimum _bucket start_.

- `pub const DESCRIPTOR_GEO_BBOX_V2: u8 = 1;` and `pub(crate) const DESCRIPTOR_LEN_V2: usize = 60;` (new, crates/stt-core/src/directory_page.rs beside `DESCRIPTOR_GEO_BBOX = 0` at :57 and `DESCRIPTOR_LEN = 52` at :67). The current 52-byte layout has only a reserved u16 per descriptor (written/checked at directory_page.rs:204, 291-294) — 2 bytes cannot hold an i64, so the kind-tag evolution path the format reserved (u8 kind in the root header, directory_page.rs:63-65) is the mechanism: kind 1 appends `min_bucket_start: i64` = `min over the segment of entry.time_start` (the tile's bucket start — distinct from the existing `t_min = min(cover_t_min ?? time_start)` at directory_page.rs:424, which is the _viewport_ prune bound and can exceed the bucket start; that divergence is exactly why the point-lookup prune needs the `b_max` slack today, §7.3).
- `PageDescriptor` (directory_page.rs:81-104) gains `pub min_bucket_start: Option<i64>`; `encode_root`/`decode_root` (directory_page.rs:247+) branch on kind; `PageDescriptor::overlaps` is unchanged (viewport prune keeps the tight cover bound); a new `pub fn prunes_point_lookup(&self, t: i64, b_max: i64) -> bool` (new) uses `min_bucket_start > t` when present, else the legacy `t_min > t + b_max` slack — so Rust and TS implement one predicate with one fallback.
- TS reader: `ensurePagesForTiles`'s slack path (poopdeck:packages/core/src/archive.ts:2355-2411, slack computed at 2364-2368) reads kind 1 and drops the slack; kind 0 archives behave exactly as today.
- Builder: emit kind 1 under `--descriptor-kind {v1|v2}` (new; default v1 until R1). Because old readers hard-reject unknown structure (decode_root's strict validation — reserved-field rejection verified at directory_page.rs:267-270, 291-294; kind-tag rejection (unverified) but implied by the same strictness), a kind-1 archive **must declare it via the manifest's required-capability mechanism** (the manifest carries required-capability declarations per §7.4) so pre-WM-6 readers fail loudly at open, not weirdly at prune time.
- **Register fence:** Hilbert-key-range descriptors are a standing rejection (§7.3 Setting: lost an A/B simulation; design doc §5). The kind registry doc-comment records the ban next to the tag constants, and a guard test pins that no such kind constant exists in the registry.

**Architecture.** Additive spec-track change to the root page only; leaf codec, entry order, and pack layout untouched. Invariants: soundness is _strengthened_ (the new bound is exact on the lower side, never tighter than correct: `min_bucket_start ≤` every member's bucket start by construction — same outward-conservative discipline as the e7 bbox rounding, directory_page.rs:93-95); descriptors remain O(1)-evaluable fixed-width; `verify_paged_structure` extends to check `min_bucket_start ≤ t_min` per leaf. BYTE-CHANGING for archives that opt in (root frame bytes, hence `.sttd` address, root hash) — default flips at R1; the TS reader support ships **before** any archive emits kind 1 (reader-first, builder-flag second — the §4-batch precedent from the earcut item). Rollback: rebuild with `--descriptor-kind v1`; readers keep both paths indefinitely.

**Tests.**

- _Unit_ (directory_page.rs inline, :745+): kind-1 encode/decode round-trip including the new column; `min_bucket_start_bounds_every_member`; `point_lookup_prune_exact_vs_slack` (a leaf whose `cover_t_min` sits one whole `b_max` above its bucket start: v1 faults the leaf, v2 prunes it — the spurious-fault fixture); kind-0 decode is byte-path-identical to today (regression pin); unknown-kind decode fails loudly.
- _Property:_ for seeded entry sets and every point-lookup `(t, bucket)` in the inventory, v2 pruning never drops a leaf containing a match (zero false negatives) and its fault set is a subset of v1's (monotone improvement).
- _Guard:_ the descriptor-kind registry test above (Hilbert-range ban), plus a `verify_paged_structure` corruption case for an out-of-bound `min_bucket_start` (extending `verify_paged_structure_clean_then_detects_corruption`, directory_page.rs:1003).
- _Determinism:_ kind-1 double-encode byte-identical (trivial but required — extend `crates/stt-core/tests/reproducible_build.rs`).
- _Golden-fixture:_ unchanged until the R1 default flip (same single regeneration event as WM-4/WM-5); `crates/stt-core/tests/spec_conformance.rs` gains the kind-1 conformance vectors.
- _Integration:_ `crates/spatiotemporal-tiles/tests/validate_cli.rs` — validator accepts kind 1, flags kind-1-without-capability-declaration; TS reader conformance against a kind-1 fixture.

**Evaluation.** Metric: leaves fetched per point-lookup whose decoded entries contribute zero matches (the §7.3 measurable), counted over a synthetic point-lookup sweep near dataset start (where the `b_max` slack bites). Harness: NEEDS-HARNESS — a directory-only leaf-fault counter; smallest home is a `--leaf-audit` mode on `stt-optimize inspect` (directory-only inspection is already the cheap path — `inspect --sample 0`), inventoried under the Phase 0 infrastructure item. Baseline: none recorded — the Phase 0 run against the current fleet _establishes_ it; the K10 re-capture provides the end-to-end cold-byte confirmation. Acceptance: zero-match leaf faults from the lower-side slack → 0 on kind-1 archives, with no false negatives (the property suite is the guarantee, the sweep is the demonstration).

**Risks & dependencies.** Depends on WM-4 only for sequencing (both rewrite `.sttd` bytes — land in the same R1 regeneration so golden pins move once); functionally independent of WM-1/WM-2 (its pricing is the leaf-fault count, not the blended workload). Risk: `b_max` derivation on the TS side spans base + temporal-LOD tiers (archive.ts:2364-2368) — the v2 column must be computed over _all_ entries in the leaf including LOD variants, or the exactness claim fails for tier-qualified lookups; the property test's inventory must include LOD entries. Risk: old-reader breakage if any archive ships kind 1 without the capability declaration — the validator check above is the fence. Borders the register at the Hilbert-key-range ban (fenced) and at the second-sketch-dimension idea (variant bitmask, 2-level bbox — §7.3 gap): explicitly _not_ taken here; the kind-tag mechanism leaves the door open without committing bytes.

## 6. Semantic validation & manifest honesty (M7)

This section closes the gap §13.2 names outright: the conformance system is _sound but incomplete_ — a defect that flattened and scrambled 106 AV archives passed `stt-validate` and was caught only by a human comparing `stt-optimize export` bboxes (export.rs:847). M7 makes decoded-content correctness executable (semantic fingerprints), makes the manifest stop lying about what it describes (vertex bounds, `z_range`, the build-assumed coalesce gap), and turns two silent cross-codebase couplings (§4.4's LOD-grid bucket, §4.3's dead `max_compressed_size`) into loud assertions or deletions. Per the phased plan (design §6, Phase 4), SH-1 lands _first_ in Phase 4 and is the acceptance gate for the R1 rebuild; SH-3/SH-4/SH-5 are pack-byte-neutral and ship earlier.

#### SH-1 — Semantic content fingerprints: build-time emission, validate-time recompute `§13.2 · M7 · Phase 4 (lands first; gates R1)`

**Change.**
Three pieces: a fingerprint type in stt-core, emission in stt-build, recompute-and-compare in stt-validate.

_Type_ (new): `stt_core::metadata::ContentFingerprint`, an additive field on `Metadata` (crates/stt-core/src/metadata.rs:218-286) following the `skip_serializing_if = "Option::is_none"` pattern every optional block there already uses (metadata.rs:270-285):

```rust
pub struct ContentFingerprint {                    // (new)
    pub version: u32,                              // 1
    pub bbox: [f64; 4],                            // decoded-VERTEX bbox [min_lon, min_lat, max_lon, max_lat]
    pub z_range: Option<[f64; 2]>,                 // when any vertex/elevation column carries z (pairs with SH-2)
    pub distinct_feature_count: u64,               // matches Metadata::distinct_feature_count (metadata.rs:251)
    pub numeric_ranges: BTreeMap<String, [f64; 2]>,     // per property column, non-null [min, max]
    pub categorical_cardinality: BTreeMap<String, u32>, // capped 10_000, mirroring PropertyStyleHint (metadata.rs:164-167)
    pub coord_tolerance_deg: f64,                  // 0.0 unless coord-quant declared; else the quant step
    pub column_tolerance: BTreeMap<String, f64>,   // attr-quant columns only; absent = exact
}
```

Only **replication-invariant** statistics are included, deliberately: a feature lands in N tiles (pyramid, clipping, temporal LOD), so min/max and distinct-count survive replication unchanged while sums/means do not. Clip-synthesized vertices lie on original segments, so the decoded vertex bbox equals the source vertex bbox at full decode (and is contained by it on any subset).

_Emission_: `input::calculate_bounds` (crates/stt-build/src/input.rs:847) grows into `input::profile_features(features: &[ParsedFeature]) -> Result<FeatureProfile>` (new; one pass, shared with SH-2), computing the fingerprint **from the ingested source features, before tiling and encode**. Wired at the existing call site (crates/spatiotemporal-tiles/src/bin/stt-build.rs:1050) into the metadata built at stt-build.rs:1382. Distinct-count is exact here (the builder holds the feature set). Tolerances are derived from the build's quantization config and are **capability-gated**: `coord_tolerance_deg` must be 0 unless `coord-quant` is in `manifest.capabilities`; `column_tolerance` entries only for `attr-quant` columns — the validator errors on a tolerance without its capability, so a writer cannot declare a wide tolerance to launder corruption.

_Recompute_ (new module): `crates/spatiotemporal-tiles/src/bin/stt-validate/fingerprint.rs` beside `schema.rs`/`summary.rs`/`temporal.rs` (main.rs:46-48):

```rust
pub struct FingerprintAccumulator { /* bbox, z, per-column min/max, cardinality sets (capped), id HLL */ }
impl FingerprintAccumulator {
    pub fn ingest(&mut self, layers: &[DecodedLayer]);
    pub fn finish(self) -> ObservedFingerprint;
}
pub fn check_fingerprint(declared: &ContentFingerprint, observed: &ObservedFingerprint,
                         decode_complete: bool) -> Vec<String>;   // (new)
```

Hooked into the existing decode loop in `validate_entries` (main.rs:384-636, alongside `check_tile_schema` at main.rs:491) and compared in `finalize_feature_check` (main.rs:661). Semantics by decode mode (reusing the `--sample` machinery, main.rs:362-375):

- **Sampled decode** — _containment_: every observed vertex/value must lie inside the declared bbox/ranges (within tolerance), observed distinct-count ≤ declared, observed cardinality ≤ declared. This is what catches the scrambled-coords class even at `--sample 300`: the AV defect read metric z values as longitudes, which land far outside any real bbox.
- **Full decode** — containment _plus_ equality within tolerance on bbox edges and per-column min/max, and distinct-count agreement within the sketch's stated error (~2 % at 2^14 registers; registers hashed with blake3, already a dependency, so bounded memory and cross-platform determinism even at 44 M ids).

Distinct-id counting uses a fixed-size HLL sketch, never an id set — bounded client/validator memory is spine (P5), and the national radar archive holds 44 M points.

_The relapse-proof rule_: any losslessly-transforming tool (the reoptimize successor, repack, reorder) **carries the fingerprint through verbatim and never recomputes it** — recomputing from its own output is exactly how the 106-archive defect would have self-certified. Two new flags support transform acceptance directly: `--emit-fingerprint <PATH>` (write observed) and `--expect-fingerprint <PATH>` (compare against an external fingerprint file); both trip the doc gate `cli_flags_are_documented_in_cli_reference` (main.rs:828) until documented. Report gains `fingerprint_checked: bool` next to the existing fields (main.rs:117-148).

_Storage decision — manifest field, not sidecar_: the manifest is the dataset's single mutable object (pack/mod.rs:344-348), the fingerprint is ~1-2 KB, it rides into `.sttb` bundles for free, and the precedent for folding small contracts into the manifest rather than minting an object class is already set by `schemas` (pack/mod.rs:364-373). A sidecar adds a 404 mode and escapes bundle validation.

_Fleet-wide run_: `scripts/validate-fleet.sh` (**never landed** — SH-1's validator shipped as `crates/spatiotemporal-tiles/src/bin/stt-validate/fingerprint.rs`, but this driver did not, so the fleet-wide `K2 → 0` acceptance in P0-8's O4 row has no instrument) walks every `*/manifest.json` under the r2-sync staging tree (`DATA_DIR=data-fleet`, scripts/r2-sync.sh:74 — the tree the 68 registered manifest URLs in `poopdeck:examples/showcase/src/datasets.ts` are published from), runs `stt-validate <dir> --json --sample 300` per archive, aggregates one JSONL report (errors, warnings-by-class so the K2 → 0 acceptance in O4 is measurable), and exits non-zero on any error. A per-archive waiver list carries exactly one entry: `wildfires`, whose 163 real drift errors are frozen because the upstream data is gone (backlog K9 — regenerating would destroy it). Content addressing makes the local run authoritative for the published fleet: identical bytes carry identical blake3 names, so green-local + the standing B2-style remote probe certifies the CDN. For the **R1 gate** the script's `--full <stem>` mode runs full-decode fingerprint equality on every archive R1 rebuilt, inserted into the standing deploy order as _packs → validate-fleet → frontend → manifests_ (shipping.md's 15-second-exposure procedure).

**Architecture.** The validator side is read-only and BYTE-NEUTRAL — ships at the top of Phase 4 (or earlier) and simply warns when `content_fingerprint` is absent (every pre-R1 archive), mirroring the CRS84-warning precedent (main.rs:145-147). The builder side adds one manifest key: **pack-byte-neutral, manifest-shape-changing** — no pack re-addressing, but newly built manifests differ, so the golden manifest pins regenerate once via the documented `regenerate_v2_golden` path (conformance.md:132) inside the R1 review. Invariants preserved: determinism (min/max + blake3-HLL are pure functions of the feature set; no RNG, no iteration-order dependence — `properties`-style BTreeMaps throughout, metadata.rs:254-257); no-thinning untouched (measurement only); random access untouched; bounded memory (sketch + running min/max only). The fingerprint is computed pre-tiling, so the tiler and encoder — including the do-not-touch TEMPLATE/TILE_META partition and drift classifier (§5) — are untouched. Rollback: revert the builder emission (old manifests never had the key; the validator's absent-key path is the permanent legacy path), revert the validator module independently.

**Tests.**

- _Unit_ (new `fingerprint.rs` tests + metadata.rs tests extended in the style of `temporal_lod_field_omitted_when_unset`, metadata.rs:911): serde round-trip; key omitted when `None` (legacy manifests byte-identical); accumulator min/max merge across layers; containment-vs-equality mode selection; tolerance-without-capability rejected.
- _Property_ (extend crates/stt-core/tests/adversarial_decode.rs — exists, in git status): fingerprint check never panics on arbitrary decoded layers; for any feature subset, observed ⊆ full-set fingerprint (the containment soundness property).
- _Golden-fixture_: v2-golden manifests gain the key — **byte pins change once**, intentionally, reviewed, batched with R1 (crates/stt-core/tests/v2_golden.rs). The decode-and-assert-values pass gains a fingerprint assertion.
- _Integration_ (extend crates/spatiotemporal-tiles/tests/validate_cli.rs — exists; follows the one-negative-per-named-check convention, validate_cli.rs:13-18): `fingerprint_catches_scrambled_coordinates` (encode the same features with x/y stride-swapped — the reoptimize defect's shape — under an honest declared fingerprint; must exit non-zero naming the bbox violation); `fingerprint_catches_scaled_property_column` (numeric range mismatch); `sampled_fingerprint_still_catches_out_of_bbox` (under `--sample 2`); positive full + sampled pass; `expect_fingerprint_accepts_identical_and_rejects_mutated` for the transform-acceptance flags. The scrambled-coords case is the acceptance test for the whole item — it is the recorded regression class.
- _Determinism_: extend crates/stt-core/tests/reproducible_build.rs (exists; `same_tile_encodes_byte_identically` per §13.1): two builds of the same input emit byte-identical `content_fingerprint` JSON.

**Evaluation.** Harness: `scripts/validate-fleet.sh` (new — this item builds it; register it in the Phase 0 harness inventory alongside `stt-optimize inspect/diff`). Metric: injected-defect detection rate (scramble / flatten / column-scale classes) and fleet false-positive count. Baseline: **0/106** — the recorded detection rate of structural validation on the AV corruption. Acceptance: 3/3 defect classes detected at both `--sample 300` and full decode; fleet run green (68 registered archives, wildfires waived per K9) with zero fingerprint false positives; runtime within the 2026-07-29 precedent (64 archives at `--sample 300` in one session). R1 gate: full-decode equality green on every rebuilt archive before the manifest flip.

**Risks & dependencies.** Depends on nothing; SH-2 and SH-6 depend on it. Tolerance derivation must track every Θ₁ lever that moves decoded values (coord-quant, attr-quant; per-vertex `vq` values are explicitly out of fingerprint v1 scope — state it in the field docs) or honest quantized archives fail; the capability-gating rule keeps that auditable. Sampled containment cannot catch a defect that keeps values inside declared ranges (e.g. a within-bbox permutation) — full-decode equality plus the verbatim-carry-through rule are the backstops; do not oversell `--sample` runs (the report already refuses to let sampled runs masquerade as full, main.rs:129-131). Borders the do-not-touch register at "sample-dependent type decisions" (§5): fingerprint statistics are _domain_ statistics, computed dataset-globally — the same invariance rule the drift classifier enforces (conformance.md:201-211); never let a per-tile shortcut back in. HLL introduces an approximation into an otherwise-exact validator — its error bound must be printed with any distinct-count finding.

#### SH-2 — Honest manifest bounds (vertex bbox) + additive `z_range` `K11 · §8.1 (secondary note) · M7 · Phase 4, rides R1`

**Change.** `input::calculate_bounds` (crates/stt-build/src/input.rs:847-909) currently takes min/max of `ParsedFeature.lon/.lat` — the **centroid** (input.rs:58-59) — while the tiler addresses tiles by vertex (`feature.geojson.geometry`, crates/stt-build/src/tiler.rs:1277). Rework it (inside SH-1's `profile_features` pass) to walk every coordinate position of `f.geojson.geometry` (the `geojson` crate's positions are `Vec<f64>`, so `position[2]`, when present, is elevation), falling back to the centroid when geometry is absent, and preserving the null-island sentinel policy verbatim: skip a feature's contribution when its parsed anchor is exactly `(0.0, 0.0)` (input.rs:877-887), with the existing all-sentinel degenerate fallback (input.rs:889-898). Return grows to include `z_range: Option<[f64; 2]>`.

Manifest side: `Metadata.z_range: Option<[f64; 2]>` (new), additive with `#[serde(default, skip_serializing_if = "Option::is_none")]` — exactly K11's accept criterion so existing manifests round-trip byte-identically. Populated from geometry z when any vertex carries one; for datasets whose altitude lives in a property column, a new opt-in `stt-build --elevation-column <NAME>` (the flag K11 notes does not exist) folds that column's min/max in — metadata only, it does not touch geometry (respecting the standing preference that render-side depth comes from `elevationProperty` over a column). Downstream surfacing: `stac_item` (crates/stt-build/src/stac.rs:71-88) emits the RFC 7946 six-element bbox `[w, s, z_min, e, n, z_max]` when `z_range` is present (STAC permits 2n-element bboxes); `to_tilejson` bounds stay four-element per the TileJSON spec (metadata.rs:498-503). manifest.schema.json pins the `z_range` shape inside `metadata` the same way `style_hints` is pinned (manifest.schema.json:214-296), and the TS `PackedManifest` type follows.

Validator side (small, rides on SH-1's accumulator): a **bounds-containment check** — `metadata.bounds` must contain the decoded vertex bbox, and declared `z_range` must contain observed z. Severity policy mirrors the CRS84 precedent: _error_ when `content_fingerprint` is present (a new-writer archive has no excuse), _warning_ on legacy archives (every centroid-bounds archive in the fleet would otherwise light up as red noise pre-R1).

**Architecture.** Pack-byte-neutral, **manifest-value-changing**: rebuilt archives get different `bounds` numbers (and possibly a new key), so this is BYTE-CHANGING at the golden-pin/manifest level and rides R1 — K11 explicitly records "ride the next rebuild rather than a dedicated one," and R1 is that rebuild. Bounds are computed from **source features, pre-clip**: the tile-relative clip buffer (§4 batch item) duplicates features into neighbours but only along original segments, so vertex bbox is the right honest quantity and is stable against the buffer change landing in the same window. Blast radius is the K11 list: the showcase opening camera, `stt-validate`'s printed range, and MCP `describe_dataset` all start telling the truth with zero code changes of their own. Volumetric discoverability unblocks altitude-aware selection (§8.1's secondary note) but this item only lands the field. Antimeridian-spanning datasets degrade exactly as today (full-width lon bbox) — no worse, and the recorded antimeridian gotchas are untouched. Rollback: revert the builder; already-republished manifests keep honest bounds harmlessly (honest ⊇ centroid); the validator check demotes to warning-only by construction on any archive without a fingerprint.

**Tests.**

- _Unit_ (input.rs bounds tests, extended/new cases): a LineString whose vertices extend past both endpoints' centroid — vertex bbox strictly wider than the old result; polygon ring case; z picked up from 3-element positions; `--elevation-column` min/max fold; sentinel skip and all-sentinel fallback preserved (behavioral pin of input.rs:877-898); empty-input default unchanged (input.rs:848-853).
- _Unit_ (metadata.rs, mirroring metadata.rs:911): `z_range` round-trip; key omitted when unset — the K11 byte-identity criterion as an explicit assertion; unknown-field forward-compat already covered by `test_metadata_ignores_unknown_fields` (metadata.rs:724).
- _Unit_ (stac.rs tests): six-element bbox iff `z_range` present; four-element otherwise.
- _Golden-fixture_: v2-golden manifests change (bounds values) — same single reviewed R1 regeneration as SH-1; **byte pins change**.
- _Integration_ (validate_cli.rs): negative — archive whose metadata declares a bbox smaller than its features (PackWriter takes metadata from the caller, the same trust seam `write_dataset_declaring` already exploits, validate_cli.rs:76) with a fingerprint present → error; same without fingerprint → warning only.
- _Determinism_: covered by the reproducible_build.rs extension in SH-1 (min/max is order-independent).

**Evaluation.** Harness: `scripts/validate-fleet.sh` (SH-1). Metric: count of fleet archives where decoded vertex bbox ⊄ declared bounds. Baseline: unknown but provably non-zero on every line/polygon/multi-point archive (K11's statement); the tile-loading campaign measured shipped 3D demos missing 20-44 % of on-screen tiles with `metadata.bounds` as a contributing understatement. Acceptance: containment green fleet-wide post-R1; showcase opening cameras frame correctly on the previously-understated demos — verified in-browser by the user per the standing visual-verification preference, not by screenshot loops.

**Risks & dependencies.** Depends on SH-1 (shared ingest pass, containment check host) and rides R1 (P6 — never dribble a manifest-value change across the fleet). The sentinel policy is load-bearing: dropping it re-opens the whole-globe-bbox failure the current code documents. Do not "fix" bounds post-clip or post-tiling — that bakes the clip buffer into a data claim. Borders no do-not-touch entry directly; the summary-tier and LOD tiers add no new vertices, so tiers need no special-casing.

#### SH-3 — Coalesce-gap co-versioning beside `blobOrdering` `§6.2 · M7 · Phase 1 (unblocks M5's adaptive gap)`

**Change.** §6.2's recorded gap (4): the build-time `measured` ordering bakes the reader's 2 MiB coalesce gap into content-addressed layouts forever, and a reader-side gap change silently invalidates fleet layouts — D6 in miniature. Add to `Manifest` (crates/stt-core/src/pack/mod.rs:350-397), directly beside `blob_ordering` (pack/mod.rs:385-390):

```rust
/// The range-coalescing gap (bytes) the writer's layout cost model assumed
/// (ordering sim + pack cut). Informational co-versioning, NOT a reader
/// directive: order-audit compares it against the reader's active gap to
/// flag layout/reader drift. Emitted iff `blobOrdering` is (they co-version);
/// absent = legacy build, assume 2 MiB.
#[serde(default, rename = "coalesceGapBytes", skip_serializing_if = "Option::is_none")]
pub coalesce_gap_bytes: Option<u64>,               // (new)
```

Set at `PackWriter::finalize` where `blob_ordering` is set (pack/mod.rs:1311), from the simulator's constant the writer already shares: `stt_core::ordering_sim::DEFAULT_COALESCE_GAP_BYTES = 2 * 1024 * 1024` (ordering_sim.rs:29, documented as matching the TS reader's `DEFAULT_RANGE_COALESCE_GAP`, archive.ts per ordering_sim.rs:26-28), or the `SimOptions.coalesce_gap_bytes` actually used when a sweep overrode it (ordering_sim.rs:70-77). Consumers: `stt_optimize::order_audit` (crates/stt-optimize/src/order_audit.rs:64) gains a drift finding when its `SimOptions` gap differs from the manifest's; the TS `PackedManifest` type gains the optional field; M5's Phase 1 adaptive gap (`G ← L̂·θ̂`) reads it to detect and report when the session gap has drifted from the layout's assumption on `measured`-ordered archives. manifest.schema.json gains the optional integer property next to `blobOrdering` (manifest.schema.json:91-94), and the three-way contract pin (Rust writer / TS reader / golden manifest, conformance.md:45-50) extends.

**Architecture.** Pure manifest metadata: pack-byte-neutral, additive, reader-ignorable — BYTE-NEUTRAL in every sense that matters for deploys, so it **ships in Phase 1**, which is required ordering: the design doc's Phase 1 sequence lists the adaptive coalesce gap as "needs M7's manifest co-versioning". The only churn is that newly written manifests carry one more key, so the v2-golden manifest pin regenerates once (reviewed; can batch with any Phase 1/2 writer touch or wait for R1 — the field is `Option`, so nothing forces it early on fixtures). The field is _informational_: the reader's fuse rule and its cost model are pinned "extend, never replace" in the do-not-touch register (§5) and this item records an assumption, changing no fuse behavior. Determinism: the value is a pure function of build config. Rollback: stop emitting; absent-key semantics are the permanent legacy path (mirroring `blob_ordering`'s own omission contract, pack/mod.rs:2751-2752).

**Tests.**

- _Unit_ (pack/mod.rs tests, extending the existing blobOrdering block at pack/mod.rs:2717-2943): key omitted when unset so pre-field builds stay byte-identical (mirror of the `!s.contains("\"blobOrdering\"")` assertion at pack/mod.rs:2752); present with the correct value after a finalize that resolved an ordering; round-trips through `Manifest::from_json_bytes`.
- _Unit_ (order_audit tests): audit flags a manifest gap ≠ sim gap; silent on match; silent-with-note on absent (legacy).
- _Golden-fixture_: pin change deferred or batched as above; state explicitly in the PR which.
- _Integration_ (TS): `poopdeck:packages/core/test/manifest-schema.test.ts` (the three-way pin, conformance.md:46) — schema accepts the field, negative case for a non-integer value, unknown-field tolerance already covered.
- _Determinism_: manifest serialization already deterministic (BTreeMap discipline); covered by existing reproducible-build assertions.

**Evaluation.** Harness: `stt-optimize order-audit` (exists). Metric: fraction of fleet manifests carrying the field (68/68 post-R1; grows from 0 as archives rebuild) and order-audit drift findings = 0 while the reader gap remains 2 MiB. Baseline: the field exists nowhere; the recorded risk is un-priced (an order of magnitude of optimal-gap variation across sessions, §6.2). Acceptance: M5's adaptive-gap item lands with the drift check wired, and any future reader-default change is flagged by audit instead of silently degrading `measured` fleet layouts.

**Risks & dependencies.** No dependencies; M5 (§6.2 adaptive gap, Phase 1) depends on it. The one design risk is scope creep toward a reader _directive_ — resist it: the reader's gap selection is M5's problem, and §5 pins the fuse rule itself. If per-archive `ArchiveOptions.coalesceGapBytes` overrides (archive.ts per §6.2) are in play, audit compares against the _effective_ reader gap, which is a reader-side concern documented in the field's description, not schema-enforced.

#### SH-4 — Build-time assertion: LOD-grid bucket ≡ `--temporal-bucket` `§4.4 · M7 · Phase 1 (byte-neutral, ships anytime)`

**Change.** §4.4's load-bearing constraint lives as two constants in two codebases with nothing asserting them equal: `LOD_BUCKET_MS = 300_000` in the generation script (scripts/data-generation/mrms_volume.py:226, fed to `lod_min_zoom` at mrms_volume.py:229/402) must equal the archive's `--temporal-bucket` (parsed at crates/spatiotemporal-tiles/src/bin/stt-build.rs:1058) — the space-only-grid incident (median 13 %, worst 0 % of the visible bucket at z8) is what happens when the temporal key decouples. Meeting point: the Parquet footer, which stt-build already reads (`key_value_metadata`, crates/stt-build/src/input.rs:187/675 for the GeoParquet `geo` key).

- _Script side_: `mrms_volume.py` and `mrms_refloor.py` stamp `stt:lod_grid_bucket_ms = "<LOD_BUCKET_MS>"` into the Parquet key-value footer when writing the `lod_min_zoom` column. (`av_common.py`'s home-zoom ladder is out of scope — its voxel grid is per-sweep spatial, not bucket-keyed.)
- _Builder side_: `input::lod_grid_bucket_ms(kv: Option<&Vec<KeyValue>>) -> Option<u64>` (new), read beside `parse_geo_metadata`; surfaced from the GeoParquet reader to the build driver. At the point where both `args.min_zoom_field` (stt-build.rs:375, plumbed at stt-build.rs:1143) and the **resolved** `temporal_bucket_ms` are known: if the flag is set and the key is present and the values differ → hard error naming both values and both sources ("input baked lod_min_zoom for a 300000 ms grid; this build buckets at 3600000 ms — the LOD floors are wrong for this archive; re-run the generation script or change --temporal-bucket"). Key present, flag unset → one warning. Flag set, key absent → one warning (legacy input; nothing asserts). The assertion must run **after** the `--auto` recommend path may overwrite the bucket (stt-build.rs:1687-1691) — that auto-override is precisely the silent-mismatch path a hand-passed flag never hits.

**Architecture.** Pure precondition: BYTE-NEUTRAL (no output byte can change; a passing build is bit-identical to today's), ships anytime, no rebuild window. It converts §4.4's "script-discipline" guarantee into an archive-native gate at the only choke point both codebases share, without porting the ℓ-assignment into the builder (that port is §4.4's own PLANNED item, not M7's). Non-Parquet inputs (Postgres/DuckDB) have no LOD-column pipeline today; the warning path covers them. Rollback: delete the check; determinism untouched.

**Tests.**

- _Unit_ (input.rs tests): footer key parsed; absent → `None`; malformed value → warning-not-panic.
- _Integration_ (new `crates/stt-build/tests/lod_bucket_assert.rs`, beside the existing `crates/stt-build/tests/summary_tier_archive.rs`): write a tiny Parquet with the key at 300 000 ms; build with `--min-zoom-field lod_min_zoom --temporal-bucket 5m` → passes; `--temporal-bucket 1h` → fails naming both values; key absent + flag set → warns and passes; key present + flag unset → warns and passes.
- _Guard test_ (rejected-design boundary, cheap): the assertion compares against the _resolved_ bucket — unit-test the check function with an auto-overridden value to pin that ordering.
- _Determinism_: byte-identical archive on the passing path (extend the byte-identical acceptance pattern the 2026-07-28 ingest refactor used, per §13.1: `diff -r` two builds).
- Script side is not CI-executed; the builder-side assert is the gate — note this in the script docstring.

**Evaluation.** Harness: the storm rebuild recipe (the 9.5 h national build is the yardstick; the assertion adds ~0 cost). Metric: mismatch class is build-impossible — measured as the negative test plus one deliberate mismatched dry run. Baseline: today a mismatch builds silently and ships a wrong-density pyramid (the recorded 13 %/0 % coverage incident). Acceptance: `storm-3d-conus` / storm-4d regeneration passes with the key stamped; a deliberately mismatched invocation exits non-zero.

**Risks & dependencies.** None; independent of every other SH item. Borders the do-not-touch entry "bucket-keyed LOD grids (§4.4) — never revert to space-only" (§5): this item _enforces_ that pin rather than modifying it. Risk: a future generation script that forgets the stamp degrades to today's warn-only behavior — acceptable (never worse than the status quo), and the fleet driver (SH-1) can count the warning class.

#### SH-5 — Delete the dead `max_compressed_size` `§4.3 · M7 · Phase 1 (byte-neutral)`

**Change.** `TileBudget.max_compressed_size` (crates/stt-core/src/budget.rs:34, default 128 KiB at budget.rs:45) is stored and never enforced — only `max_uncompressed_size` binds in `enforce_indexed` (budget.rs:107, 125-126) — and `build_tile_budget` papers over it by passing the same `max_bytes` twice (crates/stt-build/src/build_options.rs:318: `TileBudget::new(max_bytes, max_bytes, max_features)`). §4.3's gap says enforce or delete. **Delete**, with this justification: enforcing a compressed cap requires a drop→encode→zstd→check fixpoint per tile, which the incumbent's index-based, never-materialize design deliberately avoids (§4.3 Setting), and it is one of the five D4 sites whose _correct_ form — a measured post-zstd byte cap — is M1's trial-encode objective feeding M3's `--target-size`. Keeping a dead field until then is a standing spec/behavior lie; deleting closes it today and leaves the field name free for the measured version.

Concretely: remove the field from `TileBudget` and `Default`; `TileBudget::new(max_uncompressed_size: usize, max_feature_count: usize)` (signature change, budget.rs:53-63); update `build_tile_budget` (build_options.rs:303-319); leave the CLI surface untouched — `--maximum-tile-bytes` already documents itself as an UNCOMPRESSED cap (stt-build.rs:533-539), so after deletion the docs, flags, and behavior finally agree. Record the decision in the roadmap so re-proposal cites §4.3 and lands as M1's measured cap, not a resurrection.

**Architecture.** BYTE-NEUTRAL by construction: the field never influenced any output (the enforce path reads only `max_uncompressed_size` and `max_feature_count`; the under-budget fast path returns byte-identical output, budget.rs:104-109), and the budget itself is default-off (`None` unless `--maximum-tile-bytes`/`--maximum-tile-features` is passed, build_options.rs:299-319) per the no-thinning partition (Θ∞ excluded from every automated path, §13.3). Compile-time safety: removal makes any unnoticed reader a build error — grep confirms the only sites are construction and the struct itself. API note: `stt-core` is published, so the signature change is a semver-minor 0.x break riding the 0.6.0 cut (B3) or immediately after — it must not force an off-cycle publish (the crates.io HTTP/2 publish constraint stands). Rollback: trivially re-add the field.

**Tests.**

- _Unit_ (budget.rs tests extended): snapshot the keep-set of `enforce_indexed` on a fixed over-budget fixture before/after the deletion — identical selection proves behavior neutrality; under-budget fast path still returns the full index set in original order (the no-op guarantee, §4.3 constraint 3).
- _Unit_ (build_options.rs tests): `build_tile_budget(None, None, _)` still `None`; the existing assertion at build_options.rs:501 updates to the new constructor.
- _Determinism_: `enforce_indexed` re-run on identical input yields the identical keep-set (order-stability constraint, §4.3 constraint 4) — cheap, add it while in the file.
- _Guard_: none needed beyond compilation + the roadmap note; the boundary ("compressed caps are measured, per M1, or nothing") is documentation, not code.
- _Golden-fixture_: no pins change (budget default-off; field never serialized to any archive).

**Evaluation.** No harness needed — the acceptance is the neutrality snapshot plus a green `cargo test -p stt-core` / `-p stt-build`. Metric: zero behavioral diff; the silent spec/behavior gap named in §4.3's Gap line is closed. Baseline: the gap exists (field set, never read).

**Risks & dependencies.** Independent; only coordination point is the B3 publish vehicle for the API change. Borders the no-thinning doctrine (§13.3) — untouched: the budget stays opt-in, its scorer and greedy stay exactly as pinned, and nothing here changes what `--auto` may touch. Do not slide into "improving" the greedy while in the file — the joint-knapsack upgrade is §4.3's own solver item, out of M7's scope.

#### SH-6 — Conformance rule additions and contract pins `§13.2 · M7 · Phase 4 (text lands with SH-1/SH-2; SH-3's line may land earlier)`

**Change.** Make SH-1/SH-2/SH-3 normative in docs/spec/conformance.md, matching the file's structure: the validator's numbered check list lives in the `stt-validate` module doc (checks 1-11, main.rs:11-34) and is mirrored prose-style in conformance.md §2.3 (conformance.md:149-227); writer obligations are MUST/SHOULD bullets in §3 (conformance.md:275-380). Additions:

- _Validator checks (module doc + §2.3)_: **check 12 — semantic fingerprint** ("when `metadata.content_fingerprint` is present, the decoded content's vertex bbox, z range, distinct-feature count, per-column ranges and cardinalities are recomputed and compared — containment under `--sample`, equality within declared tolerances under full decode; declared tolerances are rejected unless their matching capability (`coord-quant`, `attr-quant`) is declared"); **check 13 — bounds containment** ("`metadata.bounds` (and `z_range` when declared) must contain the decoded vertex extent; errors when a fingerprint is present, warns on legacy archives"). Extend the §2.3 closing sentence (conformance.md:224-227) so "passes `stt-validate`" now also certifies the semantic invariants.
- _Writer MUSTs (§3)_: "compute `metadata.bounds` from geometry **vertices** — the quantity tiles are addressed by — never from centroids; the declared bbox MUST contain every vertex the archive decodes to."
- _Writer SHOULDs (§3)_: "emit `metadata.content_fingerprint` (version 1) computed from the source features before tiling" (SHOULD, not MUST — a third-party writer without it stays conformant and the validator degrades to warning, mirroring the CRS84 precedent at conformance.md:174-175); "emit the additive `metadata.z_range` when any vertex or a declared elevation column carries altitude"; "a writer recording `blobOrdering` SHOULD record `coalesceGapBytes`, the coalescing gap its layout cost model assumed."
- _Transform rule (§3, MUST)_: "a tool that transforms an archive losslessly (reorder, repack, re-optimize) MUST carry `content_fingerprint` through verbatim and MUST NOT recompute it from its own output" — the sentence that makes the 106-archive class structurally unrepeatable.
- _Reader requirements (§4)_: no additions — unknown-metadata tolerance already covers all three fields (conformance.md:394).
- _Schema_: manifest.schema.json pins `coalesceGapBytes` (top level, beside `blobOrdering`) and the `z_range` / `content_fingerprint` shapes inside `metadata` in the style of the existing `style_hints` pin (manifest.schema.json:214-296); the three-way CI pin (Rust `Manifest` / TS `PackedManifest` / golden manifest, conformance.md:45-50) extends to all three.

**Architecture.** Documentation plus contract tests; BYTE-NEUTRAL. The load-bearing property is the repo's existing doc-lock discipline, which this item extends rather than parallels: every new validator flag is forced into docs/api/cli-reference.md by `cli_flags_are_documented_in_cli_reference` (main.rs:827-857), every new named check gets its negative test per the validate_cli.rs convention ("one deliberately broken archive per named check in docs/spec/conformance.md", validate_cli.rs:13-18), and every schema change trips the three-way pin. Rule text must state severities exactly as implemented (error-with-fingerprint / warn-without), because §2.3's list is read as the validator's contract (the codebase already re-asserts one §2.3 invariant directly for that reason, main.rs:299-306). Rollback: revert with the code it documents.

**Tests.**

- _Contract_: `poopdeck:packages/core/test/manifest-schema.test.ts` — positive validation of a manifest carrying all three new fields; negative for a malformed `content_fingerprint` (wrong bbox arity) and non-integer `coalesceGapBytes`; unknown-fields-still-validate unchanged.
- _Doc gates_ (all existing): `cli_flags_are_documented_in_cli_reference` (main.rs:828) for `--emit-fingerprint`/`--expect-fingerprint`; `cargo test -p spatiotemporal-tiles --test cli_reference_doc` (conformance.md:468); the capability-registry pin is untouched (no new capability — fingerprints re-type nothing, they are additive metadata, which §3 explicitly exempts, conformance.md:287-288).
- _Convention completeness_: audit that checks 12 and 13 each have their validate_cli.rs negative (delivered by SH-1/SH-2) — add a comment cross-referencing the rule number from each test, the file's existing style.
- _Golden-fixture_: none beyond those SH-1/SH-2/SH-3 already own.

**Evaluation.** No harness. Acceptance: conformance.md, manifest.schema.json, the Rust and TS types, and the validator's module-doc numbering agree (all pins green); a fresh reader of §2.3 can name the exact severity of every new finding; the transform-carry-through MUST appears in §3 and is cited from the fingerprint field's rustdoc.

**Risks & dependencies.** Depends on SH-1, SH-2, SH-3 (documents them); SH-3's schema/doc line can land in Phase 1 with its code, the rest with Phase 4. Risk: rule text drifting ahead of shipped behavior — mitigate by landing each rule in the same PR as its check, never batched separately. Borders the meta-problem §13.2(b) warns about: every rule added here must stay _executable from the archive alone_ (constraint (ii)) — the fingerprint rules qualify precisely because the fingerprint travels in the manifest; resist any future rule that needs the source data to check.

## 7. The two-pass build & the R1 builder batch (M2, §4 items)

This section covers mechanism M2 — the dataset-global statistics pass that turns five recorded per-tile-greed defects (D5) into one investment — plus every smaller builder item the design doc batches into rebuild window R1 (§4), and the golden-pin update protocol that makes the window's byte break deliberate and reviewed. Everything here that changes archive bytes rides R1 under P6 (one fleet republish, ~29.3 GiB / 1,324 objects); the two explicitly byte-neutral items (TB-12's reader half, TB-13) may ship earlier. Ground truth on current behavior is the problems doc §§1.2–1.4, 2.3–2.5, 3.3–3.8; the requirements are the M2 entry and §4 of the design doc.

---

#### TB-1 — Pass-1 dataset statistics collection (the two-pass architecture) `§3.3 §3.4 §3.6 §3.8 · M2 · Phase 4 / R1`

**Change.** A new module `crates/stt-build/src/dataset_stats.rs` (_shipped 2026-08_) providing the single pass-1 scan that every dataset-global pin consumes. Today's build already runs one whole-dataset property scan — `resolve_property_types` → `fill_property_type_gaps` (crates/spatiotemporal-tiles/src/bin/stt-build.rs:902-924, crates/stt-build/src/columnar.rs:632-648) — immediately after `source.load(...)` materializes every feature in memory (stt-build.rs:1032-1041). TB-1 widens that existing scan into one combined pass rather than adding a parallel one:

- `pub fn collect_dataset_stats(features: &[ParsedFeature], filter: &AttributeFilter, types: &PropertyTypes) -> DatasetStats` (new), with
  - `NumericColStats { min: f64, max: f64, max_abs: f64, all_integer: bool, finite: u64, nulls: u64 }` (new) per numeric key — exactly the statistics `numeric_column_stats` computes per tile today (consumed at quantize.rs:463), widened to the dataset;
  - `CategoricalColStats { categories: Option<Vec<String>>, distinct_overflow: bool, total_value_bytes: u64, values: u64 }` (new) per categorical key — first-seen category order preserved (the §5-register order rule, now global), set capped at 65,536 distinct + a per-column byte cap (default 64 MiB); overflow drops the set and marks the column Utf8-forced;
  - per-zoom estimated byte mass `b̂(z)` from the existing `tile_feature_size` arithmetic (tiler.rs:1586-1589) — consumed by TB-9;
  - the per-cell-union timestamp quantile set consumed by TB-10.
- `DatasetStats::to_pins(&self) -> GlobalColumnPins` (new) producing the encoder-side pin struct (defined in TB-2), handed to the encoder via a new `EncoderConfig` field `pub global_pins: Option<Arc<GlobalColumnPins>>` (new; crates/stt-core/src/arrow_tile/config.rs:159-224 is the struct to extend — `EncoderConfig` already travels explicitly to every encode, per its own doc at config.rs:141-157).
- CLI: `--single-pass` (new, escape hatch) restores the current one-pass behavior for byte-compat debugging; the two-pass path is the R1 default.
- Manifest: the resolved pins serialize (canonical JSON, sorted keys) into an additive manifest field `encoderPins` (new) — the M7 co-versioning pattern, and the artifact `stt-serve` needs for parity (below).

**Architecture.** Pass 1 sits between load and `generate_tiles*` — no new I/O, one extra iteration over the resident `Vec<ParsedFeature>`. Memory: O(columns + capped category sets), never O(rows) — the scan borrows `&str` values (props.rs `PropValue::as_str`) and allocates only on first-seen category insert, honoring the columnar-ingest lesson (a per-row `serde_json::Map` cost ~830 B even for one key). Determinism: the scan is a parallel fold over ordered feature chunks with an associative merge that preserves left-chunk-first first-seen order — result independent of `--workers`. Streaming/DB inputs need no second source scan today because `source.load` fully materializes for all three backends (verified stt-build.rs:1032); the forward contract for a future true-streaming input (planned SedonaDB backend) is recorded in the module doc: either a second scan under a stable `ORDER BY` (required for first-seen order) or a bounded property-stream spill. `stt-serve` parity: the build↔serve byte-identity contract (build_options.rs:138-145) means `stt-serve` must consume identical pins — computed once per dataset at startup via one SQL aggregate (min/max/distinct per column) or loaded from `encoderPins`. Byte-break status: TB-1 alone is BYTE-NEUTRAL (it only computes); its consumers flip bytes. Rollback: `--single-pass`.

**Tests.**

- _Unit:_ stats over a hand-built feature set match expected min/max/all_integer/first-seen order; category cap overflow → `distinct_overflow` + dropped set; `AttributeFilter` exclusion keeps a column out of stats (extend the in-file tests of crates/stt-build/src/columnar.rs, which already cover the filter).
- _Property:_ stats invariant under feature-chunk boundaries (merge associativity), via proptest in the new module.
- _Determinism:_ `collect_dataset_stats` twice on the same input, and at `--workers 1` vs `--workers 8` → identical struct (the mandatory byte-identical-re-run test, at the stats level).
- _Integration:_ extend crates/stt-build/tests/reproducible_pipeline.rs (`full_pipeline_is_byte_reproducible`, :51) to run with pins enabled — two full builds byte-identical; extend crates/stt-build/tests/single_tile_conformance.rs (`single_tile_encoding_matches_offline_build`, :28) to pass the same pins to the single-tile path.
- _Golden:_ none changed by TB-1 itself.

**Evaluation.** No end-metric of its own; the pass-1 wall-clock overhead is the cost to watch — measure added seconds on the largest resident build (storm-3d national inputs); acceptance: pass 1 < 2 % of total build time. Harness: the build's own per-stage `tracing` timings (tiler.rs already logs per-zoom timings; add one line for pass 1).

**Risks & dependencies.** Root of the whole R1 chain (TB-2…TB-6, TB-9, TB-10 depend on it). Risk: `stt-serve` divergence if pins aren't threaded — the conformance test above is the guard. Borders the observe/seal type lattice (§5 register: keep) — TB-1 reads `PropertyTypes` output, never re-decides kinds.

---

#### TB-2 — Global attribute-range pin `§3.3 · M2 · Phase 4 / R1`

**Change.** Make the numeric-property affine a function of the column's dataset domain, not the tile's sample. In crates/stt-core/src/arrow_tile/quantize.rs:

- `GlobalColumnPins` (new, in config.rs) carries `attr: BTreeMap<String, AttrPinned>` with `AttrPinned { refuse: bool, o: f64, s: f64, leaf: PinnedLeaf }` (new; `PinnedLeaf ∈ {U16, I32}` mirroring the private `QuantLeaf`, quantize.rs:320-323).
- `pub(crate) fn build_quantized_numeric_pinned(values: &[Option<f64>], pin: &AttrPinned) -> Result<Option<(ArrayRef, String)>>` (new): applies the pinned affine verbatim; returns `None` iff `pin.refuse`.
- Pin derivation in `DatasetStats::to_pins` reproduces today's rule tree over _global_ stats: refuse iff global `max_abs ≥ AUTO_QUANT_MAX_ABS` (quantize.rs:314 — this is exactly the global decision the constant's own doc comment asks for, quantize.rs:306-313, citing the deferred-work register in docs/roadmap/stt-packed-format-decisions.md); else exact step-1 with global-span leaf when globally all-integer; else range-adaptive with `s = global_span/65535`. The auto encode path (`build_quantized_numeric_auto` dispatch at encode.rs:529-535) consults `cfg.global_pins` first and falls back to the per-tile function only when pins are absent (`--single-pass`, external one-shot callers, `stt-serve` without pins).
- The explicit `--quantize-attr name=prec` path (quantize.rs:547-615) keeps the user's step but pins `o` to the global minimum instead of the per-tile minimum.
- The affine keeps riding `TILE_META.qa` per tile (encode.rs:899-921) — now carrying the _same_ value in every tile. Deliberately not template-hoisted in R1: moving `qa` template-resident would touch the TEMPLATE/TILE_META partition, which the register says to extend, never redesign; the residual per-tile cost is a few bytes of canonical JSON, and template residency is a priced follow-on for M1.

**Architecture.** Fixes the three recorded defects: (i) the same value now decodes identically in every tile; (ii) the outlier failure (body 0–10 + rare 10⁶) is detectable globally without reintroducing drift — pass 1 may log a loud warning naming the column; (iii) the door opens to arbitrary per-column rate selection beyond {U16, I32} (explicitly _not_ exercised in R1 — menu unchanged, so the change surface stays reviewable). K2 validator drift warnings go to zero because width/type verdicts no longer vary tile-to-tile (with TB-6). Invariants: determinism (pin is a pure function of the domain); conformance-invariance rule satisfied by construction (§13.2 — the verdict is a function of D_j alone); dedup unaffected (identical columns still quantize identically — better, since the offset no longer varies). BYTE-CHANGING → R1. Rollback: `--single-pass`.

**Tests.**

- _Unit (quantize.rs in-file tests, which exist for the auto path):_ pinned affine applied verbatim; refuse pin → Float64 in every tile including tiles whose local sample would have quantized; global step-1 exactness round-trip; explicit-precision path uses global `o`.
- _Property:_ for random datasets split arbitrarily into tiles, decode(value) identical across tiles (the §3.3 constraint-2 statement as a proptest) — extend crates/stt-core/tests/adversarial_decode.rs.
- _Determinism:_ byte-identical re-encode with pins (extend reproducible_build.rs `same_tile_encodes_byte_identically`, :180).
- _Golden:_ v2-golden fixtures change bytes (fixture uses `quantize_attrs_auto: true`, v2_golden.rs:85-97) — pins regenerate under TB-14, once.
- _Guard:_ a test asserting the {U16, I32} menu is not widened by this item (rejected-design boundary: per-tile grids +61 % — and menu growth is deferred).

**Evaluation.** Metric: compressed property-column bytes (stt-optimize `inspect` per-column cost — existing harness) at fixed ε, plus `stt-validate` drift-warning count → 0 (backlog K2). Baseline: current per-tile-affine builds of earthquakes-v2 and hurricanes; no recorded byte number exists for this lever alone — capture pre/post in the R1 measurement set. Acceptance: warning count 0 fleet-wide; property bytes not regressed on any fixture dataset.

**Risks & dependencies.** Depends on TB-1. Borders the per-tile-quantization-grid ban (§5 register) from the safe side — this is the anti-per-tile move; the world-anchored _coordinate_ grid (§3.1) is untouched. Risk: a column whose global span forces I32 where dense tiles fit U16 costs 2 B/row on those tiles — priced against the template it saves in TB-6.

---

#### TB-3 — Global dict-vs-utf8 verdict per column `§3.4 · M2 · Phase 4 / R1`

**Change.** Replace the per-tile `categorical_dictionary_is_smaller` decision (encode.rs:53-71, consumed at encode.rs:558-561) with a dataset-global verdict carried in `GlobalColumnPins.dict: BTreeMap<String, GlobalDictVerdict>` with `GlobalDictVerdict::{Dictionary(Arc<Vec<String>>), Utf8}` (new). Derivation in `to_pins`: `Utf8` when `distinct_overflow` (global k > 65,535 — matching the per-tile degrade at encode.rs:558's `.ok()`), else decided from global stats — and, when the M1 sample oracle is available, by post-zstd trial-encode of a sample tile both ways (the D4 fix; M1's stated decision objective for exactly this site), with the current pre-compression surrogate applied to _dataset totals_ as the fallback. The encoder's categorical branch dispatches on the pin; the 192-byte overhead constant becomes dead on the pinned path.

**Architecture.** Kills PROPS-template forking from boundary flips: one column = one Arrow type everywhere, so `manifest.schemas` stops growing a template per (dense-tile, sparse-tile) split. Exact strings preserved either way; first-seen order preserved (per-tile first-seen today, global first-seen under TB-4). Deterministic; dedup improves (no more type-flip-induced distinct blobs for equal content). BYTE-CHANGING → R1. Rollback: `--single-pass`.

**Tests.**

- _Unit:_ verdict respected in a tile whose local surrogate would flip it (both directions); overflow → Utf8 everywhere; null handling identical on both paths.
- _Integration:_ build a synthetic dataset with dense + sparse tiles of one categorical column; assert exactly one PROPS template in `manifest.schemas` (extend reproducible_build.rs `v2_dataset_rebuild_is_byte_identical_including_schemas`, :202, which already inspects schemas).
- _Determinism:_ byte-identical rebuild with the verdict pinned.
- _Golden:_ rides the TB-14 regeneration (the v2-golden `kind` column is categorical).

**Evaluation.** Metric: compressed PROPS bytes + `manifest.schemas` entry count (both from `stt-optimize inspect` / the manifest itself). Baseline: current fleet schema counts (capture in Phase 0 inventory). Acceptance: schema count monotonically ≤ current on every fixture dataset; PROPS bytes within noise or better.

**Risks & dependencies.** Depends on TB-1; the trial-encode branch depends on M1 (Phase 2) — the surrogate fallback keeps TB-3 shippable if M1 slips. Borders the first-seen-dictionary-order register entry: kept. Prerequisite for TB-4 (only globally-Dict columns hoist).

---

#### TB-4 — Global dictionary hoist (all-or-nothing per column) `§3.8 · M2 · Phase 4 / R1`

**Change.** The biggest measured win left on the table: stop re-shipping each categorical column's category list in every tile (43.9 % of uncompressed tile bytes on earthquakes-v2, 33.6 % on hurricanes). Writer side:

- For every column with a `GlobalDictVerdict::Dictionary(categories)` pin, the encoder builds the `DictionaryArray` against the _pinned global_ category list — full list in every tile, keys `UInt16` indexing it — replacing the per-tile `build_dictionary_indices` call (columns.rs:282-311; the function stays for the unpinned path). A category missing from the pin is a hard error (stats/data drift, never silent).
- The template/tail split moves the now-identical DictionaryBatch message(s) into the TEMPLATE: `split_ipc_at_schema` (encode.rs:759-789) gains a sibling `split_ipc_after_dictionaries(ipc: &[u8]) -> Result<usize>` (new) that walks the encapsulated framing past DictionaryBatch messages to the RecordBatch boundary; `encode_layer_v2_parts` (encode.rs:880-976) uses it for the PROPS stream when any hoisted column is present. Delta dictionaries stay disabled (arrow-rs default).
- Reader-transparency **verified**, both readers: Rust `splice_decode` checks only that the template starts with an encapsulated message and concatenates (decode.rs:106-122); TS `spliceIpc` likewise (poopdeck:packages/core/src/tile.ts:156-183), and the TS categorical lift reads the dictionary off the column data via Arrow's own machinery (`(data as any).dictionary`, tile.ts:1801-1834) with the props schema's dictionary table carried over (tile.ts:1390-1394). A template carrying Schema + DictionaryBatch splices into a stock Arrow stream both languages already decode — no reader change, no new manifest capability.

**Architecture.** All-or-nothing per column, enforced by construction: the verdict is global (TB-3), so a partial hoist (template-per-category-subset forking) is unrepresentable. The category list is stored once per realized layer shape in `manifest.schemas` instead of once per tile; tails shrink to keys-only. Content-addressing, template dedup, and random access unchanged; determinism holds (global list is a pure function of pass 1). Extends the TEMPLATE/TILE_META partition (a provably-constant blob moves template-resident) — the permitted direction. BYTE-CHANGING → R1; this is the headline item of the window (packed-v2-magnitude expectation, design doc Phase 4). Rollback: `--single-pass` (per-tile dictionaries return).

**Tests.**

- _Unit:_ `split_ipc_after_dictionaries` on streams with 0, 1, n DictionaryBatch messages; malformed-framing errors (mirror the existing split_ipc_at_schema error cases).
- _Integration (Rust decode):_ encode with hoist → `decode_tile_with_templates` round-trips exact strings, including tiles containing a strict subset of categories and all-null tiles; template bytes identical across tiles (assert one PROPS template).
- _Cross-impl:_ extend the TS golden decode-equivalence suite (poopdeck:packages/core/test/fixtures, generated by `make-golden-fixture` / `make-v2-golden.sh`) — the fixture's `kind` column exercises the hoist against the real TS reader.
- _Determinism:_ byte-identical rebuild (reproducible_pipeline.rs).
- _Golden:_ v2-golden byte pins change (TB-14); the manifest.schemas table is explicitly part of that pin (v2_golden.rs:8-10), which is exactly right — the hoist is a schemas-table change.
- _Guard:_ a test that a column with verdict Utf8 never emits a dictionary anywhere (the partial-hoist-is-worse-than-none boundary).

**Evaluation.** Metric: E2E pack bytes (`stt-optimize diff` old-vs-new archives — existing harness) on categorical-heavy datasets. Baseline: the measured dictionary shares above bound the uncompressed prize; packed-v2 precedent −44.8 % E2E for template-class work. Acceptance: strict pack-byte reduction on earthquakes-v2 and hurricanes with unchanged decode-equivalence; no regression on categorical-free datasets (flights).

**Risks & dependencies.** Depends on TB-1, TB-3. Risk: zstd already compresses repeated per-tile dictionaries well, so the post-zstd win will undershoot the uncompressed share — that is expected and why the E2E metric is the acceptance, not the share. Borders `manifest.schemas`/template mechanics (register: extend, never replace — respected) and first-seen order (now global first-seen — the register's rationale, run-to-run stability, is preserved).

---

#### TB-5 — Global dense id renumbering + ids-after-sort `§3.5 §3.6 · M2 · Phase 4 / R1`

**Change.** Two coupled fixes to the id column.

1. _Ids-after-sort (free fix)._ Synthetic row-index point ids (assigned per-tile since the Waymo finding — hash ids were ~40 % of compressed point bytes; columnar.rs:272-283 per §3.6) are currently assigned _before_ the encode-time `sort_rows_by_start_time` (encode.rs:796-867, invoked at encode.rs:889), degrading their monotonicity. Add `pub synthetic_row_ids: bool` (new) to `ColumnarLayer` (crates/stt-core/src/arrow_tile/layer.rs), set by `build_point_layer` when it rewrote ids; `encode_layer_v2_parts` re-densifies `feature_ids = 0..n` after the sort when the flag is set. Safe because those ids are per-tile synthetic with no cross-tile identity contract.
2. _Global dense renumbering (keyed features)._ Pass 1 builds `IdRenumberMap` (new, in dataset_stats.rs): a bijection stable-source-key → dense u64, in first-appearance-in-time order (ties broken by source key — deterministic). The stable source key is exactly what `determine_feature_id` / `segment_feature_id` compute today (columnar.rs:1122-1145, 1148-1170) — explicit numeric ids verbatim, string ids through spec-fixed FNV-1a-64 (columnar.rs:1101-1110; the register keeps FNV as this intermediate key). Those two functions gain an optional `renumber: Option<&IdRenumberMap>` parameter and emit the dense id; clipped-segment pieces of one source feature map to one dense id (identity constraint preserved). Source fidelity: the writer emits the reverse map once as a content-addressed sidecar object, referenced from an additive manifest field `idMap { key, length }` (new) — consumers needing source ids join through it; the wire `id` column stays u64, so no reader decode-path change.

**Architecture.** Makes every id column delta-codable (dense, near-monotone after the start-time sort), including clipped-feature layers that today always pay 8 near-incompressible bytes/row. Picking (per-tile uniqueness) and cross-tile identity hold by bijectivity. Determinism: map is a pure function of the feature sequence. BYTE-CHANGING → R1 — and note this is a _content_ change (id values differ), which TB-14's fingerprint gate must model explicitly. Rollback: `--single-pass` (both halves off); the sidecar is additive so old tooling ignores it.

**Tests.**

- _Unit:_ renumber map — first-appearance order, tie determinism, clipped pieces share one dense id (extend columnar.rs in-file tests, which already cover id determination); ids-after-sort — point layer with shuffled times encodes ids `0..n` in stored order.
- _Property:_ bijectivity + identity preservation under random clipping splits (adversarial_decode.rs style).
- _Integration:_ pick-id → sidecar join recovers the source id end-to-end on a small archive.
- _Determinism:_ byte-identical rebuild with renumbering on.
- _Golden:_ pins change (TB-14); the TS `featureIds` masked-low-32-bits contract (tile.ts:1438-1449) must be re-checked against dense ids — dense ids all fit 32 bits, strictly improving that contract.
- _Guard:_ FNV constants untouched (spec_conformance.rs is the natural home — it already pins spec-fixed values).

**Evaluation.** Metric: compressed id-column bytes fleet-wide (`stt-optimize inspect` per-column cost). Baseline: current id-column share on trajectory/clipped datasets (gtfs-nl, nyc-taxi-flows) — capture in Phase 0. Acceptance: id-column bytes strictly reduced on every clipped-layer dataset; zero change in picking behavior (browser verify per the visual-verify preference).

**Risks & dependencies.** Depends on TB-1; interacts with TB-2/TB-6 (dense small ints may newly qualify for narrow leaves — let the pinned width logic see them). Borders two register entries: FNV-1a-64 kept (as the intermediate key — not replaced), and §3.5's sorted-contract (`TILE_META.sorted` declaration unchanged; the residual tie-order freedom of §3.5 is explicitly _not_ exercised in R1 — recorded as NOT PLANNED in the problems doc, and a secondary spatial tie-key would be a separate proposal).

---

#### TB-6 — Schema-template cost term |T| and global width/type pinning `§3.8 §3.4 §2.6 · M2 · Phase 4 / R1`

**Change.** Price the externality D6 leaves unpriced: every per-tile encoding choice that forks a template must see the fork's cost.

- _Global width pin:_ `AttrPinned.leaf` (TB-2) already fixes U16-vs-I32 per column from the global span, eliminating the residual `AdaptiveWidth` drift (quantize.rs:457-461 documents it as the benign leftover). The pin rule gains the drift-aware trade the §3.8 gap names: pin always-I32 when `n_datasetwide × 2 B` (the extra width cost) < the marginal template cost of carrying both width variants — template size read from a trial `TemplateCollector` snapshot.
- _|T| accounting:_ `TemplateCollector` (crates/stt-core/src/arrow_tile/frame.rs:86-110, `snapshot()` at :110) gains `pub fn template_report(&self) -> Vec<(String, usize, u64)>` (new: shape-description, count, bytes); `stt-build` prints it in the build summary, and `PackWriter::finalize` cross-checks the realized `manifest.schemas` count against the pass-1 predicted shape count, warning loudly on any unpredicted fork — the assertion that per-tile encoders no longer fork templates silently.
- _Menu-extension gate:_ any future compact-time menu extension (§2.6's U16 tiers — deliberately _not_ in R1) must call a new `fn template_cost_justifies(shape_delta: usize, byte_savings: u64) -> bool` (new) — the c_tmpl term made explicit rather than special-cased, generalizing the existing empty-layer pin (encode.rs:184-188).

**Architecture.** Pure builder/encoder accounting plus one pin refinement; the TEMPLATE/TILE_META partition itself is untouched (register: it is exactly optimal — extended via TB-4, never redesigned). The `EndTimeForm::Zero`/`Dur32` per-layer choices (encode.rs:189-231) remain per-layer — they are feasibility-gated and within-family optimal per the problems doc §2.6; TB-6 only gives them (and their future extensions) the cost term. BYTE-CHANGING only through the width-pin component → R1. Rollback: `--single-pass`.

**Tests.**

- _Unit:_ width-pin trade fires in the constructed straddling case and not otherwise; `template_report` counts match a hand-built two-shape dataset.
- _Integration:_ finalize-time predicted-vs-realized shape check green on all fixture datasets; a deliberately-forking build (mixed geometry kinds) reports the right count.
- _Determinism:_ byte-identical rebuild.
- _Golden:_ rides TB-14 (width pins can change fixture bytes).

**Evaluation.** Metric: `manifest.schemas` count × template bytes + PROPS bytes (the §3.8 gap-1 objective), via the new report + `inspect`. Baseline: current fleet schema counts. Acceptance: schema count ≤ current on every dataset; the finalize cross-check green fleet-wide.

**Risks & dependencies.** Depends on TB-1/TB-2. Borders §2.6's empty-layer template pin (kept — it becomes a special case of the general term) and the register's partition entry.

---

#### TB-7 — Tile-relative line clip buffer b(z) ∝ 2⁻ᶻ `§1.2 · §4 batch · Phase 4 / R1`

**Change.** Replace the two hardcoded degree constants in `ClipConfig` (crates/stt-build/src/clip.rs:87-104: `buffer_degrees: 0.001`, `polygon_buffer_degrees: 0.0`) with a per-zoom line buffer, polygon side untouched:

- `ClipConfig` gains `line_buffer: LineBuffer` (new) with `enum LineBuffer { TileRelativePx(f64), FixedDegrees(f64) }`; `pub fn line_buffer_degrees(&self, zoom: u8) -> f64` (new) computes `px/256 × 360/2^z` for the tile-relative mode. Default: `TileRelativePx(8.0)` — 8 px of a 256-px tile covers a 16-px stroke's half-width at every zoom (the closed-form constraint b(z) ≥ σ/2 · W(z)/E from §1.2; the same tile-relative form as tippecanoe's `--buffer`).
- CLI: `--clip-buffer-px <float>` (new, default 8) and `--clip-buffer-degrees <float>` (new, legacy escape).
- Wire-through: `clip_config_from` (tiler.rs:665-683) sets the mode; the clip call sites that read `buffer_degrees` (Liang–Barsky expansion, clip.rs:340-385 per the problems doc) take the zoom-resolved value — zoom is already in scope at every placement call.

**Architecture.** Cuts deep-zoom seam-strip duplication (0.001° is ~4.5 % of tile width at z14 and _exceeds_ tile width by z18, versus a constant 3.1 % tile-relative) and fixes the inverse defect — at low zooms 0.001° is far _below_ a stroke's ground half-width, so wide strokes can seam today. Polygon buffer stays 0: pinned optimal by the watertight bit-identical-seam construction (clip.rs:58-66) and by explicit instruction — a hard constraint of this item. Ordering constraint preserved (clip before simplification, after antimeridian split). BYTE-CHANGING → R1. Rollback: `--clip-buffer-degrees 0.001`.

**Tests.**

- _Unit:_ `line_buffer_degrees` closed form per zoom; legacy mode reproduces 0.001° exactly.
- _Guard:_ polygon buffer remains 0 in every mode — assert `polygon_buffer_degrees == 0.0` is not configurable (the pinned-optimum boundary); the existing seam-identity tests in clip.rs and crates/stt-build/tests/antimeridian_polygon.rs must stay green untouched.
- _Integration:_ a trajectory crossing a tile edge at z4 with a wide stroke has vertices in both tiles (the low-zoom fix); duplicated-vertex count at z14+ strictly lower than legacy on the same input.
- _Determinism:_ byte-identical rebuild at fixed flags.
- _Golden:_ stt-build-level fixtures change where lines cross tiles; rides TB-14.

**Evaluation.** Metric: duplicated vertex bytes at z ≥ 12 (archive bytes per zoom via `stt-optimize inspect`) with zero new seam artifacts. Baseline: current gtfs-nl / flights builds. Acceptance: deep-zoom line-layer bytes down (the doc's expectation: ~10–100× on the duplication term at the deepest zooms); seam check is a browser verify (user judges aesthetics — per the standing visual-verify preference, no screenshot loops).

**Risks & dependencies.** Independent of TB-1 (pure closed form). Borders the pinned polygon-buffer-0 optimum — designed around by construction. Interaction with TB-8: both touch line geometry below max zoom; land in the same window so the fleet re-measures once.

---

#### TB-8 — Metric simplification as default `§1.3 · §4 batch · Phase 4 / R1`

**Change.** Flip `simplify_metric` from opt-in to default. `TileConfig::default` (tiler.rs:190: `simplify_metric: false`) and the CLI default become `true`; add `--simplify-degree-table` (new) as the legacy escape hatch, replacing the current positive `--simplify-metric` flag (kept as an accepted no-op alias for one release). The metric machinery already exists and is tested: 1.0 px tolerance in cos φ-scaled locally-isometric space (simplify.rs:30-70), same VW/TD-TR selectors, subset-only selection so the scale round-trips losslessly. Applies only when `--simplify` is on (default remains false), so point-only and unsimplified builds are unaffected.

**Architecture.** The legacy degree table is up to ~2× inconsistent in ground terms at 60° latitude and its values don't match their own resolution comments (simplify.rs:13-24); metric mode was explicitly DEFERRED "awaiting a fleet republish" — R1 is that republish. Constraints preserved: the lossless max tier (lines `z > simplify_max_zoom` untouched, polygons strict `z ≥`, pinned by the existing test at simplify.rs:628-639 per the problems doc); polygon topology guards; the ordering constraint. BYTE-CHANGING for `--simplify` datasets → R1. Rollback: `--simplify-degree-table`.

**Tests.**

- _Existing to keep green:_ the latitude-consistency pair at simplify.rs:734-773 (degree mode latitude-sensitive, metric not) and the max-tier pin — both now guard the default path.
- _Unit:_ flag plumbing — default resolves metric; escape hatch reproduces legacy bytes exactly (byte-compare one tile both ways).
- _Determinism:_ byte-identical rebuild under the new default.
- _Golden:_ any committed simplified-line fixture changes; rides TB-14.

**Evaluation.** Metric: vertices/tile and tile bytes per zoom below the max tier at fixed 1.0 px rendered error; cross-latitude consistency (same ground shape → same vertex count at 0° and 60°). Baseline: legacy-table builds of drifters/animals (high-latitude data). Acceptance: latitude-consistent generalization verified by the existing test pair; byte movement reviewed, not gated (this item is about correctness of the tolerance, not size).

**Risks & dependencies.** Independent; batches under P6. The Imai–Iri optimal selector and per-dataset ε(z) search from §1.3's gap are _not_ in scope (advisor-family work, M1/M3 territory). Borders the lossless-max-tier no-thinning constraint — untouched by construction.

---

#### TB-9 — Temporal-LOD default zoom cutoffs `§2.4 · §4 batch · Phase 4 / R1`

**Change.** Stop defaulting every coarse tier to every zoom — today an un-annotated `--temporal-lod "30d"` entry gets `max_zoom_level = fallback_max_zoom` (`parse_temporal_lod`, crates/stt-build/src/build_options.rs:58-83, the `None => (piece, fallback_max_zoom)` arm at :73), i.e. maximal duplication. Two coordinated moves:

- _Builder default:_ `parse_temporal_lod` gains a byte-mass-aware default: un-annotated tiers receive `default_lod_cutoff(b̂, tier_index)` (new, in build_options.rs) — the largest z_j such that the cumulative pass-1 estimated byte mass `Σ_{z≤z_j} b̂(z) ≤ β · b̂_total` (β default 0.25; b̂ from TB-1's per-zoom estimator, tiler.rs:1586-1589 arithmetic), coarser tiers clamped strictly below finer ones, all clamped ≥ `min_zoom`. Explicit `@z` always wins. Signature change: `parse_temporal_lod(s: &str, fallback_max_zoom: u8, zoom_mass: Option<&[u64]>) -> Result<Vec<TemporalLodLevel>>`.
- _Advisor:_ `advise_temporal_lod`/`pick_tiers` (crates/stt-optimize/src/advisors/temporal.rs:71-131) emit explicit `@z` annotations computed from the per-zoom byte mass stt-optimize _measures_ (its density model), so recommended recipes never rely on the builder default. The tier-width ladder itself (CLEAN_TIERS, :44-50) is untouched — width optimization is M3's budgeted-greedy territory.

Reader untouched: `temporal_lod_for_zoom` (stt-core metadata.rs:387-393, mirrored by `TileConfig::lod_for_zoom`, tiler.rs:221-227) already dispatches on `max_zoom_level`; validation invariants (tiler.rs:426-455) unchanged.

**Architecture.** The duplication term `Σ_j Σ_{z≤z_j} b(z)` is dominated by the high-zoom byte share, and the queries that pick a coarse tier are low-zoom by construction — so cutting high-zoom emission has zero query-cost for the tier's actual users. Tiers stay exact unions (no-thinning intact; the LOD path reuses the base tiler with the bucket overridden, `generate_lod_level`, tiler.rs:382-421 — unchanged). BYTE-CHANGING (smaller archives) → R1. Rollback: write explicit `@z` = max zoom.

**Tests.**

- _Unit (build_options.rs in-file tests exist for parsing):_ default cutoff monotone in tier coarseness; explicit `@z` wins; no-mass fallback preserves current behavior; clamps.
- _Integration:_ extend the tiler LOD tests (`lod_aggregator_emits_base_plus_per_level_tiles`, tiler.rs:2281; `lod_aggregator_skips_zooms_above_max_zoom_level`, :2328) with an un-annotated spec + synthetic mass → coarse tier absent above its computed cutoff; archive round-trip via `lod_tiles_carry_bucket_size_through_archive_round_trip` (:2397).
- _Determinism:_ byte-identical rebuild (cutoff is a pure function of pass-1 mass).
- _Golden:_ only LOD-bearing fixtures change; rides TB-14.

**Evaluation.** Metric: added-tier archive bytes per unit of request-count reduction (tier bytes from `stt-optimize inspect`'s per-bucket breakdown; request counts from the advisor's own bucket arithmetic, temporal.rs:100-104). Baseline: current all-zooms emission on drifters (decade span, LOD-advised). Acceptance: tier byte share strictly down at unchanged coarse-scrub request counts for z ≤ cutoff queries.

**Risks & dependencies.** Depends on TB-1 (b̂(z)). Borders M3 (`addedTierBytes`, §12.4): TB-9 is the build-side default; the measured cost comparison stays M3's. The §2.3/§2.4 interaction sentence in the advisor (temporal.rs:59-64) remains — TB-10 does not resolve it and neither does this.

---

#### TB-10 — Adaptive-temporal exact DP + shared boundary snapping `§2.3 · §4 batch · Phase 4 / R1`

**Change.** Replace the greedy first-fit sweep in `chunk_adaptive_by_count` (tiler.rs:1465-1489) and restore an enumerable-boundary contract:

- `fn chunk_adaptive_dp(features: Vec<TileFeature>, target: u32, boundaries: &[u64]) -> Vec<(u64, Vec<TileFeature>)>` (new): group features into timestamp atoms (the existing inseparability rule, tiler.rs:1477), then solve the exact min-max partition over atoms by DP in O(mK) per cell — cells are small, so this is cheap; objective `min max_k |W_k|` deviation from `target` (the problems doc's exactly-solvable formulation).
- _Shared boundary snapping:_ pass 1 (TB-1) computes a dataset-wide candidate boundary set S — 256 quantiles of the union timestamp distribution, deduplicated, deterministic. Each window's _key_ becomes the largest s ∈ S with s ≤ the window's first atom timestamp (distinctness preserved: windows are time-ordered and atoms unsplittable, with a same-key collision resolved by falling back to the atom timestamp, logged). Adjacent cells therefore share fetch instants, fixing the recorded production gotcha (adaptive mode silently breaks fixed-boundary prefetch enumeration — the blob-ordering playback memory).
- S publishes in the manifest as an additive field `adaptiveBoundaries: Vec<i64>` (new) so clients can enumerate candidate keys; reader-side prefetch consumption is a separate client item, explicitly out of scope here.
- Scope guard unchanged: in-memory path only (`adaptive_target_features` doc, tiler.rs:136-142; streaming keeps fixed buckets), and adaptive mode still disables fixed-bucket trajectory slicing (`clip_config_from`, tiler.rs:670-677).

**Architecture.** Losslessness (every atom in exactly one window, no feature dropped) and `(z,x,y,t)` key distinctness preserved; determinism preserved (DP and S are pure functions of the inputs). BYTE-CHANGING for `--adaptive-temporal` datasets → R1. Rollback: keep the greedy behind `--adaptive-greedy` (new, debug-only) for one release.

**Tests.**

- _Existing to extend:_ `adaptive_temporal_chunking_sizes_windows_by_count` (tiler.rs:2506) re-targeted at the DP; `adaptive_chunking_keeps_identical_timestamps_together` (:2548) must pass unchanged — atoms are the invariant.
- _Unit:_ DP optimality vs brute force on small atom sets (≤ 12 atoms, exhaustive); snapped keys ∈ S ∪ {atom timestamps}; collision fallback.
- _Property:_ for random bursty inputs, DP max-window ≤ greedy max-window (dominance), and no window splits an atom.
- _Simulation:_ two adjacent cells over the same timeline share ≥ x % of window keys post-snapping vs ~0 % pre (the cross-cell alignment claim, as a unit-level simulation).
- _Determinism:_ byte-identical rebuild.
- _Golden:_ no committed fixture uses adaptive mode (fixtures are fixed-bucket) — pins unaffected; verify during TB-14 inventory.

**Evaluation.** Metric 1: variance of per-tile bytes (from `stt-optimize inspect --sample 0`, directory-only — existing, fast). Metric 2: requests-per-playhead-advance in multi-cell playback — NEEDS-HARNESS: the playback-workload simulator is Phase 0 / M4 infrastructure (the third canonical query); until it lands, report metric 1 plus shared-key fraction. Baseline: greedy builds of a bursty dataset (earthquakes-v2 with adaptive advised). Acceptance: per-tile-byte variance strictly down; shared-boundary fraction > 0.9 across neighboring cells.

**Risks & dependencies.** Depends on TB-1 (quantile set). Borders no-thinning (atoms, losslessness — preserved) and the §2.3/§2.4 advisor interaction (unresolved, unchanged). The advisor's 4×median multiplier refit is M1/M3 work, not this item.

---

#### TB-11 — Vertex-time menu extensions `§2.5 · §4 batch · Phase 4 / R1`

**Change.** Three extensions to `build_vertex_time_array` (crates/stt-core/src/arrow_tile/columns.rs:393-483), whose narrowest-first greedy is exactly optimal within its current family — the family is what grows:

1. _Bucket-proportional ceiling:_ scale `vertex_time_max_step_ms` by the tile's LOD bucket ratio, so a 30-day tier tile tolerates proportionally coarser vertex time at equal perceptual error. Implementation: `encode_and_add_tiles` (tiler.rs:1685-1739) already receives the per-tile bucket tag `Option<u64>`; when `Some(b)`, it encodes with a per-call `EncoderConfig` clone whose `vertex_time_max_step_ms` is multiplied by `b / base_bucket_ms` (base threaded in; saturating). Same in `write_lod_tile` (tiler.rs:1798-1816). Rescues wide temporal-LOD layers from the 2–4× exact-i64 fallback (warned at columns.rs:453-462).
2. _Feature-anchored u16 tier:_ a new tier between the layer-anchored u16 and u32 — deltas against each feature's _own_ `start_time` (already shipped in CORE, so the anchor is free), covering trip-shaped layers whose per-feature span is small while the layer span is wide. New TILE_META discriminant: extend `vt` with a form tag — `TileMeta.vt` today is `Option<(i64, u32)>` (frame.rs:259); add sibling key `vtf: Option<u32>` (new — step only; origin is per-feature) rather than changing `vt`'s shape (TILE_META extended, never mutated). Both readers branch on the new key; old readers reject via the capability below.
3. _Ceiling coupled to playback:_ the `s_max`-vs-playback-speed relation (§2.5's knob-level meta-problem) lands as an stt-optimize advisory writing `--vertex-time-precision` into recommended recipes — no builder constant change.

The tier scan stays narrowest-first greedy — still exactly optimal in the enlarged family (feasibility monotone in width). New required capability `vertex-time-feature-anchor` (new, in stt_core::pack's CAPABILITY_* registry) declared iff form 2 is used, mirroring how `EncoderSettings::required_capabilities` derives declarations from the config (pattern verified in examples/make-golden-fixture.rs:69-91).

**Tests.**

- _Unit (columns.rs has in-file coverage of the tier scan):_ feature-anchored tier chosen for a synthetic trips layer (wide layer span, narrow per-feature spans); ceiling scaling picks a coarser step on an LOD-tagged tile and the exact base tile is unchanged; i64 fallback still reached when nothing fits (never a silent precision cliff — the pinned behavior).
- _Decode:_ Rust + TS re-inflation of the feature-anchored form round-trips exactly at step 1 (extend crates/stt-core/src/arrow_tile/tests.rs and the TS decode tests).
- _Determinism:_ byte-identical rebuild; per-call config cloning must not vary with worker count (covered by `parallel_encode_writes_byte_identical_dataset`, tiler.rs:2156, extended to an LOD build).
- _Golden:_ fixtures carry a u16-delta trajectory layer (v2_golden.rs:33-36) — pins change under TB-14. Vertex-timestamp unit handling: keep crates/stt-build/tests/vertex_timestamp_units.rs green.
- _Guard:_ `--no-compact-times` still does not affect this column (separate lever, §2.5 knobs).

**Evaluation.** Metric: vertex_time column share of tile bytes (per-column `inspect`) on trajectory archives + count of layers hitting the i64 fallback (the once-per-process warning becomes a counted build-summary line). Baseline: the u16→i64 cliff measured at 14.3 % of nyc-taxi-flows tile bytes; current fallback counts on temporal-LOD trip datasets. Acceptance: fallback count → ~0 on LOD trip builds; column share down on at least one shipped trajectory dataset.

**Risks & dependencies.** Item 1 depends on the LOD write path only; item 2 touches both readers — sequence reader support before builder emission inside the window. Borders the TEMPLATE/TILE_META partition (extended with one additive key) and the capability doctrine (silent-misdecode prevention — hence the new capability). The delta-of-delta/Gorilla tier from §2.5's gap is _not_ included (post-zstd measurement first — M1's call).

---

#### TB-12 — Decoder-side single-ring earcut backfill, then per-feature triangle omission `§3.7 · §4 batch · reader: Phase 1 · builder: Phase 4 / R1`

**Change.** Reader change first, builder flag second — the order the code comment itself prescribes (columnar.rs:437-456 names the decoder backfill in poopdeck:packages/core/src/tile.ts as the cheapest fix).

- _Reader (byte-neutral, ships anytime):_ in the TS decoder, when a polygon layer carries a `triangles` column and a feature's baked list is empty _and_ the feature is single-ring (one ring in geometry, no `part_offsets` entry), earcut that ring at decode and splice the indices into the layer's triangle buffer — reusing the tessellation the renderers already own (`tessellateFeature` in poopdeck:packages/core/src/render/geometry.ts, per the columnar.rs:448 inventory). This relaxes the reader contract ρ from layer-global to per-feature for all three consumers at once, since all of them read the decoder's output buffer. Mirror the same backfill in the Rust decode path's consumers if any renderer-side Rust consumer exists (none known — the three readers named at columnar.rs:443-451 are all TS).
- _Builder (BYTE-CHANGING, R1):_ `needs_triangles`'s all-or-nothing emission (columnar.rs:457-458) becomes per-feature: emit `y_i = h_i` — baked indices only for hole/multi-part features (`PolygonParts::needs_triangles`), empty lists otherwise. Under `--pre-tessellate` the old bake-everything behavior remains (explicit opt-in for CPU-constrained clients). New required capability `triangles-partial` (new) declared whenever any emitted layer mixes empty and baked lists — a pre-backfill reader would silently _vanish_ single-ring features (the precisely-documented failure, columnar.rs:452-453), which is the silent-misdecode class the capability registry exists to hard-reject.
- Secondary (in-window, cheap): run earcut on quantized coordinates when coordinate quantization is on, so sub-step slivers stop wasting indices (§3.7 gap, secondary note).

**Architecture.** Saves the single-ring share of the triangle column — the column is 40–45 % of polygon-heavy tiles' wire bytes, most of it single-ring. `part_offsets` emission is untouched (register: load-bearing, zero degrees of freedom). Rollback: builder side reverts to bake-everything (the capability makes old archives + new readers, and new archives + new readers, both safe; new archives + old readers are rejected loudly, never wrong).

**Tests.**

- _Reader unit (poopdeck:packages/core tests):_ mixed layer decodes with single-ring features filled by backfill, holes untouched; a hole feature with an (adversarially) empty list is NOT backfilled (correctness constraint h_i = 1 ⇒ baked).
- _Builder unit:_ emission pattern `y_i = h_i`; `--pre-tessellate` restores full bake; non-polygon poisoning guard (encode.rs:439-445) still drops the column.
- _Cross-impl golden:_ add a mixed polygon fixture to the TS decode-equivalence corpus (make-v2-golden.sh) — decoded triangle buffers identical between a bake-everything archive and a partial archive + backfill.
- _Determinism:_ byte-identical rebuild; earcut-on-quantized determinism (pure function of grid indices).
- _Guard:_ capability declared iff partial emission occurred (extend crates/stt-core/tests/capability_registry.rs — exists).
- _Golden pins:_ polygon fixtures change under TB-14.

**Evaluation.** Metric: polygon-layer tile bytes (per-column `inspect` on the `triangles` column) and decode-time CPU delta (the λ term — measure decode ms/tile in the TS test harness). Baseline: 40–45 % triangle share on storm-field / storm4d-cloudtop. Acceptance: triangle bytes down by ≥ the measured single-ring share on those archives; no visual diff (browser verify); decode CPU increase bounded and reported.

**Risks & dependencies.** Reader half must be deployed (CF Pages) before any partial archive is published — R1's standing packs → frontend → manifests order handles first-party; the capability handles third-party. Borders `part_offsets` (untouched) and the no-thinning spine (triangles are derived data, not features — omission is reconstruction, not loss).

---

#### TB-13 — Encode/flush double-buffering `§1.4 · §4 batch · byte-neutral, ships anytime`

**Change.** Remove the flush bubble in `encode_and_add_tiles` (tiler.rs:1685-1739): today each sub-batch is encoded in parallel, then flushed serially to `add_tile_full` while the rayon pool idles. Restructure to two pipelined halves of the byte cap: while the calling thread flushes sub-batch j in order, the pool encodes sub-batch j+1 (each half capped at `budget/8` so peak transient bytes stay ≤ the current `budget/4`; wave-growth logic per half unchanged, tiler.rs:1700-1722). Implementation: `rayon::scope` with the encode of j+1 spawned as a scoped task producing `Vec<Vec<u8>>` through a single-slot channel; the writer half never migrates threads (`PackWriter` stays `&mut` on the caller). `ENCODE_CHUNK` (tiler.rs:1663) and the unbudgeted path are untouched.

**Architecture.** Byte-neutral by construction — batch boundaries cannot change output bytes (per-tile `encode_tile_with` is deterministic, hand-off order is unchanged, and the writer's finalize re-sorts anyway; the invariants documented at tiler.rs:1665-1684). Bounded memory preserved at the same cap. Ships any time; it matters most _during_ R1 itself — the fleet republish and the 9.5 h national storm rebuild are the beneficiaries — so land it before the window opens. Rollback: trivial revert; no format surface.

**Tests.**

- _Existing pin, extended:_ `parallel_encode_writes_byte_identical_dataset` (tiler.rs:2156-2234) — the load-bearing guard; extend it to run with a small `--pack-memory-budget` so the pipelined path (not just the unbudgeted one) is byte-compared, across `--workers {1, 4}`.
- _Unit:_ peak-transient accounting — instrument the two halves and assert combined in-flight encoded bytes ≤ cap + one wave (the documented overshoot bound).
- _Property:_ random tile-size sequences → identical hand-off order and identical per-tile payload bytes vs the serial path.
- _Golden:_ pins unchanged (byte-neutral — this is itself an assertion: run the v2_golden byte pin against a pipelined build).

**Evaluation.** Metric: wall-clock build time at fixed peak RSS. Baseline: the 9.5 h storm-3d-conus→national build is the recorded yardstick; for CI-scale, time a mid-size fixture build (earthquakes-v2 from source, per the showcase build recipes). NEEDS-HARNESS for systematic tracking: a build-throughput bench belongs in the Phase 0 harness inventory (none exists today — Phase 0 lists only frame-cost, QoE, ordering_sim, inspect/diff). Acceptance: measurable wall-clock reduction on a budgeted multi-GB build at unchanged peak RSS and identical output bytes.

**Risks & dependencies.** None on other TB items. Borders the byte-reproducibility contract (TileWriter doc, tiler.rs:52-56) — the extended pin test is the guard. The §1.4 size-predictor idea (pre-cutting batches via `tile_feature_size`) is _not_ included — separate, lower-value follow-on.

---

#### TB-14 — The R1 golden-pin update protocol `§13.1 · M2+M7 · Phase 4 / R1 gate`

**Change.** A written, enforced procedure (docs/roadmap/, plus CI wiring) for changing the byte pins exactly once. The pin inventory, verified:

1. **Rust byte pin:** crates/stt-core/tests/fixtures/v2-golden/{single,paged}/ + expected-hashes.json, regenerated only via `cargo test -p stt-core --test v2_golden -- --ignored regenerate_v2_golden` (v2_golden.rs:50; the generator is `#[ignore]`-gated at :470-474 precisely because regenerating REPLACES the pin).
2. **TS cross-impl fixtures:** poopdeck:packages/core/test/fixtures/{packed-golden, paged-golden, paged-golden-single} via `cargo run -p stt-core --example make-golden-fixture` (examples/make-golden-fixture.rs:27), and the decode-equivalence corpus via poopdeck:packages/core/scripts/make-v2-golden.sh (referenced at v2_golden.rs:13-14).
3. **Non-pin determinism tests** (no fixtures to regenerate, must stay green throughout): reproducible_build.rs (`same_tile_encodes_byte_identically` :180, `v2_dataset_rebuild_is_byte_identical_including_schemas` :202), crates/stt-build/tests/reproducible_pipeline.rs (`full_pipeline_is_byte_reproducible` :51), spec_conformance.rs, single_tile_conformance.rs.
4. **Inventory step:** sweep crates/stt-build/tests/fixtures and crates/stt-optimize/tests for any committed archive bytes before the window (do this during window prep, not mid-regen).

**Protocol.** (a) M7 semantic-fingerprint validation lands _first_ (its own section's item; hard dependency) and a pre-R1 fingerprint capture runs over every fixture and every fleet archive. (b) All byte-changing TB items (TB-2…TB-12 builder halves) merge to the R1 integration branch; byte-neutral TB-13 and TB-12's reader half ship beforehand. (c) One regeneration commit contains the full code delta plus every regenerated fixture — the diff is _expected_ to show every object address churning (v2_golden.rs:24-27's stated purpose: deliberate and reviewed, never accidental; precedent: the FNV-id one-time address churn). (d) The acceptance gate is fingerprint comparison, per transform class: items that must not change decoded content (TB-4 hoist, TB-6 width pins, TB-13) require _equal_ fingerprints; items with declared content transforms require the declared delta and nothing else — TB-5 (ids: fingerprint id-set cardinality + bijective join through the `idMap` sidecar, never raw id equality), TB-7/TB-8 (geometry below the lossless max tier: bbox/count/max-tier-decode invariant; sub-tier vertex deltas reviewed), TB-9 (tier presence delta only), TB-11 (vertex-time within declared step bounds). (e) `stt-validate` semantic pass green fleet-wide on the rebuilt archives _before_ publish — the M2 objective "K2 warning volume → 0" is asserted here. (f) Fleet republish once: `--no-prune`, standing 15-second-exposure packs → frontend → manifests ordering; then re-run the K10 cold-start capture (Phase 4's closing step). Rollback story: `--no-prune` retains the previous pack fleet on R2, so rollback = re-point manifests; the previous fixture corpus is one git revert away.

**Tests.** The protocol _is_ tests, plus two additions: a CI guard that fails if any file under tests/fixtures/v2-golden changes without the marker string the regen test writes (pin changes must be self-announcing); and a `fingerprint_transform_classes` table test asserting every TB item is mapped to exactly one gate class (no item lands unclassified).

**Evaluation.** Binary: one regeneration commit for the whole window (count of pin-touching commits = 1); semantic-validate green fleet-wide; K2 warnings 0; K10 re-captured. The headline byte measurement (packed-v2-magnitude reduction on categorical-heavy datasets, bounded by the 33.6–43.9 % dictionary shares) is reported from `stt-optimize diff` old-vs-new as the window's closing record.

**Risks & dependencies.** Depends on M7 landing first (the design doc's Phase 4 makes this explicit) and on every TB item's branch discipline. The gravest failure mode is the one M7 exists for — a content-corrupting builder bug passing structural validation (the 106-archive AV precedent); the fingerprint gate, not a human with an export tool, must be what catches it. Borders the git-safety rule (large uncommitted multi-session work in-tree: targeted `git restore` only, never bulk reverts) — the regen commit must be built from a clean integration branch, not the working tree.

## 8. Declared tiers & frustum selection (M8, Phase 5)

This section covers the two Phase 5 tracks of the coherence program: **M8**, the declared-tier unification — one spec concept with a stated content contract, replacing four separately-grown tier mechanisms (exact re-bucketed temporal-LOD, additive home-zoom, reduced M4/MinMaxLTTB, the scrub tier) plus the §2.2 interval-segregation erratum candidate — and the **frustum-quadtree tile selection** (Wave 3/A1) that retires the per-tile-zoom AABB enumeration. Spec work here is design-first with trigger-gated implementation; the frustum work is BYTE-NEUTRAL client code, large but shippable outside any rebuild window. Two register entries bound everything below: bucket-keyed LOD grids never revert, and the no-thinning lexicographic partition is inviolable — reduced tiers are its sanctioned declared-variant escape, never a relaxation of it.

#### DT-1 — Unified tier-declaration spec (one concept, one review) `§1.1 §2.4 §4.5 §11.6 §13.3 · M8 · Phase 5`

**Change.** A single spec design covering every tier-shaped feature, written against the two declaration surfaces that already exist. Today's surface is split: `manifest.variants[]` registers content representations (`kind: "raw" | "summary"`, variant 0 always raw; `docs/spec/manifest.schema.json:63-86`), and `metadata.temporal_lod[]` registers coarser-bucket tiers as `TemporalLodLevel { bucket_ms, max_zoom_level }` (`crates/stt-core/src/metadata.rs:207-213`, validated by `validate_temporal_lod` at metadata.rs:513, reader pick `temporal_lod_for_zoom` at metadata.rs:387-393), with the content contract normative in `docs/spec/time-model.md` §4 ("exactly the base features, re-bucketed… no reduction, aggregation, or thinning" and the explicit promise that reduced tiers "will be announced by an explicit metadata field when specified"). The unification, concretely:

- `metadata.temporal_lod[].contract` (new, optional): `"union" | "reduced"`. Absent = `"union"` = today's normative MUST — every existing manifest stays valid and byte-identical (the writer-omission convention already used for `blobOrdering`, manifest.schema.json:92).
- `variants[].kind` enum gains `"reduced"` (new), with `variants[].method` (new: `"m4" | "minmaxlttb"`) and `variants[].params` (new, object) required when kind is `reduced`. A reduced temporal tier is then a `temporal_lod` entry whose `contract: "reduced"` names a `variant_id` of kind `reduced` — riding the existing complete address `(z,x,y,t,variant_id,temporal_bucket_ms)` (spec §3, manifest.schema.json:64) rather than inventing a new axis.
- `metadata.partition` (new, optional): absent/`"replicated"` (today) or `"home-zoom"`. When `"home-zoom"`, the manifest MUST list a new must-understand capability `"additive-partition"` in `capabilities` (registry mechanism at manifest.schema.json:55; registered in `x-stt-capability-registry`), because home-zoom changes what the _base tier at one zoom_ contains — an old parent-fallback reader would silently render a sparse slice as if complete, exactly the silent-misdecode class capabilities exist to convert into loud refusal. Reduced tiers, by contrast, need NO must-understand capability: variant 0 stays complete and readers never fetch a variant they did not ask for (the summary-tier precedent).
- The scrub tier is specified as **not a storage concept**: G5 (§11.6) is a reader _policy_ consuming whichever declared tier is byte-cheapest, resolved by DT-4. The spec text states this so the fourth "mechanism" dissolves.
- Reader negotiation rule (normative): a reader MUST NOT substitute any non-base tier for base content unless it understands the tier's declared `contract` (and `method`, if reduced). Mirrored in TS: `TemporalLodLevel` in `@poopdeck.gl/core` (consumed via `temporalLodLevels`, poopdeck:packages/core/src/spatiotemporal-tileset.ts:658) gains the optional `contract`/`method` fields (new).
- Conformance rules for `stt-validate` (`docs/spec/conformance.md` + crates/spatiotemporal-tiles/src/bin/stt-validate/): (1) `contract`/`method` enum-valid, method registered; (2) `contract:"union"` tiles decode to exactly the id-union of their base buckets on a deterministic sample — implementable as an M7 semantic-fingerprint comparison once Phase 4 lands; (3) `partition:"home-zoom"` ⇒ capability present, and sampled feature ids appear at exactly one zoom; (4) a `reduced` variant never shadows variant 0 (variant 0 remains addressable at every declared zoom).

**Architecture.** Spec-track, design-first (design §6 Phase 5: "one declaration mechanism reviewed once"). All fields are additive-optional; existing archives and golden fixtures are untouched, so the declaration itself is BYTE-NEUTRAL — bytes only move when DT-2/DT-3 _emit_ the new fields, under their own gates. Preserves: the formatVersion-3 variant cutover just shipped (spec §9.3 changelog — extend the registry, do not redesign the address), the time-model union MUST as the default, and conservative-superset soundness (an undeclared tier is simply never substituted). Rollback: optional fields; readers ignore what writers never emitted.

**Tests.** Unit: extend `crates/stt-core/src/metadata.rs` tests (`temporal_lod_roundtrips_through_json`, metadata.rs:890) with contract-bearing round-trips; TS mirror in `poopdeck:packages/core/test/manifest-schema.test.ts` (exists) — accept/reject cases for each new field. Integration: `crates/spatiotemporal-tiles/tests/validate_cli.rs` (exists) — unknown contract hard-fails, `reduced` without `method` fails, `partition:"home-zoom"` without the capability fails; `crates/stt-core/tests/spec_conformance.rs` (exists) pins the schema/spec agreement. Guard: `poopdeck:packages/core/test/capabilities.test.ts` (exists) — reader refuses an `additive-partition` archive it does not implement. Golden fixtures: **unchanged** — no writer emits the fields until DT-2/DT-3.

**Evaluation.** The deliverable is O5's "one reviewed tier-declaration mechanism": a reviewed spec diff plus green conformance suite; no runtime metric. Cheap fleet check: `stt-validate` over the published fleet stays green (nothing declared yet).

**Risks & dependencies.** The union-equality conformance check depends on M7's semantic fingerprints (Phase 4) — ship the declaration-time checks first, the content check second. Borders the time-model content-contract MUST (extend by declaration, never weaken the default) and the TEMPLATE/TILE_META partition (register: extend, never replace). All later DT items depend on this design being settled first — that is the point of designing once.

#### DT-2 — Home-zoom build-side assignment (additive partition in stt-build) `§1.1 §4.5 · M8 · Phase 5, trigger-gated`

**Change.** The counted-out half of the additive decomposition: today the reader ships (`lodMode: 'additive'` — zoom-union enumeration at spatiotemporal-tileset.ts:1715-1718, no-dedup render at :4074-4081, oversize-parent exemption at :1747) but only script-built AV LiDAR bundles use it (`lod_home_zoom`, scripts/data-generation/av_common.py:2099-2186 per §4.5); stt-build itself always replicates per zoom (`place_feature` loop, §1.1 Incumbent). Build: new module `crates/stt-build/src/home_zoom.rs` (_shipped 2026-08; wired at `crates/spatiotemporal-tiles/src/bin/stt-build.rs:1658`_) with

```rust
pub fn assign_home_zoom(
    features: &[FeatureRecord],
    zoom_band: RangeInclusive<u8>,
    s_px: f64,
    temporal_bucket_ms: Option<u64>,  // Some ⇒ 4D voxel key (animated data)
    origin: GridOrigin,               // pinned, window-independent
) -> Vec<u8>                          // one home zoom per feature (new)
```

— coarse-to-fine voxel claiming (the av_common shape), deterministic by construction: candidates ordered by (importance desc, FNV-1a-64 feature id asc — the §3.6 ids, no RNG), voxel pitch `s_px · 156543·cosφ / 2^z`, origin pinned (the `mrms_refloor.py` nesting lesson). For animated datasets the voxel key MUST include `⌊t/temporal_bucket_ms⌋` — the §4.4 bucket-keyed constraint (13%→65% visible-bucket coverage; register: never revert to space-only). CLI: `--additive-lod[=<s_px>]` (new; default `s_px` proposed 0.4 but re-fit per P1 — the recorded value came from one Miami sweep), mutually exclusive with user-supplied `--min-zoom-field`. Internally the pass synthesizes the per-feature band and routes through the **existing** band mechanism (`min_zoom_field`/`max_zoom_field`, crates/stt-build/src/tiler.rs:148-156, applied at tiler.rs:714-726 per §1.1) so the placement authority needs no change. Manifest: `metadata.partition = "home-zoom"` + capability `additive-partition` (DT-1). Reader wiring: the tileset defaults `lodMode` from the manifest declaration (today a manual option, defaulted at spatiotemporal-tileset.ts:823) so a declared archive cannot be opened under parent-fallback by accident; the capability refusal is the backstop for old readers.

**Trigger (spelled out).** The recorded trigger is "per-zoom duplication dominating a dataset that matters" (§1.1, stt-packed-format-decisions.md:442-452). Operationalized as a doctor finding: `stt-optimize inspect` computes duplication factor `D = Σ_z stored feature rows at z ÷ distinct base features` (earthquakes-v2: 523 K / 47.5 K ≈ 11×) plus a projected additive size via an M1 sample-encode of the assignment on the sample tiles. Implementation proceeds only when a fleet dataset shows D ≥ 4 (threshold to be fit, P1) AND the replicated geometry is the dominant archive-byte share per M1's per-column measurement. Until then this item is design + the trigger instrumentation only.

**Architecture.** BYTE-CHANGING for archives that opt in; default OFF, so the published fleet is untouched — graduating any showcase archive batches with a rebuild window per P6. Invariants: determinism (D6 — byte-reproducible assignment; no RNG, pinned origin); no-thinning via union completeness (§4.5 constraint 2: `⋃_z L_z` = the full feature set — every feature stored exactly once, storage O(N) vs O(|Z|·N)); random access unchanged (tiles are ordinary tiles). Rollback: drop the flag → the replication build, byte-identical to before.

**Tests.** Determinism: extend `crates/stt-build/tests/reproducible_pipeline.rs` (exists) — two `--additive-lod` builds byte-identical (`diff -r` class). Property (new `crates/stt-build/tests/additive_home_zoom.rs`): every feature id at exactly one zoom; union-of-zooms id set == input id set; synthesized band ≡ an explicit `--min-zoom-field`/`--max-zoom-field` build of the same assignment. Guard (rejected-design boundary): animated fixture where a space-only voxel key would leave a temporal bucket under-covered — assert the 4D key holds per-bucket coverage (encodes the §4.4 regression cheaply). Golden fixture: one new additive fixture; existing `crates/stt-core/tests/v2_golden.rs` pins unchanged. TS integration: extend `poopdeck:packages/core/test/additive-lod.test.ts` (exists) for manifest-driven `lodMode` defaulting, and `capabilities.test.ts` for refusal.

**Evaluation.** Metric: stored rows per distinct feature (target ≈ 1.0) and archive bytes vs the replication build; per-zoom occupied-voxel coverage within tolerance of the replication build at screen resolution (the §4.5 objective; script precedent `lidar_summarize_eval.py`). Baseline: earthquakes-v2 523 K rows / 47.5 K features, the recorded "up to 11×" doctrine cost. Harness: `stt-optimize inspect` + the density profile emitter; runs on whichever dataset fires the trigger.

**Risks & dependencies.** Depends on DT-1 (declaration + capability) and benefits from M1 (trigger measurement) and M2 (a two-pass build is the natural host for a dataset-global assignment pass). Pitched-camera cost of additive mode (union over `[minZoom..z]` multiplies today's per-zoom enumeration) is repaired by FS-1/FS-2, whose walk emits interior nodes for free — sequence additive graduation after FS lands. Borders: bucket-keyed-grid register entry (hard); importance-column absence changes _which_ feature wins a voxel, never whether coverage holds — document it. The trigger may never fire; the Phase 5 deliverable is the reviewed design plus instrumentation, and that is acceptable.

#### DT-3 — M4/MinMaxLTTB reduced temporal tiers (the declared-variant escape) `§13.3 §2.4 · M8 · Phase 5, trigger-gated`

**Change.** The first genuinely feature-_reducing_ tier, inside the doctrine because it is declared and additive (Θ₂ in §13.3's partition; base tier stays lossless and untouched). Implement in-house — the "tsdownsample is reusable" claim was refuted 0-3 (stt-packed-format-decisions.md:433-441). Build: new `crates/stt-build/src/reduced_tier.rs` (new):

```rust
pub fn reduce_m4(rows: &TileRows, bins: &TimeBins, value_col: &str) -> Vec<RowRef>       // (new)
pub fn reduce_minmaxlttb(rows: &TileRows, bins: &TimeBins, value_col: &str, ratio: u32) -> Vec<RowRef> // (new)
```

Selection-only — the output is a subset of real features (min/max/first/last per pixel-aligned time bin for M4; MinMax-preselect-then-LTTB for the shape-preserving fallback), never synthesized values, mirroring the simplifier's subset-only constraint. Emitted as tiles under a DT-1 `reduced` variant at `(bucket_ms, max_zoom_level)` coordinates, addressed by `variant_id` + `temporal_bucket_ms`. CLI: `--reduced-tier "m4:<bucket>@<zoom>[,...]"` (new), parsed alongside `parse_temporal_lod` (crates/stt-build/src/build_options.rs:57-82, verified: `<dur>[@zoom]` grammar to reuse). Advisor: `stt-optimize` proposes it with a _measured_ `addedTierBytes` via the M1 sample oracle (closing §12.4's gap for this tier class), suggestion-only. Reader: substitution only behind an explicit tier policy — the existing per-zoom LOD dispatch (`scrubTemporalLodBucketMs` at spatiotemporal-tileset.ts:1496-1514 and `fetchSelectionTilesForZoom` at :1523-1539, plus `STTArchive.pickTemporalLodForZoom` at poopdeck:packages/core/src/archive.ts:3758 and `getTileIdsInBoundsForTemporalLod` at :3590) extends to prefer a declared reduced tier when the archive declares one AND the app opted in and the reader understands `method` (DT-1's negotiation rule).

**Trigger (spelled out).** Recorded: "base-tier bandwidth-bound wide-window playback" (§13.3 Gap). Operationalized: on a named route at zoom ≤ candidate cutoff, QoE stall counters non-zero AND fetch bytes/sim-second exceed the dual-EWMA throughput estimate — both signals already emitted (`getQoeStats()`, the Phase 0 harness inventory).

**Architecture.** Additive new objects only — existing blobs, directories, and golden pins unchanged; deploys are incremental (13.1). Flag default OFF. Determinism: bin edges derived from the declared bucket grid, ties broken by feature id — byte-reproducible. This item is what un-gates DT-4's temporal axis (§11.6's G5 is "counted out until a genuinely byte-cheaper feature-reducing tier exists — which is exactly this"). It also reshapes §2.4's storage term from duplication to sublinear — a reason NOT to over-invest in the union-tier cutoff optimizer beyond the §4-batch default. Rollback: stop emitting; readers that never saw the variant never miss it.

**Tests.** Property (the load-bearing one): M4 pixel-identity — rasterize value-over-time per pixel column from base vs reduced at aligned bins and assert the identical touched-pixel set (the two-color non-AA caveat encoded in the test as its stated domain); MinMaxLTTB bounded shape deviation. Determinism: byte-identical re-run of a `--reduced-tier` build (extend `reproducible_pipeline.rs`). Conformance: `validate_cli.rs` rejects an unregistered method; content check per DT-1 rule (2)'s reduced branch (declared, method-valid, base untouched). Golden: one new fixture; existing pins unchanged. TS: extend `poopdeck:packages/core/test/temporal-lod.test.ts` (exists) for reduced-tier pick, following the `summary-tier-dispatch.test.ts` dispatch pattern; guard test: with policy off, reduced content is never substituted for base (the never-silent boundary).

**Evaluation.** Metric: bytes fetched per sim-second during zoomed-out, wide-window playback at z ≤ cutoff. Baseline: capture on the trigger dataset with `poopdeck:tools/bench/src/frame-cost.mjs` (exists; `ROUTE=` support) + QoE counters — the capture itself rides the Phase 0 harness-inventory item. Acceptance: ≥ 10× byte reduction (recorded bound: up to ~100×, the hard 4·w M4 bound) with the pixel-identity property green and zero change to base-tier behavior.

**Risks & dependencies.** Depends on DT-1 (declaration) and M1 (measured `addedTierBytes`). The M4 guarantee is exact only for its rendering model — the spec text must scope the contract to what `method` actually promises, not "looks the same." Borders: no-thinning register (this IS the sanctioned escape — any drift toward applying reduction to the base tier is out of bounds); TEMPLATE/TILE_META partition (extend). Trigger may not fire this cycle; design + emitter skeleton reviewed is the Phase 5 floor.

#### DT-4 — Scrub-tier decision tree (measure, then wire or delete) `§11.6 · M8 · Phase 0 measurements → Phase 5`

**Change.** No new mechanism — a recorded decision procedure over the Phase 0 scrub measurements (design §6 Phase 0, bullet 4: time-to-first-pixel, fresh-frame fraction, bytes-during-scrub vs baseline, settle latency, pop count — the five §11.6 names, all currently unmeasured), run on the storm, `/drive`, and multi-source composite routes. The wiring under decision is fully implemented and DEFAULT OFF with zero showcase call sites: governor interactive bit (playback-governor.ts:895-939 per §11.6), tileset `scrubLod` — spatial drop via `effectiveSelectionZoom` (spatiotemporal-tileset.ts:1472-1484), temporal pick via `scrubTemporalLodBucketMs` (:1496-1514) and `fetchSelectionTilesForZoom` (:1523-1539). The tree:

1. **Spatial axis already at target with `scrubLod` off** (parents resident via best-available make previews free) → **delete** the scrub-LOD tileset policy per §11.6's recorded deletion clause, keeping the governor's preview/commit split (that part is register-pinned). `scrub-lod.test.ts` retires with it.
2. **Spatial axis wins measurably** → wire `scrubLod: { spatial: true }` as a showcase chassis default (the missing call sites), temporal stays off.
3. **Temporal axis (G5)**: stays counted out until DT-3's reduced tier exists — the current coarse aggregator re-buckets without reducing, so a temporal drop fetches _fatter_ tiles, violating §11.6's byte-discipline constraint. When DT-3 lands on a dataset, re-run the same five measurements with `scrubLod: { temporal: true }` resolving to the reduced variant; the existing pick already sizes the bucket to timeline pixel width, so the mechanism is tier-agnostic once a byte-cheaper tier is declared.

Preserved absolutely (register): preview-never-gates (G7), the restore invariant, bytes-during-drag ≤ baseline.

**Architecture.** Client-only, BYTE-NEUTRAL, reversible per branch (the kill switch is the absent `scrubLod` option). Sits entirely downstream of declared tiers — DT-4 consumes what DT-1/DT-3 declare, it declares nothing itself. Rollback: remove the chassis default; the tileset policy is inert without it.

**Tests.** Extend `poopdeck:packages/core/test/scrub-lod.test.ts` (exists) and `poopdeck:packages/playback/test/playback-governor.test.ts` (exists): branch-2 default-on chassis behavior; branch-3 reduced-variant substitution under the interactive bit; the standing guards (readiness never sees the preview tier; fine tier restored before commit readiness) stay green in every branch. If branch 1 wins, the deletion lands with a governor-side regression test proving preview/commit semantics survive the policy's removal.

**Evaluation.** Harness: NEEDS-HARNESS — the five-metric scrub QoE capture is the Phase 0 infrastructure item (the design doc marks it cheap and prerequisite; the keep-vs-delete decision "hinges on running the specified measurement"). Metrics/units: time-to-first-pixel (ms), fresh-frame fraction (ratio), bytes during scrub vs no-policy baseline (MB), settle latency (ms), pop count. Acceptance: the decision itself, recorded with numbers; if wired, fresh-frame fraction rises with bytes-during-scrub at or below baseline on the measured routes.

**Risks & dependencies.** Depends on the Phase 0 measurement run and (branch 3 only) DT-3. Do not fit `seekSettleMs = 200 ms` here — that is a D1 constant belonging to the M5/M6 track. Borders the §11.6 register invariants (hard) and §8.2's scrub zoom-drop constant — which FS-work subsumes, so no tuning of the drop either.

#### DT-5 — Interval-segregation spec erratum (draft, condition-gated) `§2.2 · M8 · Phase 5`

**Change.** A drafted erratum, produced **only if** the condition below holds — per design §6 Phase 5: "drafted only if a heavy-tailed-duration dataset shows the read-amplification in the K10-refreshed numbers." The pathology (§2.2): single start-bucket placement (`chunk_by_temporal_bucket`, crates/stt-build/src/tiler.rs:1444-1456; normative spec §3.1) plus the exact `time_end` bound means one decade-long interval makes its whole tile a permanent fetch for every later window, dragging all co-resident short features with it. Two deliverables:

- **Trigger instrumentation** (built now): `stt-optimize inspect --read-amp <window>` (new flag) — a directory-only walk (the fast `--sample 0` class) computing, from per-entry `(time_start, time_end, cover_t_min)` bounds and a sliding window, the read-amplification share `A` = expected fetched bytes attributable to tiles kept resident solely by a long-lived `time_end`, plus the residual-lifetime histogram. Deterministic, no decode.
- **The draft** (written iff triggered): relax time-model §3.1's one-tile-per-(cell, bucket) rule via a per-bucket **long-lived sibling tile**: features whose residual lifetime spans > K buckets are placed in a sibling tile at the same `(z,x,y,t)`, each tile carrying its own exact tight bounds — so the short tile's `time_end` collapses to the short features' maximum and stops pinning it. Note the precise MUST being relaxed: this keeps single placement (`|A_i| = 1`, no duplicated bytes, dedup intact) and relaxes only tile _cardinality_ per bucket — strictly weaker than the bounded-replication alternative (§2.2 Gap), which the draft proposes only as a fallback if segregation measures insufficient. Addressing/representation (a directory-visible sibling vs a DT-1 declared layout note) is the draft's central open question, to be resolved against old-reader compatibility: the reader prune predicate (spec §5; docs/spec/time-model.md:239-251) is unchanged either way — siblings simply present tighter bounds.

**Condition (spelled out).** On the K10-refreshed cold-start/playback captures, a fleet dataset with heavy-tailed interval durations (trips, storm cells — trajectories escape via boundary slicing, clip.rs:593-701 per §2.2; points/polygons do not) shows `A` ≥ a material share of playback fetch bytes (proposed 25%, fit per P1 before adoption). No number, no erratum.

**Architecture.** Drafting is paperwork; implementation, if it ever happens, is BYTE-CHANGING builder work that rides a _future_ rebuild window (P6) — explicitly not R1, which is already scoped. Preserves: exact per-tile bounds (register: within the one-bound-per-tile family the computed bounds are optimal — the draft multiplies the family, keeps exactness per member), no-thinning (placement change, zero content change), determinism (K and the split are pure functions of feature times).

**Tests.** For the instrumentation (now): unit tests on synthetic interval fixtures — one decade-long feature in a busy tile reproduces the §2.2 pathology and `--read-amp` reports it; determinism of the metric across runs; a fixed-grid-only guard (adaptive-temporal cells, §2.3, are out of scope and the flag must say so, not silently compute nonsense). No builder tests until an accepted erratum schedules implementation.

**Evaluation.** Metric: bytes-fetched-per-query-window at fixed archive size (§2.2's stated objective). Baseline: the measured `A` on trips/storm datasets post-K10. Acceptance for drafting = condition met; acceptance for the erratum = spec review. Harness: the new inspect flag; K10 capture per Phase 0/O4.

**Risks & dependencies.** Depends on the K10 re-capture (Phase 0, re-run again after R1 per Phase 4) and DT-1 (if the declared-layout representation wins). Borders the spec §8 no-duplication MUST — the reason this is an erratum, not a feature. The bucket width Δ (§2.1) scales the whole term; if M4's workload model later re-derives Δ per dataset, re-check the condition before drafting.

_Downstream since 2026-08-26: FS-1 … FS-3 change files in the poopdeck.gl repository; their open half lives in that register._

#### FS-1 — Frustum-quadtree cover primitive in core `§8.1 §8.2 · Wave 3/A1 · Phase 5`

**Change.** New module `poopdeck:packages/core/src/geo/frustum-cover.ts` (new), framework-free plain-data API (the core package carries zero deck dependency — guarded by `poopdeck:packages/core/test/kernel-framework-free.test.ts`; plane extraction stays in each chassis):

```ts
export interface FrustumPlane {
  normal: [number, number, number];
  distance: number;
}
export interface FrustumCoverOptions {
  minZoom: number;
  maxZoom: number;
  cameraZoom: number; // fractional
  cameraPosition: [number, number, number]; // same space as the planes
  elevationBounds?: [number, number]; // zRange / A3 altitude slab, metres
}
export function coverFrustumQuadtree(
  planes: readonly FrustumPlane[],
  opts: FrustumCoverOptions,
): TileId[] | null; // (new) — a quadtree CUT of (z,x,y) cells
```

The walk ports the shape of deck's `getOSMTileIndices` (the Wave 3/A1 record in docs/roadmap/tile-loading-3d-2026-07.md): root-down; per node, test the node's 3D Mercator AABB (dilated by `elevationBounds`) against the plane set — OUT prunes, IN/INTERSECT descends; per-branch target zoom `z_t(node) = clamp(⌊cameraZoom − log2(dist(node)/refDist)⌋, minZoom, maxZoom)`; refine while `node.z < z_t`, emit at the cut. Under the **binding bounds contract** (tile-loading-3d §4): every predicate uncertainty resolves conservative — a boundary-ambiguous plane test is INTERSECT, never OUT (under-cover is the blank-region bug class; a blank map has −∞ utility per §8.2); non-finite planes ⇒ return `null` and the caller keeps its previous selection, the exact discipline `normalizeViewportBounds` established (poopdeck:packages/core/src/geo/viewport-bounds.ts:102-117). Antimeridian: walk the wrapped root copy when the frustum crosses the seam and emit `x mod 2^z`, matching the archive scan's wrap-at-emit contract (`tileXSpanForLonRange`); latitude is inherently clamped by the ±85.05° tree extent (`MAX_MERCATOR_LAT`, viewport-bounds.ts:150).

**Architecture.** A pure, deterministic, output-sensitive function (O(cells emitted)); no new runtime dependency in core. BYTE-NEUTRAL — ships anytime; inert until FS-2 consumes it. This is the piece that changes §8.2's problem from "one global integer zoom" to "a per-node quadtree cut" — which is why §8.2's `(C, h)` constants must not be tuned beforehand (design §6 Phase 5: the frustum walk _subsumes_ §8.2; any tuning effort spent on the AABB stopgap is discarded when this lands). Rollback: unused export.

**Tests.** New `poopdeck:packages/core/test/frustum-cover.test.ts`. Unit: a flat unrotated camera degenerates to exactly the `boundsToTiles` box at the same zoom; antimeridian-crossing camera emits wrapped columns; polar camera stays bounded; `elevationBounds` dilation adds the far columns a tall column demo needs (A3 parity). Property, on the existing **pitch×bearing matrix** — 24 bearings × 18 pitches = 432 real cameras (`BEARINGS`/`PITCHES`, poopdeck:packages/layers/test/chassis-viewport-bounds.test.ts:173-174) with its ground-sampling `drawnTiles` oracle (:120-171): every drawn tile has an ancestor-or-self in the emitted cut — zero misses at all 432 cameras (the same invariant the AABB matrix pins today at :230-245); cut size ≤ the AABB single-zoom enumeration at equal coverage; above-horizon cameras (`isAboveHorizon`, :196-201) never emit an unbounded set. Determinism: identical output across repeated calls (cheap; pure function). Guard (rejected-design boundary): a deliberately garbage plane set returns `null`, never a truncated cut — truncation is the blank-region symptom class.

**Evaluation.** Metric: cells in the cut vs cells the AABB enumerates, per camera. Baselines recorded in tile-loading-3d-2026-07.md: **47 tiles across z5–z8 vs 754 at z8** (Wave 3/A1) and **832 cells at pitch 85/z8 vs ~12 flat** (§4.4). Acceptance: ≥10× fewer cells at the pitch ≥ 60 cameras of the matrix, with the zero-miss property green. Harness: the test suite itself; no browser needed at this layer.

**Risks & dependencies.** The plane-space convention (deck common space vs core Mercator) must be normalized in the chassis adapter (FS-2), never inside core — a convention bug here presents as systematic under-cover, which the 432-camera oracle exists to catch. The per-branch zoom formula is an SSE approximation; keep it the simple `log2(distance)` form — the exact per-tile SSE constraint (§8.2's "formal ideal") is the follow-on refinement, not this item.

#### FS-2 — Tileset id-list selection path + chassis wiring `§8.1 §8.2 · Wave 3/A1 · Phase 5`

**Change.** Consume the cut. Tileset: the viewport input gains `tileCells?: TileId[]` (new) as an alternative to `{bounds, zoom}` — Wave 3/A1's recorded surgery note: "`selectAndLoadTiles` already builds `neededTileKeys` from an id list, so the surgery is contained" (bounds stay supplied alongside, for the flush-tolerance and coverage-index math). In `selectAndLoadTiles` (poopdeck:packages/core/src/spatiotemporal-tileset.ts:1782), when `tileCells` is present the per-zoom parallel enumeration (`zoomLevels.map` + `fetchSelectionTilesForZoom`, :1899-1915) is replaced by a grouped directory slice over exactly the named cells: new archive API

```ts
async getAvailableTilesForCells(
  cells: readonly TileId[], timeRange: { start: number; end: number },
): Promise<TileId[]>   // (new, poopdeck:packages/core/src/archive.ts)
```

walking only those cells' directory columns with the existing interval-overlap filter (the occupied-cell index path behind `MAX_QUERY_SCAN_CELLS` is the precedent for cell-addressed scans). Preserved semantics: per-zoom tier dispatch (`pickTierForZoom`; scrub bucket via the existing `scrubBucketMs` parameter, :1523-1539); the scrub spatial drop becomes a uniform −k applied to per-branch targets (lifting `effectiveSelectionZoom`'s clamp logic, :1472-1484); additive `lodMode` replaces its `[minZoom..z]` loop (:1715-1718) with the walk's visited interior nodes — the natural union, at zero extra enumeration; the stale-pass generation guard (:1897) wraps the new await unchanged. The spatial prefetch-flush trigger `zoom !== this.lastSpatialZoom` (:1851) is replaced by a cut signature (the `quantizedSpatialKey` pattern, :1768) — **change the key only, not the constants**: `SPATIAL_FLUSH_TOLERANCE`, the budget `(C, h)`, and the prefetch-invalidation behavior are D6's "one physical cost, three constants" and §8.2 register territory. Chassis (deck): extract planes via `viewport.getFrustumPlanes()` (available on `@deck.gl/core` ^9.3.2, poopdeck:packages/layers/package.json:52), convert to core space, call `coverFrustumQuadtree`; on `null` or a foreign viewport, fall back to today's `getViewportBounds` (poopdeck:packages/layers/src/layers/spatiotemporal-layer.ts:2141) → `normalizeViewportBounds` → budget path. Rollout: layer prop `selectionMode?: 'aabb' | 'frustum'` (new), default `'aabb'` until FS-3 acceptance passes, then flipped.

**Architecture.** BYTE-NEUTRAL, client-only, but the largest single change in the hottest path since the 2026-07-26 bounds fix — hence the default-off flag and the FS-3 acceptance gate before the flip. Invariants: coverage soundness (conservative superset end to end), bounded memory (the cut is hundreds of cells, not `4^z`), single normalization authority (the AABB fallback keeps viewport-bounds.ts as its one repair point). Rollback: flip `selectionMode` back; both paths coexist by design, and the budget backstop stays for foreign viewports permanently (tile-loading-3d §4.4: "expect it to stay as the backstop").

**Tests.** Extend `poopdeck:packages/layers/test/chassis-viewport-bounds.test.ts` — the 432-camera matrix runs the frustum path under the same zero-miss and named-camera assertions it runs the AABB path (including the shipped-demo cameras pinned at :348-364: storm-4d-isolines p62/b20, earthquake-columns p55/b15). Extend `poopdeck:packages/core/test/selection-hardening.test.ts` and `tileset-viewport-normalize.test.ts` (both exist): `tileCells` path under stale-generation supersession, directory-failure retry (the `lastSelectKey` clearing at :1927 must hold). Extend `prefetch-flush.test.ts` / `prefetch-flush-needed.test.ts`: cut-signature flush fires on a real move, not on sub-tile drift. Extend `summary-tier-dispatch.test.ts` and `scrub-lod.test.ts`: tier dispatch over mixed-zoom cells. Integration: extend `additive-lod.test.ts` — walk-emitted union equals the loop enumeration on a flat camera. New unit tests for `getAvailableTilesForCells` in `poopdeck:packages/core/test/archive.test.ts` (exists): cell-addressed slice equals the bounds slice restricted to those cells, wrap-aware.

**Evaluation.** Harness: `poopdeck:tools/bench/src/frame-cost.mjs` (exists; `ROUTE=` for `/drive`, `/worlds`) plus fetch/decode QoE counters. Metrics: tiles requested, bytes fetched, decodes per pitched frame. Baseline: the recorded 754-vs-47 and 832-vs-12 figures plus the 2026-08-03 frame-cost captures of the pitched routes. Acceptance: **~10–16× fetch reduction at the shipped pitched cameras at verified coverage** (the O5 line), zero regression on flat-camera demos, `runwayEvictions` not regressed.

**Risks & dependencies.** Depends on FS-1. The volumetric-demo browser-verify queue (roadmap L2) grows by one pass — schedule it with the flag flip; per the project's visual-verify preference the aesthetic check is done in-browser by the user, not by a screenshot loop. Do-not-touch borders: prefetch supersession exemptions (§9.2 register), the exact-selection/quantized-side-effects two-tier split (§8.5 register), budget constants (§8.2 — subsumed, not tuned). Draw-call-bound routes (`/drive`, earthquake) will not gain fps from fewer fetches — that is the recorded separate draw-consolidation item; do not let its flat fps discredit this item's byte metric.

#### FS-3 — Mixed-zoom working-set semantics and acceptance `§8.2 §8.3 · Wave 3/A1 · Phase 5`

**Change.** The cut breaks a structural assumption downstream: `getVisibleTiles` (spatiotemporal-tileset.ts:4065) derives ONE `primaryZoom` as the max zoom of the needed set (:4092-4098) and treats everything coarser as fallback — under a quadtree cut the _intended_ cover is mixed-zoom (near-field z11 nodes and far-field z6 nodes are all primary). Generalize: the cut arrives as an antichain by construction; pass 1 treats every cut member as primary-at-its-own-zoom (cover keys become cell-at-cut-zoom rather than the global-`primaryZoom` `"x/y/t"` strings at :4106-4110); the parent-fallback and stand-in passes (§8.3's three-pass greedy, :4144-4302) run relative to each cut node with their existing depth caps (`PARENT_FALLBACK_LEVELS` = 4, `CHILD_LOOKAHEAD_LEVELS` = 2, `ANCESTOR_LOOKBACK_LEVELS` = 2) unchanged — the greedy is register-pinned "optimal given its caps": extend its keying, never replace its passes. Readiness and the buffered runway aggregate over cut members (per-node readiness, AND-reduced), preserving F5's separation of readiness write-off from fetch eligibility (tile-loading-3d §5b) and the eviction hard-exclusion of `neededTileKeys` per member. `fitZoomToCellBudget` (poopdeck:packages/core/src/tile-budget.ts:145) goes inert under frustum selection — **verify inertness** (assert cut size ≪ budget across the matrix), then keep it as the AABB-fallback backstop exactly as tile-loading-3d §4.4 instructs; no tuning of `DEFAULT_VIEWPORT_CELL_BUDGET` = 256 or hysteresis 0.25 before, during, or after without new evidence.

**Architecture.** BYTE-NEUTRAL client work completing FS-2; the delivery contract it must preserve is §8.3's: each visible cell reaches the renderer with **at most one** cover (the antichain), and no cell that residency could fill is blank. The known historical failure mode to design against is the double-draw: the 2026-07-29 fix removed a parent that double-drew every dot because "covered" was mis-defined under a partial view — a mixed-zoom keying bug reintroduces exactly that class. Rollback: rides FS-2's `selectionMode` flag; the single-primary path remains the `'aabb'` branch.

**Tests.** Extend `poopdeck:packages/core/test/parent-fallback-clamp.test.ts` and `ancestor-standin-overlap.test.ts` (both exist): fallback and stand-in relative to a mixed-zoom cut, slack-ring semantics per cut node. Extend `tile-budget.test.ts`: the inertness assertion. Extend `buffered-runway.test.ts` and `eviction-playhead-tiers.test.ts`: mixed-zoom needed set in runway math and eviction hard-exclusion. New `poopdeck:packages/core/test/mixed-zoom-cover.test.ts`: (a) the delivered set is an antichain over visible cells at every matrix camera — the single-cover contract; (b) the double-draw regression guard (a cut node's ancestor is never delivered alongside it for the same visible cell); (c) no blank cell that residency could fill, including during a cut transition. Simulation: a camera sweep pitch 0→85 at fixed center asserting continuous coverage while the cut re-shapes (the transition frames are where a gap would hide).

**Evaluation.** This item carries the program's **coverage-equivalence acceptance**: on the 432-camera matrix plus the shipped demo cameras — zero `drawnTiles` misses, single-cover invariant green, and fetch bytes ~10–16× down at high pitch vs the AABB baseline (O5: "pitched-camera fetch bytes ~10× down at verified coverage"). Secondary: fps via `frame-cost.mjs` reported but not gated (see FS-2 risk note). Only after this acceptance does FS-2's default flip — and only after the flip is any question about §8.2's constants even admissible, at which point the expected answer is that they are inert.

**Risks & dependencies.** Depends on FS-1 + FS-2. Borders three register entries hard: the fallback-cover greedy (extend, never replace), the two-threshold gating shape (§11.1 — readiness aggregation changes inputs, never the controller), and §8.2 (subsumed — the explicit Phase 5 instruction that no §8.2 tuning happens before this work is enforced here by making the budget-inertness assertion part of acceptance). Additive-mode datasets (DT-2's consumers) bypass the antichain entirely (:4074-4081) — the walk's interior-node union must be covered by the extended `additive-lod` tests, not assumed.
